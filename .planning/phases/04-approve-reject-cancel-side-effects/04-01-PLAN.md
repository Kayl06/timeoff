---
phase: 04-approve-reject-cancel-side-effects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/modules/leave-balances/balance-arithmetic.ts
  - packages/database/src/modules/leave-balances/balance-arithmetic.test.ts
  - packages/database/src/modules/leave-balances/year-from-start-date.ts
  - packages/database/src/modules/leave-balances/year-from-start-date.test.ts
  - packages/database/src/modules/notifications/leave-request-notification-type.ts
  - packages/database/src/modules/notifications/leave-request-notification-type.test.ts
  - packages/database/package.json
autonomous: true
requirements:
  - BAL-03
  - BAL-04
  - NOTIF-01
  - NOTIF-02
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "applyApprovalDeduct adds total_days to used_days and subtracts the same amount from remaining_days without using total_allowance (BAL-03, D-01)."
    - "applyApprovalRestore is the inverse of applyApprovalDeduct (BAL-04, D-06)."
    - "remaining_days may go negative when used exceeds remaining; the helper does not clamp to 0 (BAL-03, D-04)."
    - "yearFromStartDate('2025-12-31') returns 2025 from the ISO prefix (BAL-03, D-02)."
    - "shouldApplyApprovalDeduct is true only for pending; shouldApplyApprovalRestore is true only for approved (BAL-04, D-05, D-06, D-07)."
    - "leaveRequestNotificationType maps approved to request_approved and rejected to request_rejected (NOTIF-01, NOTIF-02, D-10)."
    - "Count 0 stays visible on the stats card. Do not hide the card. Do not invent a list empty-state this phase (D-12)."
    - "Existing dialog pending flags (isApprovingLeaveRequest). No new spinner chrome."
    - "Failed PATCH: existing sonner error from payload.error (including 409 missing balance). Table stays."
    - "After employee refetch, unread count includes persisted request_approved / request_rejected rows."
    - "After approve/cancel refetch, remaining_days match deducted/restored rows (Phase 3 chrome)."
    - "Unchanged Phase 3 type-order rules."
    - "Existing table; no new columns or bulk chrome this phase."
    - "Integer count only. One persisted notice → count at least 1 for that employee."
  artifacts:
    - path: "packages/database/src/modules/leave-balances/balance-arithmetic.ts"
      provides: "applyApprovalDeduct applyApprovalRestore shouldApplyApprovalDeduct shouldApplyApprovalRestore"
      exports: ["applyApprovalDeduct", "applyApprovalRestore", "shouldApplyApprovalDeduct", "shouldApplyApprovalRestore"]
    - path: "packages/database/src/modules/leave-balances/year-from-start-date.ts"
      provides: "yearFromStartDate"
      exports: ["yearFromStartDate"]
    - path: "packages/database/src/modules/notifications/leave-request-notification-type.ts"
      provides: "leaveRequestNotificationType"
      exports: ["leaveRequestNotificationType"]
    - path: "packages/database/package.json"
      provides: "node:test script lists the three new test files"
      contains: "balance-arithmetic.test.ts"
  key_links:
    - from: "packages/database/src/modules/leave-balances/service.ts"
      to: "packages/database/src/modules/leave-balances/balance-arithmetic.ts"
      via: "04-03 updateBalanceAfterApproval will call applyApprovalDeduct"
      pattern: "applyApprovalDeduct"
    - from: "packages/database/src/modules/notifications/service.ts"
      to: "packages/database/src/modules/notifications/leave-request-notification-type.ts"
      via: "04-03 createLeaveRequestNotification will call leaveRequestNotificationType"
      pattern: "leaveRequestNotificationType"
  prohibitions:
    - statement: "Do not restyle approve/reject/cancel/delete dialogs or the Leave Balance card."
    - statement: "Do not derive remaining_days from total_allowance minus used_days."
    - statement: "Do not parse start_date with Date#getFullYear."
    - statement: "Do not add a Supabase migration."
    - statement: "Do not call deduct helpers from POST /api/leave-requests/bulk."
    - statement: "Do not put Resend in packages/database."
---

## Phase Goal

**As a** manager or employee, **I want to** approve, reject, or cancel a single leave request so remaining days deduct or restore, a durable audit row uses my real user id, and the employee is notified in-app and by email, **so that** the request lifecycle is truthful on a shared deployment.

<objective>
Wave 0 Nyquist helpers for BAL-03 delta deduct, BAL-04 restore, D-02 year from start_date, and D-10 CHECK-valid notification types so the 04-03 tracer can wire them without inventing arithmetic.

Purpose: Lock D-01: D-02: D-04: D-05: D-06: D-07: D-10: in pure node:test before any PostgREST write.
Output: applyApprovalDeduct, applyApprovalRestore, yearFromStartDate, leaveRequestNotificationType, registered database tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-CONTEXT.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-RESEARCH.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-PATTERNS.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-VALIDATION.md
@packages/database/src/modules/leave-balances/plan-default-inserts.ts
@packages/database/src/modules/leave-balances/plan-default-inserts.test.ts
@packages/database/package.json
</context>

<interfaces>
From 04-PATTERNS.md Pattern 1 and RESEARCH Pattern 1:
applyApprovalDeduct(balance, totalDays) returns { used_days, remaining_days } with used_days plus totalDays and remaining_days minus totalDays. applyApprovalRestore is the inverse. Do not return carried_over. Do not read total_allowance.

From 04-CONTEXT.md D-02:
yearFromStartDate(startDate: string): number uses Number(startDate.slice(0, 4)) and throws when that is not an integer. Do not use new Date(startDate).getFullYear().

From 04-CONTEXT.md D-05 D-06 D-07:
shouldApplyApprovalDeduct(status) is true only when status is pending. shouldApplyApprovalRestore(status) is true only when status is approved.

From 04-CONTEXT.md D-10 and schema CHECK:
leaveRequestNotificationType('approved') is request_approved. leaveRequestNotificationType('rejected') is request_rejected. Throw on any other action.

From plan-default-inserts.test.ts:
node:assert/strict, node:test describe/it, import leaf with explicit .ts suffix for Node 26 strip-types.
</interfaces>

## Artifacts this phase produces

- `applyApprovalDeduct` / `applyApprovalRestore` / `shouldApplyApprovalDeduct` / `shouldApplyApprovalRestore` in `packages/database/src/modules/leave-balances/balance-arithmetic.ts`
- `yearFromStartDate` in `packages/database/src/modules/leave-balances/year-from-start-date.ts`
- `leaveRequestNotificationType` in `packages/database/src/modules/notifications/leave-request-notification-type.ts`
- test files registered on `@timeoff/database` test script

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0: deduct restore year and status gates</name>
  <reversibility rating="costly">D-01 remaining_days is deducted in place; a wrong formula permanently skews every later approve.</reversibility>
  <files>packages/database/src/modules/leave-balances/balance-arithmetic.ts, packages/database/src/modules/leave-balances/balance-arithmetic.test.ts, packages/database/src/modules/leave-balances/year-from-start-date.ts, packages/database/src/modules/leave-balances/year-from-start-date.test.ts, packages/database/package.json</files>
  <read_first>packages/database/src/modules/leave-balances/plan-default-inserts.ts, packages/database/src/modules/leave-balances/plan-default-inserts.test.ts, packages/database/package.json, .planning/phases/04-approve-reject-cancel-side-effects/04-CONTEXT.md, .planning/phases/04-approve-reject-cancel-side-effects/04-PATTERNS.md</read_first>
  <behavior>
    - applyApprovalDeduct({ used_days: 2, remaining_days: 18 }, 3) returns used_days 5 and remaining_days 15 (BAL-03, D-01)
    - applyApprovalDeduct with remaining_days 1 and totalDays 3 returns remaining_days -2 (D-04)
    - applyApprovalRestore of a deduct result equals the original used_days and remaining_days (BAL-04, D-06)
    - applyApprovalDeduct does not read or return total_allowance or carried_over (D-01)
    - yearFromStartDate('2025-12-31') is 2025 (D-02)
    - yearFromStartDate throws on a string whose first four characters are not an integer (D-02)
    - shouldApplyApprovalDeduct is true for pending and false for approved, rejected, and cancelled (D-05, D-07)
    - shouldApplyApprovalRestore is true only for approved (D-06)
  </behavior>
  <action>
    Create balance-arithmetic.ts as a pure kebab-case helper next to plan-default-inserts.ts (file-level JSDoc). Export applyApprovalDeduct and applyApprovalRestore per D-01 and D-06: mutate used_days and remaining_days by plus-or-minus totalDays. Export shouldApplyApprovalDeduct(status) true only for pending and shouldApplyApprovalRestore(status) true only for approved per D-05 D-06 D-07. Do not clamp remaining_days per D-04. Do not import a database client. Do not compute remaining from total_allowance.

    Create year-from-start-date.ts exporting yearFromStartDate that reads the ISO date prefix per D-02. Throw Error with message Invalid start_date when the prefix is not an integer. Do not construct a Date.

    Create the two node:test files with node:assert/strict and .ts specifiers. Cover carry-over-untouched deduct, negative remaining, restore inverse, December 31 year, and both status gates. Append both test paths to packages/database scripts.test after the existing plan-default-inserts.test.ts entry. Keep that existing filename. Do not add Vitest.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/database</automated>
  </verify>
  <acceptance_criteria>
    - applyApprovalDeduct and applyApprovalRestore are exported from balance-arithmetic.ts
    - yearFromStartDate is exported from year-from-start-date.ts
    - packages/database package.json scripts.test includes balance-arithmetic.test.ts and year-from-start-date.test.ts and plan-default-inserts.test.ts
    - npm test --workspace=@timeoff/database exits 0
  </acceptance_criteria>
  <done>Deduct, restore, year, and status gates are unit-tested (BAL-03, BAL-04, D-01, D-02, D-04, D-05, D-06, D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: CHECK-valid leave-request notification type</name>
  <reversibility rating="costly">D-10 type strings must pass notifications.type CHECK; invalid categories never persist.</reversibility>
  <files>packages/database/src/modules/notifications/leave-request-notification-type.ts, packages/database/src/modules/notifications/leave-request-notification-type.test.ts, packages/database/package.json</files>
  <read_first>packages/database/src/modules/notifications/service.ts, packages/database/migrations/001_initial_schema.sql, packages/database/package.json, .planning/phases/04-approve-reject-cancel-side-effects/04-CONTEXT.md</read_first>
  <behavior>
    - leaveRequestNotificationType('approved') returns request_approved (NOTIF-01, D-10)
    - leaveRequestNotificationType('rejected') returns request_rejected (NOTIF-02, D-10)
    - leaveRequestNotificationType throws on any other action string
    - leaveRequestNotificationCopy('approved') title is Leave Request approved and message is Your leave request has been approved. (D-11)
  </behavior>
  <action>
    Create leave-request-notification-type.ts as a pure mapper. Export leaveRequestNotificationType(action) that returns request_approved or request_rejected per D-10. Export leaveRequestNotificationCopy(action) that keeps title Leave Request ${action} and message Your leave request has been ${action.toLowerCase()}. per D-11. Throw on an action that is not approved or rejected. Do not import a repository.

    Create leave-request-notification-type.test.ts with node:test importing ./leave-request-notification-type.ts. Assert the two CHECK-valid types and that an unknown action throws. Append the test path to packages/database scripts.test (keep every filename already listed). Do not add Vitest.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/database</automated>
  </verify>
  <acceptance_criteria>
    - leaveRequestNotificationType is exported
    - packages/database package.json scripts.test includes leave-request-notification-type.test.ts
    - npm test --workspace=@timeoff/database exits 0
  </acceptance_criteria>
  <done>Approve/reject notification types are CHECK-valid and unit-tested (NOTIF-01, NOTIF-02, D-10, D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit helpers (no network) | totalDays, start_date, and action are test fixtures; no HTTP |
| npm test scripts | No new runtime packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | applyApprovalDeduct | high | mitigate | Amount is the caller-supplied totalDays; 04-03 must pass persisted request.total_days only (D-01) |
| T-04-02 | Tampering | leaveRequestNotificationType | high | mitigate | Mapper emits only request_approved or request_rejected (D-10) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | No new package-manager installs this plan |

</threat_model>

<verification>
npm test --workspace=@timeoff/database exits 0 including the three new test files and plan-default-inserts.test.ts. Service wiring, PATCH mail, and hook invalidation land in 04-03 and 04-04.
</verification>

<success_criteria>
Delta deduct/restore, ISO year, status gates, and CHECK-valid notification types are locked by node:test before any Route Handler change.
</success_criteria>

<output>
Create `.planning/phases/04-approve-reject-cancel-side-effects/04-01-SUMMARY.md` when done
</output>
