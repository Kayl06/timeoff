---
context: phase
phase: 04-approve-reject-cancel-side-effects
task: 1
total_tasks: 2
status: paused
last_updated: 2026-08-29T17:55:30.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do not re-run 04-01 or 04-02 — both have SUMMARYs (`9476359`, `08f6665`). Next plan is 04-03.
- [ ] CONSTRAINT: Sequential on `main` — Cursor Task has no worktree isolation. `workflow.use_worktrees` is `false`. Never spawn two executors that write the same checkout.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-dispatch a completed plan | 04-01 and 04-02 are complete with SUMMARYs. Re-running them duplicates pins and helpers. | blocking | `ls` 04-01-SUMMARY.md and 04-02-SUMMARY.md; start 04-03 |
| Parallel executors on main | Cursor has no `isolation=worktree`. Parallel 04-01/04-02 on the same tree races. | blocking | `use_worktrees: false`; one `gsd-executor` at a time |
| Commit `.env.local` or print keys | Mail env is optional; values must never land in git or chat | advisory | Commit `env.example` names only |
| Mark Phase 3 complete without UAT | Verifier is `human_needed` (21/22). 03-UAT.md is still `testing` | advisory | `/gsd-verify-work 03` after the five browser checks |
| Deduct on bulk this phase | BAL-05 / LEAVE-04 are Phase 5 | advisory | 04-04 must leave bulk status-only |
| Restyle shipped chrome | Leave Balance card, dialogs, Notifications stats card stay as-is | advisory | UI-SPEC D-12 / D-19 |

<current_state>
Phase 4 Wave 1 is complete. 04-01 helpers and 04-02 Resend adapter are committed with SUMMARYs. Web tests: 91 passed. Next is Wave 2 plan 04-03 (PATCH approve tracer). 04-04 is not started. Session remains paused — do not auto-execute until `/gsd-resume-work`.

Phase 3 code is shipped. GSD status is `human_needed`. Five UAT items sit in `03-UAT.md`.
</current_state>

<completed_work>

Completed this session:
- Phase 3 execute + verifier (`human_needed`) + `03-UAT.md` (`d7a7434`)
- Phase 4 discuss `--auto` (D-01–D-20), UI-SPEC, research, patterns, VALIDATION, 4 plans / 3 waves, checker PASSED
- 04-01 both tasks + SUMMARY (`9476359`)
- 04-02 pin resend (`3f3ecd6`), sendMail (`1cebded`), SUMMARY (`08f6665`) — 91 web tests
</completed_work>

<remaining_work>

- 04-03: PATCH approve deduct + throw on audit/notify + mail after durable writes; auto-approve path; invalidate `leaveBalance`
- 04-04: reject no deduct; approved cancel/delete restore; `session.user.id` audit; hook invalidation; bulk unchanged
- Phase 4 verifier / UAT / code-review / secure / validate
- Phase 3 `/gsd-verify-work 03` after human UAT
- Phases 5–7 not discussed or planned
</remaining_work>

<decisions_made>

- User: implement and decide all phases (recommended options, no more discuss prompts)
- D-01: `used_days += total_days`, `remaining_days -= total_days` (do not recompute from allowance)
- D-02: year from `start_date` ISO prefix
- D-03: 409 if no matching balance row
- D-06: restore only approved cancel/delete
- D-08/D-09: audit `session.user.id`; throw on insert fail; never `'system'`
- D-10/D-11: CHECK types `request_approved` / `request_rejected`; throw on notify fail
- D-13–D-15: Resend adapter; mail best-effort after durable writes; unset key → `{ sent: false }`
- D-18: bulk is Phase 5
- Isolation: sequential on `main` (`use_worktrees: false`)
- Resend legitimacy SUS too-new is documented, not a pipeline stop
</decisions_made>

<blockers>
- Phase 3 UAT (5 items) — not blocking Phase 4 code
- Hosted mail and hosted db push — human, not blocking local execute
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine resume state
2. `.planning/phases/04-approve-reject-cancel-side-effects/04-CONTEXT.md` — locked D-01–D-20
3. `.planning/phases/04-approve-reject-cancel-side-effects/04-03-PLAN.md` — next plan
4. `.planning/phases/04-approve-reject-cancel-side-effects/04-01-SUMMARY.md` and `04-02-SUMMARY.md` — Wave 1 already shipped
5. `.planning/phases/04-approve-reject-cancel-side-effects/04-RESEARCH.md` — deduct-then-status, Resend, no RPC
6. `.planning/phases/03-live-remaining-days/03-UAT.md` — Phase 3 human checks (do not close Phase 3 without this)
7. `apps/web/src/lib/mail.ts` — finished adapter; 04-03 calls it after durable writes

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: Re-run 04-01 or 04-02 → skip to 04-03
- [ANTI-PATTERN]: Parallel executors on `main` → one plan at a time
- [ANTI-PATTERN]: `service_role` on leave writes → tenant BFF only (`createTenantDatabaseService`)
- [ANTI-PATTERN]: Silent approve without deduct → 409 missing row; deduct then status; reverse on status fail
- [ANTI-PATTERN]: Notification types `success`/`error` → CHECK `request_approved` / `request_rejected` only
- [ANTI-PATTERN]: Audit `user_id: 'system'` → `session.user.id` UUID
- [ANTI-PATTERN]: `/api/send` or `NEXT_PUBLIC_RESEND_*` → server-only adapter from already-gated leave routes

## Infrastructure State

- `npm run dev` was running in the user’s terminal (Next.js web)
- Local Supabase expected via `npm run supabase:start` (Phase 3 push was `--local`)
- `RESEND_API_KEY` / `EMAIL_FROM` optional; local approve works without them
- Branch: `main`, ahead of origin — do not push unless asked
- Dirty/untracked only: `.planning/config.json`, `.gsd/`, `.planning/milestone.lock` (do not treat as product work)
- Do not commit `.env.local`

## Pre-Execution Critique Required

_Not applicable — execution already started. Resume execute, do not re-plan._

<context>
The user paused mid Wave 1. The background 04-02 executor finished afterward (`08f6665`). Wave 1 is done. Resume with 04-03 then 04-04. Do not fold Phase 5 bulk or Phase 6 reset into this pause resume.
</context>

<next_action>
Start with: execute 04-03 on `main` (sequential), then 04-04. Do not re-run 04-01 or 04-02.
</next_action>
