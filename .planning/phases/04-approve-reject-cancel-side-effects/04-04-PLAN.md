---
phase: 04-approve-reject-cancel-side-effects
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - packages/database/src/modules/leave-requests/service.ts
  - packages/database/src/index.ts
  - apps/web/src/app/api/leave-requests/[id]/route.ts
  - apps/web/src/hooks/use-leave-request-operations.ts
autonomous: true
requirements:
  - BAL-04
  - LEAVE-02
  - LEAVE-03
  - NOTIF-02
  - NOTIF-04
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Reject never calls updateBalanceAfterApproval or restoreBalanceAfterReversal (BAL-04, D-05)."
    - "Reject writes a durable audit row with approverId and persists type request_rejected; both inserts throw on failure (LEAVE-02, NOTIF-02, D-08, D-09, D-10, D-11)."
    - "After durable reject writes, the BFF calls sendLeaveDecisionMail with decision rejected; mail failure still returns 200 (NOTIF-04, D-14, D-15)."
    - "Cancel or delete of an approved request restores total_days before the status write; pending or rejected cancel/delete does not touch balances (BAL-04, D-06, D-16)."
    - "Cancel then delete does not restore twice because status is no longer approved after cancel (D-06)."
    - "Cancel and delete audit user_id is session.user.id via actorUserId; IDatabaseService cancel/delete/softDelete take that argument (LEAVE-03, D-08)."
    - "Cancel and delete throw if audit insert fails (LEAVE-03, D-09)."
    - "Cancel and delete do not send mail (D-14)."
    - "Reject, cancel, and delete onSuccess invalidate leaveBalance and notifications plus existing request-list keys (D-12, D-20)."
    - "POST /api/leave-requests/bulk still does not call approveLeaveRequest (D-18)."
    - "Count 0 stays visible on the stats card. Do not hide the card. Do not invent a list empty-state this phase (D-12)."
    - "Existing dialog pending flags (isApprovingLeaveRequest). No new spinner chrome."
    - "Failed PATCH: existing sonner error from payload.error (including 409 missing balance). Table stays."
    - "After employee refetch, unread count includes persisted request_approved / request_rejected rows."
    - "After approve/cancel refetch, remaining_days match deducted/restored rows (Phase 3 chrome)."
    - "Unchanged Phase 3 type-order rules."
    - "Existing table; no new columns or bulk chrome this phase."
    - "Integer count only. One persisted notice → count at least 1 for that employee."
  artifacts:
    - path: "packages/database/src/modules/leave-requests/service.ts"
      provides: "reject without balance; cancel/delete restore when approved; actorUserId on cancel/delete"
      contains: "restoreBalanceAfterReversal"
    - path: "packages/database/src/index.ts"
      provides: "IDatabaseService cancelLeaveRequest deleteLeaveRequest softDeleteLeaveRequest actorUserId"
      contains: "actorUserId"
    - path: "apps/web/src/app/api/leave-requests/[id]/route.ts"
      provides: "reject mail; cancel/delete pass session.user.id"
      contains: "sendLeaveDecisionMail"
    - path: "apps/web/src/hooks/use-leave-request-operations.ts"
      provides: "reject cancel delete invalidate leaveBalance and notifications"
      contains: "['notifications', userId]"
  key_links:
    - from: "apps/web/src/app/api/leave-requests/[id]/route.ts"
      to: "packages/database/src/index.ts"
      via: "cancelLeaveRequest(id, session.user.id) and deleteLeaveRequest(id, session.user.id)"
      pattern: "session.user.id"
    - from: "packages/database/src/modules/leave-requests/service.ts"
      to: "packages/database/src/modules/leave-balances/service.ts"
      via: "restore only when shouldApplyApprovalRestore(current status)"
      pattern: "shouldApplyApprovalRestore"
    - from: "apps/web/src/app/api/leave-requests/bulk/route.ts"
      to: "packages/database/src/index.ts"
      via: "bulkUpdateLeaveRequests status-only; must not call approveLeaveRequest"
      pattern: "bulkUpdateLeaveRequests"
  prohibitions:
    - statement: "Do not restyle approve/reject/cancel/delete dialogs or the Leave Balance card."
    - statement: "Do not send mail on cancel or delete."
    - statement: "Do not add restore-from-soft-delete BFF or UI (restoreLeaveRequest stays unused)."
    - statement: "Do not call approveLeaveRequest from POST /api/leave-requests/bulk."
    - statement: "Do not add leaveBalance invalidation to bulk onSuccess."
    - statement: "Do not use a service-role client."
    - statement: "Do not add a Supabase migration."
---

## Phase Goal

**As a** manager or employee, **I want to** approve, reject, or cancel a single leave request so remaining days deduct or restore, a durable audit row uses my real user id, and the employee is notified in-app and by email, **so that** the request lifecycle is truthful on a shared deployment.

<objective>
Expand from the 04-03 approve tracer: reject skips balances and still audits, notifies, and mails; cancel/delete restore only when current status is approved and audit with the session actor. D-05: D-06: D-08: D-09: D-11: D-12: D-14: D-16: D-18: D-19: D-20:

Purpose: Close BAL-04, LEAVE-02, LEAVE-03, NOTIF-02, NOTIF-04 without changing bulk (Phase 5).
Output: reject path, actorUserId on cancel/delete, approved-only restore, hook invalidation, bulk left status-only.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-CONTEXT.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-PATTERNS.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-UI-SPEC.md
@packages/database/src/modules/leave-requests/service.ts
@packages/database/src/index.ts
@apps/web/src/app/api/leave-requests/[id]/route.ts
@apps/web/src/app/api/leave-requests/bulk/route.ts
@apps/web/src/hooks/use-leave-request-operations.ts
</context>

<interfaces>
From 04-03:
createAuditLog and createNotification already throw. leaveRequestNotificationType('rejected') is request_rejected. restoreBalanceAfterReversal exists. PATCH already maps CONFLICT to 409. sendLeaveDecisionMail exists.

From 04-01:
shouldApplyApprovalRestore(status) is true only for approved.

From IDatabaseService today:
cancelLeaveRequest(id), deleteLeaveRequest(id), softDeleteLeaveRequest(id). Add actorUserId: string to all three and thread through DatabaseService wrappers to LeaveRequestService.

From PATCH [id]:
session.user.id is already in scope. Pass it into cancel and delete. After rejectLeaveRequest succeeds, sendLeaveDecisionMail with decision rejected. Cancel and delete must not call mail (D-14).

From bulk/route.ts:
Keep bulkUpdateLeaveRequests with status and approver_id only. Do not import or call approveLeaveRequest.

From 04-UI-SPEC:
Keep existing sonner success strings. Reject onError should toast the thrown message when present (409). Do not restyle dialogs (D-19).
</interfaces>

## Artifacts this phase produces

- `rejectLeaveRequest` with no balance writes, throw-through audit/notify, BFF mail
- `cancelLeaveRequest(id, actorUserId)` / `softDeleteLeaveRequest(id, actorUserId)` restore-when-approved
- IDatabaseService actor argument on cancel/delete/softDelete
- reject/cancel/delete query invalidation including leaveBalance and notifications
- bulk route unchanged (status-only)

<tasks>

<task type="auto" tdd="true">
  <name>Reject skips balances and still notifies</name>
  <files>packages/database/src/modules/leave-requests/service.ts, apps/web/src/app/api/leave-requests/[id]/route.ts</files>
  <read_first>packages/database/src/modules/leave-requests/service.ts, apps/web/src/app/api/leave-requests/[id]/route.ts, apps/web/src/lib/mail.ts, packages/database/src/modules/notifications/leave-request-notification-type.ts</read_first>
  <behavior>
    - rejectLeaveRequest does not call updateBalanceAfterApproval or restoreBalanceAfterReversal (BAL-04, D-05)
    - rejectLeaveRequest calls createAuditLog with user_id approverId and action REJECT_LEAVE_REQUEST (LEAVE-02, D-08)
    - rejectLeaveRequest calls createLeaveRequestNotification with action rejected (NOTIF-02, D-10)
    - rejectLeaveRequest does not swallow audit or notify errors (D-09, D-11)
  </behavior>
  <action>
    Rewrite rejectLeaveRequest to keep repository.reject then createAuditLog (user_id approverId, REJECT_LEAVE_REQUEST) then createLeaveRequestNotification(..., 'rejected'). Remove the swallow try/catch around audit and notify per D-09 D-11. Do not call any LeaveBalanceService method (D-05). Do not import resend.

    After rejectLeaveRequest returns in PATCH, getUserById(updated.user_id) and sendLeaveDecisionMail({ to: employee.email, requestId, decision: 'rejected' }) per D-14 D-15. Mail must not change the 200. Keep CONFLICT → 409 from 04-03. Do not restyle the reject dialog (D-19).
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/database && npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - rejectLeaveRequest in leave-requests/service.ts does not call updateBalanceAfterApproval
    - [id]/route.ts reject branch calls sendLeaveDecisionMail
    - npm test --workspace=@timeoff/database exits 0
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Reject leaves remaining days unchanged, persists audit and request_rejected, and attempts employee mail (BAL-04, LEAVE-02, NOTIF-02, NOTIF-04, D-05, D-14).</done>
</task>

<task type="auto">
  <name>Cancel and delete restore with session actor</name>
  <reversibility rating="costly">D-08 actor UUID on cancel/delete is the LEAVE-03 fix; a non-UUID sentinel cannot insert into audit_logs.user_id.</reversibility>
  <files>packages/database/src/modules/leave-requests/service.ts, packages/database/src/index.ts, apps/web/src/app/api/leave-requests/[id]/route.ts</files>
  <read_first>packages/database/src/modules/leave-requests/service.ts, packages/database/src/index.ts, apps/web/src/app/api/leave-requests/[id]/route.ts, packages/database/src/modules/leave-balances/balance-arithmetic.ts</read_first>
  <action>
    Add actorUserId: string to LeaveRequestService.cancelLeaveRequest and softDeleteLeaveRequest. Before the status or soft-delete write, if shouldApplyApprovalRestore(leaveRequest.status) then restoreBalanceAfterReversal using persisted total_days (D-06, D-16). If the later status write fails after a restore, re-apply applyApprovalDeduct on a fresh read (same-request reverse). After the write, createAuditLog with user_id actorUserId and the existing CANCEL_LEAVE_REQUEST or SOFT_DELETE_LEAVE_REQUEST action; do not swallow (D-08, D-09). Read current status before the write so a later delete of an already-cancelled row does not restore again (Pitfall 9). Do not send mail (D-14). Do not add a BFF for restoreLeaveRequest. Leave createLeaveRequest swallow try/catch in place.

    Thread actorUserId through IDatabaseService and DatabaseService for cancelLeaveRequest, deleteLeaveRequest, and softDeleteLeaveRequest. PATCH cancel and delete pass session.user.id. Keep canCancelOrDeleteLeave and createTenantDatabaseService (D-17).
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/database && npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - IDatabaseService cancelLeaveRequest, deleteLeaveRequest, and softDeleteLeaveRequest include actorUserId
    - [id]/route.ts cancel and delete pass session.user.id
    - cancelLeaveRequest and softDeleteLeaveRequest call shouldApplyApprovalRestore
    - npm test --workspace=@timeoff/database exits 0
  </acceptance_criteria>
  <done>Approved cancel/delete restores days once and audits with the session user id (BAL-04, LEAVE-03, D-06, D-08).</done>
</task>

<task type="auto">
  <name>Invalidate remaining-day keys; keep bulk status-only</name>
  <files>apps/web/src/hooks/use-leave-request-operations.ts</files>
  <read_first>apps/web/src/hooks/use-leave-request-operations.ts, apps/web/src/app/api/leave-requests/bulk/route.ts</read_first>
  <action>
    Copy the delete onSuccess invalidation set (recentRequests, leaveBalance, notifications, teamLeaveRequests, allLeaveRequests, personalLeaveRequests) onto reject and cancel onSuccess per D-12 D-20. Keep existing sonner success strings. Reject and cancel onError should toast the thrown message when present so API error text surfaces. Do not add leaveBalance to bulkApprove or bulkReject onSuccess (D-18). Do not edit dashboard-stats.tsx, leave-balance-card.tsx, or any dialog file (D-19). Confirm bulk/route.ts still calls only bulkUpdateLeaveRequests and does not call approveLeaveRequest. Do not change bulk/route.ts unless a stray deduct import appeared — if the file is already status-only, leave it.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - use-leave-request-operations.ts reject and cancel onSuccess invalidate leaveBalance and notifications
    - use-leave-request-operations.ts bulk onSuccess does not mention leaveBalance
    - apps/web/src/app/api/leave-requests/bulk/route.ts does not call approveLeaveRequest
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Reject/cancel/delete refresh remaining days and unread count for the actor; bulk stays status-only (D-12, D-18, D-20).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PATCH cancel/delete | Session cookie; actor id must come from session, not body |
| BFF → audit_logs | user_id FK to users.id; session UUID only |
| Bulk POST | Status-only this phase; must not gain deduct |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | cancel/delete restore | high | mitigate | Restore only when current status is approved; reverse if status write fails (D-06, D-16) |
| T-04-02 | Repudiation | cancel/delete audit | high | mitigate | actorUserId is session.user.id; throw on insert failure (D-08, D-09) |
| T-04-02 | Tampering | reject notification | high | mitigate | type request_rejected via mapper; throw on insert failure (D-10, D-11) |
| T-04-04 | Elevation of Privilege | PATCH + bulk | high | mitigate | Tenant BFF only; bulk must not call approveLeaveRequest (D-17, D-18) |
| T-04-03 | Information Disclosure | reject mail | high | mitigate | Same server-only adapter; cancel/delete do not mail (D-14) |
| T-04-SC | Tampering | npm installs | high | accept | No new installs this plan |

</threat_model>

<verification>
npm test --workspace=@timeoff/database and npm test --workspace=@timeoff/web exit 0. Reject has no balance calls. Cancel/delete take actorUserId. Bulk route still has no approveLeaveRequest call. Hosted Resend inbox remains a manual check when RESEND_API_KEY is set.
</verification>

<success_criteria>
Reject does not change remaining days and still notifies; approved cancel/delete restore once and audit with a real user id; actor dashboards refetch remaining days and unread count.
</success_criteria>

<output>
Create `.planning/phases/04-approve-reject-cancel-side-effects/04-04-SUMMARY.md` when done
</output>
