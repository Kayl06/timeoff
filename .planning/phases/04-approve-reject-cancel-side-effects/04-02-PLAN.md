---
phase: 04-approve-reject-cancel-side-effects
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/mail.ts
  - apps/web/src/lib/mail.test.ts
  - apps/web/src/lib/env.ts
  - apps/web/env.example
autonomous: true
requirements:
  - NOTIF-03
  - NOTIF-04
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "apps/web depends on the resend package pinned to the official Node SDK version after package-legitimacy check (NOTIF-03, NOTIF-04, D-13)."
    - "sendMail returns { sent: false } without throwing when RESEND_API_KEY or EMAIL_FROM is unset (NOTIF-03, D-15)."
    - "sendMail returns { sent: false } without throwing when the injected send function returns an SDK error object (NOTIF-03, D-15)."
    - "sendLeaveDecisionMail emails the employee on approved or rejected only and uses idempotency keys leave-request/{id}/approved or leave-request/{id}/rejected (NOTIF-03, NOTIF-04, D-14)."
    - "env.ts and env.example list RESEND_API_KEY and EMAIL_FROM as optional names only; they are never production-required (D-13, D-15)."
    - "Count 0 stays visible on the stats card. Do not hide the card. Do not invent a list empty-state this phase (D-12)."
    - "Existing dialog pending flags (isApprovingLeaveRequest). No new spinner chrome."
    - "Failed PATCH: existing sonner error from payload.error (including 409 missing balance). Table stays."
    - "After employee refetch, unread count includes persisted request_approved / request_rejected rows."
    - "After approve/cancel refetch, remaining_days match deducted/restored rows (Phase 3 chrome)."
    - "Unchanged Phase 3 type-order rules."
    - "Existing table; no new columns or bulk chrome this phase."
    - "Integer count only. One persisted notice → count at least 1 for that employee."
  artifacts:
    - path: "apps/web/src/lib/mail.ts"
      provides: "sendMail and sendLeaveDecisionMail"
      exports: ["sendMail", "sendLeaveDecisionMail"]
    - path: "apps/web/src/lib/mail.test.ts"
      provides: "unset-key and SDK-error unit tests"
      contains: "sendMail"
    - path: "apps/web/src/lib/env.ts"
      provides: "optional RESEND_API_KEY and EMAIL_FROM on EnvironmentConfig"
      contains: "RESEND_API_KEY"
    - path: "apps/web/env.example"
      provides: "optional Resend env names"
      contains: "EMAIL_FROM"
    - path: ".planning/phases/04-approve-reject-cancel-side-effects/COVERAGE.md"
      provides: "Resend emails.send INTEGRATE; all other capabilities OPT-OUT"
      contains: "emails.send"
  key_links:
    - from: "apps/web/src/app/api/leave-requests/[id]/route.ts"
      to: "apps/web/src/lib/mail.ts"
      via: "04-03 PATCH approve will call sendLeaveDecisionMail after the service returns"
      pattern: "sendLeaveDecisionMail"
    - from: "apps/web/src/lib/mail.ts"
      to: "apps/web/src/lib/env.ts"
      via: "optional RESEND_API_KEY and EMAIL_FROM; never log key values"
      pattern: "RESEND_API_KEY"
  prohibitions:
    - statement: "Do not add an unauthenticated /api/send route."
    - statement: "Do not add Resend to packages/database."
    - statement: "Do not install @resend/node, nodemailer, or react-email."
    - statement: "Do not commit secrets or add Resend names to productionRequiredVars."
    - statement: "Do not restyle dialogs or cards."
    - statement: "Do not tag resend as VERIFIED npm registry; legitimacy is SUS too-new."
---

## Phase Goal

**As a** manager or employee, **I want to** approve, reject, or cancel a single leave request so remaining days deduct or restore, a durable audit row uses my real user id, and the employee is notified in-app and by email, **so that** the request lifecycle is truthful on a shared deployment.

<objective>
Wave 0 mail adapter for NOTIF-03 and NOTIF-04. D-13: D-14: D-15: pin official resend after package-legitimacy check (SUS too-new documented; do not stop for a human verify), then ship a server-only sendMail that never throws when the key is unset.

Purpose: Phase 6 password reset reuses this adapter. Approve/reject HTTP stay 200 when transport is down.
Output: pinned resend on @timeoff/web, mail.ts, mail.test.ts, optional env names, COVERAGE.md already in the phase directory.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-CONTEXT.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-RESEARCH.md
@.planning/phases/04-approve-reject-cancel-side-effects/04-PATTERNS.md
@.planning/phases/04-approve-reject-cancel-side-effects/COVERAGE.md
@apps/web/src/lib/env.ts
@apps/web/env.example
@apps/web/package.json
@apps/web/src/lib/create-company-rpc.test.ts
</context>

<interfaces>
From official Resend Node send (04-RESEARCH.md Pattern 4):
resend.emails.send({ from, to, subject, html }) returns { data, error } and does not throw on API errors. Check error. try/catch only for network failures.

From 04-CONTEXT.md D-13 D-15:
RESEND_API_KEY and EMAIL_FROM are optional even in production. Unset key or from → log via devLog, return { sent: false }, do not throw.

From 04-PATTERNS.md mail.ts:
sendMail accepts optional deps.send so tests inject a fake and never construct the real SDK. sendLeaveDecisionMail({ to, requestId, decision }) uses subject Leave Request approved or Leave Request rejected, html/text only, optional idempotencyKey leave-request/${id}/approved or leave-request/${id}/rejected.

From apps/web/src/lib/create-company-rpc.test.ts:
node:assert/strict, node:test, .ts specifier. Append src/lib/mail.test.ts to apps/web scripts.test; keep every existing filename.
</interfaces>

## Artifacts this phase produces

- pinned `resend` on `@timeoff/web` after `package-legitimacy check resend`
- `sendMail` / `sendLeaveDecisionMail` in `apps/web/src/lib/mail.ts`
- optional `RESEND_API_KEY` / `EMAIL_FROM` in `env.ts` and `env.example`
- phase `COVERAGE.md` (already written; do not add `/api/send`)

<tasks>

<task type="auto">
  <name>Pin official resend after legitimacy check</name>
  <reversibility rating="costly">D-13 locks outbound mail to Resend; swapping later is a coordinated adapter change for Phase 6 too.</reversibility>
  <files>apps/web/package.json</files>
  <read_first>.planning/phases/04-approve-reject-cancel-side-effects/04-RESEARCH.md, .planning/phases/04-approve-reject-cancel-side-effects/COVERAGE.md, apps/web/package.json</read_first>
  <action>
    Run node $HOME/.claude/gsd-core/bin/gsd-tools.cjs query package-legitimacy check resend and capture the JSON verdict. RESEARCH.md already recorded [SUS] too-new on the 2026-08-28 publish despite official docs naming this package and ~10M weekly downloads. Do not insert a blocking human verify. Do not tag the package as registry-verified. Document the SUS verdict and the command output in 04-02-SUMMARY.md.

    Pin the version from official Resend Node docs (https://resend.com/docs/send-with-nodejs) and npm view resend version. RESEARCH cited 6.25.0. Install that official version with npm install resend@6.25.0 -w @timeoff/web (or the newer official docs pin if npm view is higher and still the same package). @timeoff/web must list resend in dependencies. Do not install @resend/node, nodemailer, or react-email. Do not add resend to packages/database. Do not add an /api/send route. Confirm COVERAGE.md already INTEGRATEs emails.send and OPT-OUTs domains, audiences, broadcasts, and the rest with reasons. Do not rewrite that matrix unless the pinned SDK adds a new top-level resource that must be opted out.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/web/package.json'); if(!p.dependencies.resend) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json dependencies includes resend
    - packages/database/package.json dependencies does not include resend
    - COVERAGE.md contains emails.send and INTEGRATE
    - 04-02-SUMMARY.md records the package-legitimacy check output and the SUS too-new verdict
  </acceptance_criteria>
  <done>Official resend is pinned on @timeoff/web after a recorded legitimacy check (D-13). SUS too-new is documented, not a pipeline stop.</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: server-only sendMail adapter</name>
  <files>apps/web/src/lib/mail.ts, apps/web/src/lib/mail.test.ts, apps/web/src/lib/env.ts, apps/web/env.example, apps/web/package.json</files>
  <read_first>apps/web/src/lib/env.ts, apps/web/env.example, apps/web/src/lib/create-company-rpc.test.ts, apps/web/package.json, .planning/phases/04-approve-reject-cancel-side-effects/04-PATTERNS.md</read_first>
  <behavior>
    - sendMail with unset RESEND_API_KEY returns { sent: false } and does not throw (D-15)
    - sendMail with unset EMAIL_FROM returns { sent: false } and does not throw (D-15)
    - sendMail when deps.send resolves { error: { message: 'rate limited' } } returns { sent: false } and does not throw (D-15)
    - sendMail when deps.send resolves { data: { id: 'msg_1' }, error: null } returns { sent: true }
    - sendLeaveDecisionMail({ to, requestId, decision: 'approved' }) calls sendMail with subject Leave Request approved and idempotencyKey leave-request/{id}/approved (D-14)
  </behavior>
  <action>
    Add optional RESEND_API_KEY and EMAIL_FROM to EnvironmentConfig in env.ts. Read them when present. Do not push either name onto requiredVars or productionRequiredVars per D-15. Never log the key value. Append the two names to env.example with a comment that local approve/reject still return 200 if unset. Do not write onboarding@resend.dev as a production from-address.

    Create apps/web/src/lib/mail.ts (kebab-case, file-level JSDoc, server-only). Export sendMail({ to, subject, html, text, idempotencyKey }, deps?) that reads key and from from deps.env or process.env. If either is missing, devLog.warn and return { sent: false }. Otherwise call deps.send or new Resend(key).emails.send with from, to as an array, subject, html, optional text, optional idempotencyKey. If the SDK returns error, or the call throws, devLog.error and return { sent: false }. Never throw. Export sendLeaveDecisionMail({ to, requestId, decision }) for approved or rejected only per D-14, using in-app title sense for subject and html/text strings (no react-email). Use idempotencyKey leave-request/${requestId}/${decision}. Do not interpolate manager comments into html. Do not import this module from any use client file. Do not add /api/send.

    Create mail.test.ts with node:test importing ./mail.ts. Inject deps.send and deps.env so the test never hits the network. Append src/lib/mail.test.ts to apps/web scripts.test and keep every existing test filename. Do not add Vitest.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - sendMail and sendLeaveDecisionMail are exported from mail.ts
    - env.ts EnvironmentConfig includes RESEND_API_KEY and EMAIL_FROM
    - apps/web package.json scripts.test includes mail.test.ts and leave-balance-display.test.ts
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Mail adapter is unit-tested for skip and SDK error (NOTIF-03, NOTIF-04, D-13, D-14, D-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server adapter → Resend HTTPS | API key and from-address; never expose to the browser |
| env.ts / env.example | Names only in git; values stay in gitignored .env.local |
| Browser | Must not import mail.ts or receive a public-prefixed Resend key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure | env.ts / mail.ts | high | mitigate | Optional server env names only; never a NEXT_PUBLIC Resend key; never log the key (D-13) |
| T-04-03 | Information Disclosure | Route surface | high | mitigate | No /api/send; mail is called from existing session-gated leave routes in 04-03 |
| T-04-05 | Tampering | sendLeaveDecisionMail | medium | mitigate | Fixed approve/reject copy; do not interpolate comments into html |
| T-04-SC | Tampering | npm install resend | high | mitigate | Run package-legitimacy check; pin official Node SDK version; document SUS too-new; do not install lookalike packages |

</threat_model>

<verification>
npm test --workspace=@timeoff/web exits 0 including mail.test.ts. apps/web/package.json lists resend. Hosted inbox UAT of NOTIF-03/04 stays a later human-check when a real key exists (D-15).
</verification>

<success_criteria>
A mocked or unset transport never throws; official resend is pinned; COVERAGE.md remains emails.send-only.
</success_criteria>

<output>
Create `.planning/phases/04-approve-reject-cancel-side-effects/04-02-SUMMARY.md` when done
</output>
