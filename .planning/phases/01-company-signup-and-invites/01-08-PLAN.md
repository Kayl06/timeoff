---
phase: 01-company-signup-and-invites
plan: 08
type: execute
wave: 7
depends_on:
  - 01-07
files_modified:
  - apps/web/src/lib/invite-accept.ts
  - apps/web/src/lib/invite-accept.test.ts
  - apps/web/src/app/api/auth/invites/preview/route.ts
  - apps/web/src/app/auth/accept-invite/page.tsx
autonomous: true
gap_closure: true
gap_ids:
  - G-01-4
requirements:
  - TENANT-03
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /api/auth/invites/preview for a usable token whose invite.email already has a users row returns 409 with EMAIL_EXISTS_ERROR, not 200 Join-ready payload (G-01-4)."
    - "Opening /auth/accept-invite with that token shows the existing-account sentence and Back to sign in before the join form (G-01-4)."
    - "Invalid/expired tokens still show This invite is invalid or has expired. Ask your admin to send a new invite."
    - "Credentials accept 409 with EMAIL_EXISTS_ERROR switches to the same existing-account card, not toast-only (G-01-4)."
    - "Usable token with no existing users row still shows Join {company name} and the credentials form."
  artifacts:
    - path: "apps/web/src/lib/invite-accept.ts"
      provides: "invitePreviewPageState after usability + existing email"
      exports: ["invitePreviewPageState"]
    - path: "apps/web/src/app/api/auth/invites/preview/route.ts"
      provides: "users.email check on preview"
    - path: "apps/web/src/app/auth/accept-invite/page.tsx"
      provides: "exists pageState and InviteStatusCard for EMAIL_EXISTS_ERROR"
  key_links:
    - from: "apps/web/src/app/api/auth/invites/preview/route.ts"
      to: "users table"
      via: "eq email from invite row after inviteIsUsable"
      pattern: "from('users')"
    - from: "apps/web/src/app/auth/accept-invite/page.tsx"
      to: "GET /api/auth/invites/preview"
      via: "HTTP 409 → pageState exists"
      pattern: "exists"
  prohibitions:
    - statement: "Do not add a company picker on accept-invite."
      status: resolved
      verification: judgment
    - statement: "Do not restyle dashboard or calendar chrome."
      status: resolved
      verification: judgment
    - statement: "Do not implement accept RPC or Google bind mapping in this plan (01-09)."
      status: resolved
      verification: judgment
    - statement: "Do not put preview writes on IDatabaseService."
      status: resolved
      verification: judgment
---

## Phase Goal

Close the accept-page half of UAT G-01-4: verify existing email before the join form.

<objective>
Preview looks up users by invite.email; accept-invite shows an existing-account card instead of Join {company}.

Purpose: Preview never queried users, so a leftover or raced token still showed the join form; credentials 409 was toast-only via an unmounted toaster (G-01-4).
Output: invitePreviewPageState; preview 409; exists card on accept-invite.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/debug/invite-existing-email.md
@.planning/phases/01-company-signup-and-invites/01-05-SUMMARY.md
@.planning/phases/01-company-signup-and-invites/01-07-PLAN.md
@apps/web/src/lib/invite-accept.ts
@apps/web/src/lib/invite-auth.ts
@apps/web/src/app/api/auth/invites/preview/route.ts
@apps/web/src/app/auth/accept-invite/page.tsx
</context>

<interfaces>
EMAIL_EXISTS_ERROR from invite-auth.ts (01-07): An account with this email already exists. Sign in, or ask your admin for an invite.

invitePreviewPageState(input): 'invalid' | 'exists' | 'ready'
- usable false → invalid
- usable true and existingUser true → exists
- usable true and existingUser false → ready

GET preview: after inviteIsUsable and companyName resolve, query users .eq('email', invite.email).maybeSingle(). If a row exists, return 409 { error: EMAIL_EXISTS_ERROR }. Do not include the existing user's company_id in JSON. If no row, keep 200 { companyName, email }.

Accept-invite PageState adds 'exists'. loadPreview: HTTP 409 → setPageState('exists'). queryError === 'exists' also sets exists (for 01-09 Google redirect if it uses this query). InviteStatusCard(copy={EMAIL_EXISTS_ERROR}) with Back to sign in. Join form only when pageState === 'ready'.

On POST /api/auth/invites/accept 409 whose error is EMAIL_EXISTS_ERROR (or status 409 that is not mismatch), setPageState('exists') rather than toast-only.

Import toast from sonner on this page so any remaining toasts render (SessionProvider mounts sonner only). Do not remount a second toaster.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Preview 409 when invite email already exists</name>
  <files>apps/web/src/lib/invite-accept.ts, apps/web/src/lib/invite-accept.test.ts, apps/web/src/app/api/auth/invites/preview/route.ts</files>
  <read_first>apps/web/src/lib/invite-accept.ts, apps/web/src/lib/invite-accept.test.ts, apps/web/src/lib/invite-auth.ts, apps/web/src/app/api/auth/invites/preview/route.ts</read_first>
  <behavior>
    - invitePreviewPageState({ usable: false, existingUser: false }) is invalid
    - invitePreviewPageState({ usable: false, existingUser: true }) is invalid
    - invitePreviewPageState({ usable: true, existingUser: true }) is exists
    - invitePreviewPageState({ usable: true, existingUser: false }) is ready
  </behavior>
  <action>
    Add invitePreviewPageState to invite-accept.ts. Extend invite-accept.test.ts (already on the web test script). Do not change companyIdFromInvite or inviteIsUsable behavior.

    In preview/route.ts: after a usable invite and companyName, select users by invite.email only. If userLookupError, throw (500). If a row exists, return NextResponse.json({ error: EMAIL_EXISTS_ERROR }, { status: 409 }) importing EMAIL_EXISTS_ERROR from invite-auth.ts (use .ts suffix if the test runner would load this module; the route can use @/lib/invite-auth). Skip the users query when the invite is not usable (404 path first).
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - invite-accept.ts exports invitePreviewPageState
    - invite-accept.test.ts covers invalid / exists / ready
    - preview/route.ts queries from('users') by invite email
    - preview/route.ts returns 409 EMAIL_EXISTS_ERROR when a users row exists
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Preview no longer returns Join-ready JSON for an email that already has an account.</done>
</task>

<task type="auto">
  <name>Accept-invite existing-account card</name>
  <files>apps/web/src/app/auth/accept-invite/page.tsx</files>
  <read_first>apps/web/src/app/auth/accept-invite/page.tsx, apps/web/src/lib/invite-auth.ts</read_first>
  <action>
    Extend PageState with exists. If queryError is exists, start in exists (same Card as mismatch/invalid via InviteStatusCard). In loadPreview, status 409 sets exists (do not treat 409 as invalid/expired). Render InviteStatusCard with EMAIL_EXISTS_ERROR imported from invite-auth. Keep INVALID_COPY and MISMATCH_COPY paths.

    On credentials submit, if response.status === 409 and it is not the mismatch copy, setPageState('exists') and return (no join success). Map 400 details onto password/name field errors if present (same merge idea as signup) so composition failures are visible; this page already gates length 12.

    Import { toast } from 'sonner' for success and remaining errors. Do not add a company picker. Do not edit dashboard or calendar files. Do not implement Google ACCOUNT_EXISTS_PATH here (01-09).
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - accept-invite/page.tsx PageState includes exists
    - loadPreview treats HTTP 409 as exists
    - EMAIL_EXISTS_ERROR is rendered on the exists card
    - page imports toast from sonner
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Invitee with an existing account sees verification copy and sign-in, not the join form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous browser → GET preview | Raw invite token in query |
| Preview route → users.email | Existence check for invited address |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Information Disclosure | preview 409 | medium | mitigate | Return EMAIL_EXISTS_ERROR only; never other company_id or name; invalid tokens stay 404 invalid_or_expired |
| T-01-SC | Tampering | npm installs | high | accept | No new packages |

ASVS L1: existence leak matches signup/accept 409; do not leak tenant identity.
</threat_model>

<verification>
npm test --workspace=@timeoff/web exits 0. Preview contains a users lookup.
<human-check>
Open a valid invite link for an email that already signed up: card shows the existing-account sentence and Back to sign in, not Join {company}. Invalid token copy unchanged.
</human-check>
</verification>

<success_criteria>
G-01-4 preview/UI: existing email is verified before the join form.
</success_criteria>

<output>
Create `.planning/phases/01-company-signup-and-invites/01-08-SUMMARY.md` when done
</output>
