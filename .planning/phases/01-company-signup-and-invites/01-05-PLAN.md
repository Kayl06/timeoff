---
phase: 01-company-signup-and-invites
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - apps/web/src/lib/validation.ts
  - apps/web/src/lib/invite-accept.ts
  - apps/web/src/lib/invite-accept.test.ts
  - apps/web/package.json
  - apps/web/src/app/api/auth/invites/preview/route.ts
  - apps/web/src/app/api/auth/invites/accept/route.ts
  - apps/web/src/app/auth/accept-invite/page.tsx
  - apps/web/src/lib/auth.ts
autonomous: true
requirements:
  - TENANT-03
  - TENANT-05

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Accept binds users.company_id to the invite's company_id only; the accept UI has no company picker (TENANT-03)."
    - "Missing/blank/invalid/expired token shows This invite is invalid or has expired. Ask your admin to send a new invite. not an empty join form."
    - "Token resolve: centered h-8 w-8 spinner matching /auth/error Suspense; submit uses Joining...."
    - "Invalid/expired and email-mismatch copy on the same Card; primary path Back to sign in."
    - "Credentials fields fill independently; Google join is an alternate complete path, not a half-filled credentials form."
    - "Company name on CardTitle wraps (text-center, no ellipsis)."
    - "Google accept: pending invite cookie; Google email must match invite.email or show This invite was sent to a different email. Sign in with that address, or ask your admin for a new invite."
    - "Invitee role is employee; they never become owner via accept."
  artifacts:
    - path: "apps/web/src/app/api/auth/invites/preview/route.ts"
      provides: "GET preview by token hash"
      exports: ["GET"]
    - path: "apps/web/src/app/api/auth/invites/accept/route.ts"
      provides: "POST credentials accept"
      exports: ["POST"]
    - path: "apps/web/src/app/auth/accept-invite/page.tsx"
      provides: "Join company UI"
      contains: "Join company"
    - path: "apps/web/src/lib/invite-accept.ts"
      provides: "companyIdFromInvite mapper"
      exports: ["companyIdFromInvite", "inviteIsUsable"]
  key_links:
    - from: "apps/web/src/app/auth/accept-invite/page.tsx"
      to: "apps/web/src/app/api/auth/invites/preview/route.ts"
      via: "GET /api/auth/invites/preview?token="
      pattern: "/api/auth/invites/preview"
    - from: "apps/web/src/app/auth/accept-invite/page.tsx"
      to: "apps/web/src/app/api/auth/invites/accept/route.ts"
      via: "POST token, firstName, lastName, password, confirmPassword"
      pattern: "/api/auth/invites/accept"
    - from: "apps/web/src/lib/auth.ts"
      to: "apps/web/src/app/api/auth/pending-context/route.ts"
      via: "pending invite cookie then Google signIn bind"
      pattern: "pendingInvite"
  prohibitions:
    - statement: "Do not add a company picker on accept-invite."
      status: resolved
      verification: judgment
    - statement: "Do not put accept writes on IDatabaseService."
      status: resolved
      verification: judgment
    - statement: "Do not finish RLS this phase; cannot see other companies means join binding, not PostgREST isolation."
      status: resolved
      verification: judgment
    - statement: "Do not add a fifth users.role CHECK value; invitees are employee."
      status: resolved
      verification: judgment
    - statement: "Do not restyle dashboard/calendar/forgot/reset chrome."
      status: resolved
      verification: judgment
---

## Phase Goal

**As a** first user of a client company, **I want to** create my company at signup and invite teammates by email, **so that** later people join only that company and Google cannot drop anyone into a global employee pool.

<objective>
Invitee joins the invite's company only via credentials or Google, with UI-SPEC accept-invite and email-mismatch copy.

Purpose: TENANT-03; complete Google invite bind for TENANT-05.
Output: preview/accept APIs, /auth/accept-invite, auth.ts invite-cookie branch.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-company-signup-and-invites/01-RESEARCH.md
@.planning/phases/01-company-signup-and-invites/01-PATTERNS.md
@.planning/phases/01-company-signup-and-invites/01-UI-SPEC.md
@apps/web/src/app/auth/signup/page.tsx
@apps/web/src/app/auth/error/page.tsx
@apps/web/src/app/api/auth/signup/route.ts
@apps/web/src/lib/auth.ts
@apps/web/src/lib/invite-token.ts
@apps/web/src/lib/google-signin-gate.ts
</context>

<interfaces>
hashInviteToken(token) then lookup company_invites.token_hash (hex)
inviteIsUsable(invite): status pending AND expires_at > now
companyIdFromInvite(invite): invite.company_id only — never from query params besides token

Accept POST body: token, firstName, lastName, password, confirmPassword (reuse nameSchema + passwordSchema)
Preview GET query: token; response { companyName, email } only (not other invites)

After credentials accept: client signIn('credentials') then /dashboard
Success toast: You've joined {company name}. Redirecting to dashboard...
</interfaces>

## Artifacts this phase produces

- `inviteAcceptSchema`, `InviteAcceptInput`
- `companyIdFromInvite`, `inviteIsUsable`
- `GET /api/auth/invites/preview`
- `POST /api/auth/invites/accept`
- `AcceptInvitePage` at `apps/web/src/app/auth/accept-invite/page.tsx`
- Google invite bind in `auth.ts` signIn

<tasks>

<task type="auto" tdd="true">
  <name>Invite preview and credentials accept APIs</name>
  <files>apps/web/src/lib/validation.ts, apps/web/src/lib/invite-accept.ts, apps/web/src/lib/invite-accept.test.ts, apps/web/package.json, apps/web/src/app/api/auth/invites/preview/route.ts, apps/web/src/app/api/auth/invites/accept/route.ts</files>
  <read_first>apps/web/src/app/api/auth/signup/route.ts, apps/web/src/lib/invite-token.ts, apps/web/src/lib/validation.ts, apps/web/src/app/api/test-connection/route.ts</read_first>
  <behavior>
    - companyIdFromInvite returns invite.company_id and ignores any other id field
    - inviteIsUsable is false when status is not pending or expires_at is in the past
    - GET preview unknown token → 404 JSON that the page maps to invalid/expired copy
    - POST accept valid token creates user with that company_id role employee and marks invite accepted
  </behavior>
  <action>
    Add invite-accept.ts: companyIdFromInvite(invite: { company_id: string }): string returns company_id only. inviteIsUsable({ status, expires_at }). Tests in invite-accept.test.ts. Append the test file to the web test script.

    Add inviteAcceptSchema: token min 1, firstName, lastName, password, confirmPassword (password match refine).

    GET preview: hash query token; find invite; if missing or not usable, 404 { error: 'invalid_or_expired' }. Else 200 { companyName, email } via join to companies. Do not return other invites or raw token_hash.

    POST accept: validateInput; hash token; usable invite; if users.email already exists 409. bcrypt 12; insert user with company_id from companyIdFromInvite, role employee, department/team Unassigned as existing signup did for non-org fields. Update invite status accepted, accepted_at now. Strip password from 201. Identity client @/lib/supabase only.

    Do not accept a company id from the JSON body.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - invite-accept.test.ts is in the web test script and passes
    - preview and accept route handlers exist
    - accept POST schema has no companyId / company_id field
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Credentials join binds only the invite's company_id.</done>
</task>

<task type="auto">
  <name>Accept-invite page</name>
  <files>apps/web/src/app/auth/accept-invite/page.tsx</files>
  <read_first>apps/web/src/app/auth/signup/page.tsx, apps/web/src/app/auth/error/page.tsx, .planning/phases/01-company-signup-and-invites/01-UI-SPEC.md</read_first>
  <action>
    New client page cloning signup Card chrome: min-h-screen gradient, max-w-md, glass Card, 48×48 TO mark, from-primary CTA. No company-name field. Heading Join {company name}. Description You're invited to this company only. Create your account or continue with Google to join.

    On load, read token from searchParams. If blank, show invalid/expired copy (not a join form). Else GET preview; while resolving, centered h-8 w-8 spinner like error Suspense. On 404, same invalid/expired copy; primary Back to sign in.

    Credentials: first field First name (focus). Password toggle aria-label Show password / Hide password. Submit Join company / Joining.... Success toast You've joined {company name}. Redirecting to dashboard... then signIn credentials redirect false then /dashboard.

    Google: Continue with Google; POST pending-context kind invite token then signIn('google', { callbackUrl: '/dashboard' }).

    Email mismatch copy on this Card when query error=mismatch or API says mismatch. react-hot-toast like signup. No company picker select.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - accept-invite/page.tsx contains Join company and no company picker select element
    - password toggle uses aria-label Show password
    - preview fetch uses /api/auth/invites/preview
  </acceptance_criteria>
  <done>Invitee can open the link and join that company only.</done>
</task>

<task type="auto">
  <name>Google join via pending invite cookie</name>
  <files>apps/web/src/lib/auth.ts</files>
  <read_first>apps/web/src/lib/auth.ts, apps/web/src/lib/google-signin-gate.ts, apps/web/src/lib/pending-auth-cookie.ts, apps/web/src/lib/invite-token.ts, apps/web/src/lib/invite-accept.ts</read_first>
  <action>
    Replace the 01-03 signIn behavior that sent pendingInvite + no user to INVITE_REQUIRED_PATH. When pending kind is invite: hash cookie token, load invite, require inviteIsUsable, require Google email equals invite.email (case-insensitive). On mismatch, return /auth/accept-invite?error=mismatch (string redirect). On match and no user, insert employee with companyIdFromInvite, mark invite accepted, clear cookies, return true. On match and existing user already in that company, return true. If existing user belongs to a different company_id, do not rebind; return INVITE_REQUIRED_PATH.

    decideGoogleSignIn still returns true for pendingInvite; auth.ts performs the bind. Unknown Google without cookies still returns INVITE_REQUIRED_PATH. Do not use a boolean deny for InviteRequired or mismatch.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts Google branch handles pending invite kind with email match
    - mismatch redirects to accept-invite?error=mismatch
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Google invitee joins the invite company only; email mismatch cannot steal a seat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → preview/accept with token query | Token is a capability URL; hashed lookup |
| Google profile email vs invite.email | Attacker Google account must not join someone else's invite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Spoofing | accept POST | high | mitigate | Lookup by SHA-256 hash; usable pending + expiry; no company_id in body |
| T-01-17 | Elevation of Privilege | Google invite bind | high | mitigate | Email must equal invite.email; else mismatch URL; no global employee insert |
| T-01-18 | Information Disclosure | preview GET | medium | mitigate | Return companyName + invited email only |
| T-01-19 | Tampering | company picker | high | mitigate | No picker; companyIdFromInvite only |
| T-01-06 | Information Disclosure | PostgREST open RLS | medium | accept | Join binding only; Phase 2 closes cross-tenant reads |
| T-01-SC | Tampering | npm installs | high | accept | No new packages |

ASVS L1: Google auto-provision remains in scope and is mitigated here plus 01-03.
</threat_model>

<verification>
npm test --workspace=@timeoff/web including invite-accept.test.ts. Accept schema has no company id. No company picker on the page.
<human-check>
Open a copy-link in a second browser: Join {company}. Credentials join lands on dashboard. Invalid token shows expired copy. Google with wrong account shows email-mismatch copy.
</human-check>
</verification>

<success_criteria>
An invitee joins that company and cannot pick another; Google cannot dump them into a global pool.
</success_criteria>

<output>
Create `.planning/phases/01-company-signup-and-invites/01-05-SUMMARY.md` when done
</output>
