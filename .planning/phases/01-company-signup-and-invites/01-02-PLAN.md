---
phase: 01-company-signup-and-invites
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/database/migrations/20250818193733_add_companies_and_invites.sql
  - packages/database/seed.sql
  - packages/types/src/index.ts
  - packages/database/src/modules/users/types.ts
  - apps/web/src/lib/validation.ts
  - apps/web/src/lib/create-company-rpc.ts
  - apps/web/src/lib/create-company-rpc.test.ts
  - apps/web/src/app/api/auth/signup/route.ts
  - apps/web/src/app/auth/signup/page.tsx
  - apps/web/package.json
autonomous: false
requirements:
  - TENANT-01

user_setup:
  - service: supabase
    why: Schema push applies companies, company_invites, users.company_id, and create_company_with_owner
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: apps/web/.env.local (copy from apps/web/env.example if missing)
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: apps/web/.env.local
    dashboard_config:
      - task: "CLI linked to the same project .env.local points at (local supabase start or npx supabase link)"
        location: "repo root supabase/config.toml project_id timeoff, or hosted project"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "First credentials signup with companyName creates a companies row whose owner_id is that user and a users row with company_id set and role admin (TENANT-01)."
    - "create_company_with_owner p_password is nullable: credentials pass a bcrypt hash; Google (01-03) passes SQL/JSON NULL via the same helper (TENANT-01 OAuth owner)."
    - "company_invites.token_hash is VARCHAR(64) storing lowercase SHA-256 hex (hashInviteTokenHex); 01-04 stores that hex and 01-05 looks up that hex."
    - "Signup POST body is camelCase firstName, lastName, confirmPassword, companyName plus existing fields; 201 then client toast Company created. Sign in with your new credentials. and redirect /auth/signin."
    - "Duplicate email returns HTTP 409 with API error User with this email already exists; the signup page shows An account with this email already exists. Sign in, or ask your admin for an invite. Second POST does not create a second company for that email."
    - "Concurrent signup for the same email: one RPC winner; loser 409; no companies row with null owner_id."
    - "Unfilled signup shows labels/placeholders only; no fake company; CTA enabled only after client validation including company name. Copy: Company name is required."
    - "Submit shows Loader2 + Creating company...; inputs and Google disabled={isLoading}. Card stays visible."
    - "Field errors: AlertCircle + text-sm text-red-600 + border-red-500. Duplicate email copy. Toast on 500."
    - "Fields fill independently; per-field errors; password-strength widget only after password has characters (existing)."
    - "Company name max 80 characters, wraps in the input; emails are single-line and scroll inside the input."
  artifacts:
    - path: "packages/database/migrations/20250818193733_add_companies_and_invites.sql"
      provides: "companies, company_invites.token_hash VARCHAR(64), users.company_id, create_company_with_owner with nullable p_password"
      contains: "create_company_with_owner"
    - path: "apps/web/src/lib/create-company-rpc.ts"
      provides: "buildCreateCompanyWithOwnerArgs; p_password string or null always present"
      exports: ["buildCreateCompanyWithOwnerArgs"]
    - path: "packages/types/src/index.ts"
      provides: "User.company_id"
      contains: "company_id"
    - path: "apps/web/src/app/api/auth/signup/route.ts"
      provides: "POST signup via RPC"
      exports: ["POST"]
    - path: "apps/web/src/app/auth/signup/page.tsx"
      provides: "Create company UI"
      contains: "Create your company"
    - path: "apps/web/src/lib/validation.ts"
      provides: "companyName on userRegistrationSchema"
      contains: "companyName"
  key_links:
    - from: "apps/web/src/app/auth/signup/page.tsx"
      to: "apps/web/src/app/api/auth/signup/route.ts"
      via: "fetch POST /api/auth/signup JSON camelCase including companyName"
      pattern: "/api/auth/signup"
    - from: "apps/web/src/app/api/auth/signup/route.ts"
      to: "packages/database/migrations/20250818193733_add_companies_and_invites.sql"
      via: "supabase.rpc create_company_with_owner"
      pattern: "create_company_with_owner"
  prohibitions:
    - statement: "Do not rebuild the stub /(admin)/users page as a global operator console."
      status: resolved
      verification: judgment
    - statement: "Do not restyle dashboard, request form, calendar, forgot-password, or reset-password chrome except the owner invite entry."
      status: resolved
      verification: judgment
    - statement: "Do not add a new component library, font, or brand palette."
      status: resolved
      verification: judgment
    - statement: "Do not put invite or company writes on the browser IDatabaseService facade."
      status: resolved
      verification: judgment
    - statement: "Do not add a fifth value to the users.role CHECK; first-user role is admin; ownership is companies.owner_id."
      status: resolved
      verification: judgment
    - statement: "Do not finish tenant RLS this phase; do not edit 009_fix_rls_for_nextauth.sql."
      status: resolved
      verification: judgment
    - statement: "Do not add a mailer this phase."
      status: resolved
      verification: judgment
    - statement: "Do not seed leave_balances this phase (BAL-02 / Phase 3)."
      status: resolved
      verification: judgment
---

## Phase Goal

**As a** first user of a client company, **I want to** create my company at signup and invite teammates by email, **so that** later people join only that company and Google cannot drop anyone into a global employee pool.

<objective>
Confirm the tenant schema door, then ship the credentials create-company tracer (UI → POST /api/auth/signup → RPC) and push the migration.

Purpose: TENANT-01 happy path on the existing Next.js 14 app. Google create-company is 01-03.
Output: migration applied; signup creates company + owner; UI-SPEC signup copy.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-company-signup-and-invites/01-RESEARCH.md
@.planning/phases/01-company-signup-and-invites/01-PATTERNS.md
@.planning/phases/01-company-signup-and-invites/01-UI-SPEC.md
@.planning/phases/01-company-signup-and-invites/SKELETON.md
@scripts/create-migration.js
@apps/web/src/app/api/auth/signup/route.ts
@apps/web/src/app/auth/signup/page.tsx
@apps/web/src/lib/validation.ts
@packages/database/seed.sql
@packages/types/src/index.ts
@packages/database/src/modules/users/types.ts
@packages/database/migrations/001_initial_schema.sql
@packages/database/migrations/009_fix_rls_for_nextauth.sql
</context>

<interfaces>
From apps/web/src/app/api/auth/signup/route.ts:
export async function POST(request: NextRequest)
validateInput(userRegistrationSchema, body) → 400 formatValidationErrors
existing user → 409 { error: 'User with this email already exists' }
bcrypt.hash password saltRounds 12
supabase from @/lib/supabase

From apps/web/src/lib/validation.ts:
userRegistrationSchema already expects firstName, lastName, confirmPassword (signup page currently posts snake_case — fix in this plan)
Add companyName: z.string().min(1, 'Company name is required').max(80)
Keep Zod 3; do not upgrade.

From packages/database/migrations/001_initial_schema.sql:
users.role CHECK (role IN ('employee', 'supervisor', 'admin', 'hr'))
uuid_generate_v4(), TIMESTAMP WITH TIME ZONE DEFAULT NOW()

RPC name (discretion): create_company_with_owner
Locked signature (both callers must use these argument names):
  p_email TEXT
  p_password TEXT  -- NULLABLE. Credentials (this plan): bcrypt hash string. Google (01-03): JSON/SQL NULL, not empty string, not omitted.
  p_first_name TEXT
  p_last_name TEXT
  p_company_name TEXT
RETURNS the inserted users row.
When p_password IS NULL, INSERT users.password as NULL (column is VARCHAR(255) without NOT NULL — 002_add_auth_fields.sql). When p_password IS NOT NULL, INSERT that pre-hashed value.
JS helper buildCreateCompanyWithOwnerArgs always includes the p_password key (string | null) so PostgREST never sees a missing required argument.

Grant EXECUTE to anon (signup uses the anon JS client).

company_invites.token_hash: VARCHAR(64) NOT NULL UNIQUE — lowercase SHA-256 hex from hashInviteTokenHex. Do not use a binary column type.

PATTERNS.md lists packages/database/src/modules/companies/ and company-invites/ quartets. Do not create those directories this phase. Identity writes stay on Route Handlers + @/lib/supabase. packages/types User.company_id and users module types.company_id land in this plan (snake_case, matches Postgres). Web camelCase companyId on supabase.ts User and next-auth.d.ts lands in 01-03.
</interfaces>

## Artifacts this phase produces

- SQL file from `node scripts/create-migration.js add_companies_and_invites` (expected `20250818193733_add_companies_and_invites.sql` given current max prefix `20250818193732`; never a `014_` name)
- Tables `companies`, `company_invites` (`token_hash VARCHAR(64)`); column `users.company_id`; function `create_company_with_owner` with nullable `p_password`
- `buildCreateCompanyWithOwnerArgs` in `apps/web/src/lib/create-company-rpc.ts`
- `company_id` on `packages/types` User and `packages/database/src/modules/users/types.ts` User / CreateUserData / UpdateUserData
- `userRegistrationSchema.companyName`
- Signup heading/description/CTA per UI-SPEC

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm tenant schema (one-way door)</name>
  <read_first>.planning/phases/01-company-signup-and-invites/01-RESEARCH.md, packages/database/migrations/001_initial_schema.sql, packages/database/seed.sql</read_first>
  <decision>On-disk tenant model for TENANT-01: companies.owner_id plus NOT NULL users.company_id, versus a fifth users.role CHECK value, versus nullable company_id.</decision>
  <context>
    This is a one-way data shape. Later phases (2–7) filter by company_id. Changing it after rows exist needs a data migration. RESEARCH recommends option-a. CONTEXT.md does not exist so the human has not already walked through this door. Seed users (admin@company.com and four others) must land in one demo company so NOT NULL holds. Hosted extra users without company_id are backfilled into that same demo company in the migration.
  </context>
  <options>
    <option id="option-a">
      <name>companies.owner_id + users.company_id NOT NULL + RPC create_company_with_owner</name>
      <pros>Matches TENANT-01; keeps the four-value role CHECK; atomic insert avoids orphan companies; invitees stay employee</pros>
      <cons>Requires backfill of existing users; undo is a drop-column migration</cons>
    </option>
    <option id="option-b">
      <name>Add a fifth users.role CHECK value for owner</name>
      <pros>No companies.owner_id column</pros>
      <cons>Touches every isAdminOrHR comparison; CHECK migration on live rows; RESEARCH rejects this</cons>
    </option>
    <option id="option-c">
      <name>Nullable users.company_id</name>
      <pros>Easier backfill</pros>
      <cons>Orphan users and failed TENANT-01 ownership invariant</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Human selects option-a, option-b, or option-c
    - Executor does not write the migration until resume-signal is received
  </acceptance_criteria>
  <resume-signal>Select: option-a (recommended), option-b, or option-c</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end credentials create company — one path</name>
  <precondition>Decision checkpoint resolved; prefer option-a unless the resume-signal named another option. If option-b or option-c was chosen, stop and re-plan — this task implements option-a only.</precondition>
  <reversibility rating="one-way">users.company_id NOT NULL and companies.owner_id are the tenant columns Phases 2–7 will read; dropping them later is a data migration.</reversibility>
  <files>packages/database/migrations/20250818193733_add_companies_and_invites.sql, packages/database/seed.sql, packages/types/src/index.ts, packages/database/src/modules/users/types.ts, apps/web/src/lib/validation.ts, apps/web/src/lib/create-company-rpc.ts, apps/web/src/lib/create-company-rpc.test.ts, apps/web/src/app/api/auth/signup/route.ts, apps/web/src/app/auth/signup/page.tsx, apps/web/package.json</files>
  <read_first>scripts/create-migration.js, packages/database/migrations/001_initial_schema.sql, packages/database/migrations/009_fix_rls_for_nextauth.sql, apps/web/src/app/api/auth/signup/route.ts, apps/web/src/app/auth/signup/page.tsx, apps/web/src/lib/validation.ts, packages/database/seed.sql, .planning/phases/01-company-signup-and-invites/01-UI-SPEC.md, .planning/phases/01-company-signup-and-invites/01-PATTERNS.md</read_first>
  <behavior>
    - userRegistrationSchema requires companyName 1–80 characters
    - POST /api/auth/signup with valid camelCase body including companyName and unique email returns 201
    - Created user role is admin; companies.owner_id equals that user id
    - Same email again returns 409 with error User with this email already exists
    - buildCreateCompanyWithOwnerArgs with passwordHash string sets p_password to that string
    - buildCreateCompanyWithOwnerArgs with passwordHash null sets p_password to null (key present)
  </behavior>
  <action>
    Run node scripts/create-migration.js add_companies_and_invites at repo root. Use the filename the script prints (Math.max of existing numeric prefixes plus one). If the script names a file other than 20250818193733_add_companies_and_invites.sql, use the actual filename everywhere (including this plan's path if it differs).

    Implement option-a per the checkpoint (D-schema / RESEARCH discretion): table companies (id uuid default uuid_generate_v4, name varchar, owner_id uuid references users(id), timestamps) and company_invites (id, company_id references companies, email, token_hash VARCHAR(64) NOT NULL UNIQUE, expires_at, status CHECK pending/accepted/expired, accepted_at, timestamps). token_hash stores lowercase SHA-256 hex from hashInviteTokenHex (64 chars). Triggers using existing update_updated_at_column. Then add users.company_id uuid references companies(id). Backfill: insert one company named Timeoff Demo, assign every existing users.company_id to it, set owner_id to 770e8400-e29b-41d4-a716-446655440005 (admin@company.com), then ALTER company_id SET NOT NULL.

    Function create_company_with_owner in plpgsql with arguments p_email TEXT, p_password TEXT (NULLABLE), p_first_name TEXT, p_last_name TEXT, p_company_name TEXT. Body: insert company with null owner_id, insert user with company_id and role admin (not a new CHECK value) using p_password as users.password (NULL when p_password is NULL), update companies.owner_id, return the user row. Single transaction so there is never a company without owner. GRANT EXECUTE to anon, authenticated, service_role. Callers: this plan credentials signup passes the bcrypt hash as p_password; 01-03 Google create-company passes NULL via the same helper.

    Enable RLS on new tables. Policies for SELECT/INSERT/UPDATE must match 009's open shape (USING true / WITH CHECK true). Leave 009_fix_rls_for_nextauth.sql unmodified.

    Do not create packages/database/src/modules/companies/ or company-invites/ (PATTERNS File Classification lists those quartets for later). Identity stays on Route Handlers.

    Add company_id: string to packages/types User and to packages/database/src/modules/users/types.ts User, CreateUserData, and UpdateUserData (optional on UpdateUserData). Do not add an owner value to UserRole.

    Update seed.sql so all five sample users include company_id and the demo company is owned by admin@company.com. Insert company before users or insert then update. Do not add leave_balances rows.

    Create apps/web/src/lib/create-company-rpc.ts exporting buildCreateCompanyWithOwnerArgs({ email, passwordHash, firstName, lastName, companyName }) returning { p_email, p_password, p_first_name, p_last_name, p_company_name }. passwordHash is string | null; p_password is that value (null stays null; do not convert null to empty string; always include the key). Tests in create-company-rpc.test.ts: hash in → p_password equals hash; null in → p_password === null and Object.prototype.hasOwnProperty.call(args, 'p_password'). Append src/lib/create-company-rpc.test.ts to the web test script.

    Extend userRegistrationSchema with companyName min 1 max 80 message Company name is required.

    Change POST signup: after 409 duplicate email check and bcrypt 12, call supabase.rpc('create_company_with_owner', buildCreateCompanyWithOwnerArgs({ email, passwordHash: hashedPassword, firstName, lastName, companyName })). First-user role admin. Keep 400/409/201/500 patterns and strip password from JSON. Identity stays on @/lib/supabase — do not add methods to IDatabaseService.

    Fix signup page POST keys to firstName, lastName, confirmPassword, companyName (page today sends first_name/last_name and omits confirmPassword). UI-SPEC: heading Create your company; description Enter your company name, then create the owner account. Teammates join by invite only.; Company name label/placeholder Acme Inc.; Building2 icon h-12 input above Google; submit Create company / Creating company...; success toast Company created. Sign in with your new credentials.; 409 client copy An account with this email already exists. Sign in, or ask your admin for an invite.; primary CTA from-primary like sign-in (replace from-blue-500 to-sky-500); keep react-hot-toast; keep password-requirement widget strings; Google remains on the page but 01-03 will require company name before signIn('google'). First focus Company name. Do not switch this page to react-hook-form.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists from create-migration.js (filename matches the script output; starts with an 8+ digit prefix)
    - SQL contains create_company_with_owner, p_password, companies, company_invites, VARCHAR(64), users.company_id
    - git diff packages/database/migrations/009_fix_rls_for_nextauth.sql is empty
    - userRegistrationSchema includes companyName
    - create_company_rpc.test.ts is in the web test script and npm test --workspace=@timeoff/web exits 0
    - packages/types User and users/types.ts User include company_id
    - signup page JSON keys include firstName, lastName, confirmPassword, companyName
    - signup route calls buildCreateCompanyWithOwnerArgs and supabase.rpc with create_company_with_owner
    - IDatabaseService in packages/database/src/index.ts is unchanged
    - no new files under packages/database/src/modules/companies or packages/database/src/modules/company-invites
  </acceptance_criteria>
  <done>Credentials create-company path is coded; schema not yet pushed.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Push companies schema to the database</name>
  <precondition>apps/web/.env.local has NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY; supabase CLI can reach local (npm run supabase:start) or the linked remote.</precondition>
  <files>packages/database/migrations/20250818193733_add_companies_and_invites.sql</files>
  <read_first>package.json, packages/database/migrations/20250818193733_add_companies_and_invites.sql</read_first>
  <action>
    After the migration file from the tracer exists, apply it before any signup verification that hits Postgres.

    From repo root run npm run supabase:db:push -- --yes (passes --yes to supabase db push). If the CLI still prompts, stop — this plan is already non-autonomous; do not invent a different migration runner.

    Confirm the function exists with a supabase SQL or studio-equivalent check: create_company_with_owner is present with nullable p_password; users has company_id; company_invites.token_hash is character/varchar 64.

    Then npm test --workspace=@timeoff/web. Optional smoke: POST /api/auth/signup against the running app only if npm run dev is already up; do not require a new host.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push --yes completed without skipping the new migration
    - create_company_with_owner exists in the target database
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Schema is live; tracer can persist a company.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/auth/signup | Untrusted JSON (companyName, email, password) |
| Signup route → Postgres RPC | Anon key calls create_company_with_owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | POST /api/auth/signup | high | mitigate | Zod companyName 1–80; bcrypt 12; unique email 409 before RPC |
| T-01-05 | Elevation of Privilege | create_company_with_owner | high | mitigate | One transaction: company then owner then owner_id; role admin is existing CHECK |
| T-01-06 | Information Disclosure | companies / company_invites RLS | medium | accept | Open policies matching 009; TENANT-04 is Phase 2 — do not claim isolation is done |
| T-01-07 | Repudiation | duplicate signup | low | mitigate | 409 exact API message; client maps UI-SPEC copy |
| T-01-SC | Tampering | npm installs | high | accept | No new packages |

ASVS L1: mitigate high on this boundary. Cross-tenant PostgREST reads remain Phase 2.
</threat_model>

<verification>
Migration applied. Signup page posts camelCase + companyName. Duplicate email 409. npm test --workspace=@timeoff/web green.
<human-check>
Open /auth/signup: company name field above Google, heading Create your company, Create company CTA uses primary gradient, loading state keeps the card visible.
</human-check>
</verification>

<success_criteria>
A new email can create a company and become owner; repeating the same email does not create a second company.
</success_criteria>

<output>
Create `.planning/phases/01-company-signup-and-invites/01-02-SUMMARY.md` when done
</output>
