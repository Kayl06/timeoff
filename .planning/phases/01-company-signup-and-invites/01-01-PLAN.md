---
phase: 01-company-signup-and-invites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/invite-token.ts
  - apps/web/src/lib/invite-token.test.ts
  - apps/web/src/lib/google-signin-gate.ts
  - apps/web/src/lib/google-signin-gate.test.ts
  - apps/web/src/lib/company-owner.ts
  - apps/web/src/lib/company-owner.test.ts
autonomous: true
requirements:
  - TENANT-01
  - TENANT-05
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "hashInviteToken is SHA-256 of the raw token; inviteTokenMatches uses timingSafeEqual on equal-length digests (TENANT-02/03 token safety)."
    - "decideGoogleSignIn returns true when existingUser is set, or pendingCompany is set, or pendingInvite is set; otherwise it returns the InviteRequired path string (TENANT-05)."
    - "isCompanyOwner returns true only when userId equals ownerId and both are non-empty (TENANT-01/02 owner check)."
    - "apps/web package.json test script runs node:test with experimental-strip-types on the three Wave 0 test files."
    - "Second credentials signup with the same email is 409 and must not create a second company for that email (TENANT-01 idempotency — locked here as company-owner/signup contract; wired in 01-02)."
    - "Concurrent signup: one winner; loser is 409; no company row without owner_id (TENANT-01 concurrency — RPC in 01-02; helper tests lock owner assignment shape)."
  artifacts:
    - path: "apps/web/src/lib/invite-token.ts"
      provides: "generateInviteToken, hashInviteToken, inviteTokenMatches"
      exports: ["generateInviteToken", "hashInviteToken", "inviteTokenMatches"]
    - path: "apps/web/src/lib/google-signin-gate.ts"
      provides: "decideGoogleSignIn pure gate"
      exports: ["decideGoogleSignIn", "INVITE_REQUIRED_PATH"]
    - path: "apps/web/src/lib/company-owner.ts"
      provides: "isCompanyOwner"
      exports: ["isCompanyOwner"]
    - path: "apps/web/package.json"
      provides: "test script for node:test"
      contains: "experimental-strip-types"
  key_links:
    - from: "apps/web/src/lib/google-signin-gate.ts"
      to: "apps/web/src/lib/auth.ts"
      via: "01-03 signIn callback will call decideGoogleSignIn — not wired this plan"
      pattern: "decideGoogleSignIn"
    - from: "apps/web/package.json"
      to: "apps/web/src/lib/invite-token.test.ts"
      via: "npm test --workspace=@timeoff/web"
      pattern: "invite-token.test.ts"
  prohibitions:
    - statement: "Do not rebuild the stub /(admin)/users page as a global operator console."
      status: resolved
      verification: judgment
      reason: "PROJECT.md Admin IA; onboarding is signup plus invites."
    - statement: "Do not restyle dashboard, request form, calendar, forgot-password, or reset-password chrome except the owner invite entry."
      status: resolved
      verification: judgment
      reason: "UI-SPEC brownfield lock."
    - statement: "Do not add a new component library, font, or brand palette."
      status: resolved
      verification: judgment
      reason: "UI-SPEC design system lock."
    - statement: "Do not put invite or company writes on the browser IDatabaseService facade or the anon-key client from hooks."
      status: resolved
      verification: judgment
      reason: "Open RLS plus anon key would let anyone insert invites."
    - statement: "Do not add a fifth value to the users.role CHECK; ownership is companies.owner_id and first-user role stays admin."
      status: resolved
      verification: judgment
      reason: "Existing CHECK is employee, supervisor, admin, hr."
    - statement: "Do not finish tenant RLS this phase; new tables use the same open policies as migration 009."
      status: resolved
      verification: judgment
      reason: "TENANT-04 / AUTHZ-02 are Phase 2."
    - statement: "Do not add a mailer this phase; invites are copy-link only."
      status: resolved
      verification: judgment
      reason: "INTEGRATIONS.md has no mailer; UI-SPEC success helper is the link."
    - statement: "Do not auto-provision unknown Google users into a global employee pool."
      status: resolved
      verification: judgment
      reason: "TENANT-05; decideGoogleSignIn deny path locked this plan, wired in 01-03."
---

## Phase Goal

**As a** first user of a client company, **I want to** create my company at signup and invite teammates by email, **so that** later people join only that company and Google cannot drop anyone into a global employee pool.

<objective>
Wave 0 Nyquist helpers for TENANT-01 owner checks, TENANT-05 Google gate, and hashed invite tokens — so later tracer and expansion plans have failing-closed unit tests and `npm test --workspace=@timeoff/web`.

Purpose: 01-VALIDATION.md Wave 0 must exist before schema/UI work. Pure functions only; no new routes or tables.
Output: invite-token, google-signin-gate, company-owner modules plus tests; web `test` script.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-company-signup-and-invites/01-RESEARCH.md
@.planning/phases/01-company-signup-and-invites/01-PATTERNS.md
@.planning/phases/01-company-signup-and-invites/01-VALIDATION.md
@apps/web/src/lib/date-utils.ts
@apps/web/package.json
</context>

<interfaces>
From apps/web/src/lib/date-utils.ts (module shape analog):
named exports, kebab-case file, JSDoc on exported functions.

From 01-RESEARCH.md Pattern 3 — implement these exact names:
generateInviteToken(): string — randomBytes(32).toString('hex')
hashInviteToken(token: string): Buffer — createHash('sha256').update(token, 'utf8').digest()
inviteTokenMatches(token: string, storedHash: Buffer): boolean — length check then timingSafeEqual

decideGoogleSignIn(input: { existingUser: boolean; pendingCompany: boolean; pendingInvite: boolean }): true | typeof INVITE_REQUIRED_PATH
INVITE_REQUIRED_PATH is the string /auth/error?error=InviteRequired (export the constant; do not return a boolean deny)

isCompanyOwner(userId: string, ownerId: string): boolean
</interfaces>

## Artifacts this phase produces

- `generateInviteToken`, `hashInviteToken`, `inviteTokenMatches` in `apps/web/src/lib/invite-token.ts`
- `decideGoogleSignIn`, `INVITE_REQUIRED_PATH` in `apps/web/src/lib/google-signin-gate.ts`
- `isCompanyOwner` in `apps/web/src/lib/company-owner.ts`
- `apps/web` script `test`
- test files `invite-token.test.ts`, `google-signin-gate.test.ts`, `company-owner.test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0: invite token helpers and web test script</name>
  <files>apps/web/package.json, apps/web/src/lib/invite-token.ts, apps/web/src/lib/invite-token.test.ts</files>
  <read_first>apps/web/src/lib/date-utils.ts, apps/web/package.json, .planning/phases/01-company-signup-and-invites/01-VALIDATION.md, .planning/phases/01-company-signup-and-invites/01-RESEARCH.md</read_first>
  <behavior>
    - generateInviteToken returns a 64-character hex string
    - hashInviteToken is deterministic for the same input and 32 bytes long
    - inviteTokenMatches is true for the generating token and false for a different 64-hex token
    - npm test --workspace=@timeoff/web runs the invite-token test file
  </behavior>
  <action>
    Add apps/web script test as node --test --experimental-strip-types src/lib/invite-token.test.ts (append the other two test filenames in later tasks in this plan). Do not add vitest, tsx, or Jest. Do not upgrade zod.

    Create invite-token.ts using node:crypto only: randomBytes, createHash sha256, timingSafeEqual. Named exports generateInviteToken, hashInviteToken, inviteTokenMatches. File-level JSDoc. Never log the raw token.

    Create invite-token.test.ts with node:test and node:assert/strict. If executor Node is 18 and strip-types fails, compile the two files with tsc to a temp dir then node --test the emitted JS; do not install tsx.

    Per TENANT-02/03: lookup will be by hash; this helper is the only token primitive later routes may use.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json scripts.test includes node --test --experimental-strip-types and invite-token.test.ts
    - generateInviteToken, hashInviteToken, inviteTokenMatches are exported from invite-token.ts
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Token helpers tested; web test script exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: Google sign-in gate pure function</name>
  <files>apps/web/src/lib/google-signin-gate.ts, apps/web/src/lib/google-signin-gate.test.ts, apps/web/package.json</files>
  <read_first>apps/web/src/lib/invite-token.ts, .planning/phases/01-company-signup-and-invites/01-RESEARCH.md</read_first>
  <behavior>
    - existingUser true returns true regardless of pending flags
    - existingUser false and pendingCompany true returns true
    - existingUser false and pendingInvite true returns true
    - existingUser false and both pending flags false returns INVITE_REQUIRED_PATH
    - INVITE_REQUIRED_PATH equals /auth/error?error=InviteRequired
  </behavior>
  <action>
    Create google-signin-gate.ts as a pure function decideGoogleSignIn. Export INVITE_REQUIRED_PATH as /auth/error?error=InviteRequired. The deny result is that string (NextAuth signIn string-redirect). Do not return a boolean deny from this helper — AccessDenied is the wrong error page.

    Unknown Google without pending company or invite cookie must not insert a users row; this function is the decision table 01-03 will call. Do not import supabase or next-auth here.

    Add google-signin-gate.test.ts. Append src/lib/google-signin-gate.test.ts to the web test script.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - decideGoogleSignIn and INVITE_REQUIRED_PATH are exported
    - tests cover existing user, pending company, pending invite, and deny URL
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>TENANT-05 gate is unit-tested before auth.ts wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: company owner predicate</name>
  <files>apps/web/src/lib/company-owner.ts, apps/web/src/lib/company-owner.test.ts, apps/web/package.json</files>
  <read_first>apps/web/src/lib/date-utils.ts, .planning/phases/01-company-signup-and-invites/01-RESEARCH.md</read_first>
  <behavior>
    - isCompanyOwner('a','a') is true
    - isCompanyOwner('a','b') is false
    - isCompanyOwner('','') is false
    - isCompanyOwner('a','') is false
  </behavior>
  <action>
    Create company-owner.ts exporting isCompanyOwner(userId, ownerId). True only when both strings are non-empty and equal. Invite APIs in 01-04 must use this plus companies.owner_id, not role === admin alone.

    Add company-owner.test.ts. Append it to the web test script so the script lists invite-token.test.ts, google-signin-gate.test.ts, and company-owner.test.ts.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - isCompanyOwner exported
    - web test script lists all three Wave 0 test files
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Owner predicate tested for TENANT-01/02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helper → later Route Handlers | Token hashing and owner checks will sit in front of Postgres writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | invite-token.ts | high | mitigate | 32-byte CSPRNG token; SHA-256 at rest; timingSafeEqual compare |
| T-01-02 | Elevation of Privilege | google-signin-gate.ts | high | mitigate | Default deny unless existing user or pending company/invite; 01-03 must call this before any Google insert |
| T-01-03 | Elevation of Privilege | company-owner.ts | medium | mitigate | Owner is id equality against companies.owner_id (wired 01-04), not admin role alone |
| T-01-SC | Tampering | npm installs | high | accept | No new packages this plan; Wave 0 uses node:test |
</threat_model>

<verification>
npm test --workspace=@timeoff/web exits 0. All three test files exist. No vitest/tsx in package.json.
</verification>

<success_criteria>
Wave 0 of 01-VALIDATION.md is implemented: web test script, invite-token, google-signin-gate, company-owner and their tests.
</success_criteria>

<output>
Create `.planning/phases/01-company-signup-and-invites/01-01-SUMMARY.md` when done
</output>
