---
phase: 01-company-signup-and-invites
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - apps/web/src/lib/validation.ts
  - apps/web/src/app/api/auth/invites/route.ts
  - apps/web/src/components/invite-teammates-dialog.tsx
  - apps/web/src/components/navigation.tsx
  - apps/web/src/components/dashboard/dashboard-view.tsx
autonomous: true
requirements:
  - TENANT-02

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "POST /api/auth/invites uses getServerSession(authOptions); missing session is 401; session.user.id not equal companies.owner_id is 403 with Only the company owner can invite teammates."
    - "Invite company_id always equals the session owner's company; the client cannot choose another company_id (TENANT-02)."
    - "201 body includes email, acceptUrl (raw token in query), expiresAt; token_hash stored; raw token not stored."
    - "409 when that email is already a user in this company or has a pending invite; client shows That email is already in this company."
    - "Zero pending invites: Empty state heading No teammates yet + Empty state body. Email field still shown above the list."
    - "Sending invite... + Loader2; email + submit disabled; existing list rows stay."
    - "Validation copy inline; Duplicate / already member copy; API toast; dialog stays open. Unauthorized copy if non-owner."
    - "Typical volume: a short stack of pending/sent emails (one row per invite). Latest send also shows helper + read-only link + Copy invite link."
    - "One new email at a time; list may already have rows while the field is empty for the next invite."
    - "Dialog body scrolls if many pending rows. Company name wraps in the description. Emails are single-line; long emails scroll inside the input."
    - "Zero: empty copy. One: single row + copy link after send. Many: stacked rows; still one email per Send invite (no batch)."
    - "Invite URL is a selectable Input readOnly + Copy, not truncated mid-URL. Emails scroll in the field."
    - "Hidden for non-owners (zero items). Owners always see the item between Settings and Log out."
    - "Menu item is instant; no spinner on the nav item. Loading lives in the dialog (E3)."
    - "Unauthorized copy is API/dialog (E3), not on the menu item."
    - "One Invite teammates item for owners."
    - "Item is all-or-nothing; never half-visible."
    - "shadcn DropdownMenu default overflow (scroll/clip inside the menu). Label Invite teammates does not truncate."
    - "Zero for non-owner; one for owner; never many invite items."
    - "Label is the fixed string Invite teammates."
    - "Same empty copy as E3 when the owner has no teammates yet. Do not build /(admin)/users."
    - "No new dashboard loader; empty copy appears after the existing session/dashboard load. List loading is E3."
    - "Failed invite/member fetch uses existing dashboard toast; do not add a teammates directory error page."
    - "Owners with teammates use the invite dialog list (E3). Dashboard does not add a teammate table this phase."
    - "No partial teammate directory on the dashboard."
    - "No dashboard list this phase; overflow is handled in the dialog (E3)."
    - "Zero: empty copy on dashboard. One/many: shown in the E3 dialog list only."
    - "Empty state body wraps; no ellipsis."
  artifacts:
    - path: "apps/web/src/app/api/auth/invites/route.ts"
      provides: "GET pending invites, POST create invite"
      exports: ["GET", "POST"]
    - path: "apps/web/src/components/invite-teammates-dialog.tsx"
      provides: "Owner invite dialog"
      exports: ["InviteTeammatesDialog"]
    - path: "apps/web/src/components/navigation.tsx"
      provides: "Invite teammates menu item"
      contains: "Invite teammates"
  key_links:
    - from: "apps/web/src/components/invite-teammates-dialog.tsx"
      to: "apps/web/src/app/api/auth/invites/route.ts"
      via: "fetch POST/GET /api/auth/invites credentials include"
      pattern: "/api/auth/invites"
    - from: "apps/web/src/app/api/auth/invites/route.ts"
      to: "apps/web/src/lib/company-owner.ts"
      via: "isCompanyOwner(session.user.id, company.owner_id)"
      pattern: "isCompanyOwner"
    - from: "apps/web/src/components/navigation.tsx"
      to: "apps/web/src/components/invite-teammates-dialog.tsx"
      via: "isOwner menu item opens dialog"
      pattern: "InviteTeammatesDialog"
  prohibitions:
    - statement: "Do not rebuild the stub /(admin)/users page."
      status: resolved
      verification: judgment
    - statement: "Do not put invite writes on IDatabaseService or useDatabaseService."
      status: resolved
      verification: judgment
    - statement: "Do not add a mailer; copy-link only."
      status: resolved
      verification: judgment
    - statement: "Do not add a dashboard header CTA next to LeaveRequestForm."
      status: resolved
      verification: judgment
    - statement: "Do not implement Settings."
      status: resolved
      verification: judgment
    - statement: "Do not finish RLS this phase."
      status: resolved
      verification: judgment
---

## Phase Goal

**As a** first user of a client company, **I want to** create my company at signup and invite teammates by email, **so that** later people join only that company and Google cannot drop anyone into a global employee pool.

<objective>
Owner-only copy-link invites bound to the owner's company, with dialog list and nav entry per UI-SPEC.

Purpose: TENANT-02. Accept flow is 01-05.
Output: GET/POST /api/auth/invites, InviteTeammatesDialog, nav item, dashboard empty copy for owners with no teammates.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-company-signup-and-invites/01-RESEARCH.md
@.planning/phases/01-company-signup-and-invites/01-PATTERNS.md
@.planning/phases/01-company-signup-and-invites/01-UI-SPEC.md
@apps/web/src/app/api/auth/signup/route.ts
@apps/web/src/components/leave-request/enhanced-approve-dialog.tsx
@apps/web/src/components/navigation.tsx
@apps/web/src/hooks/use-leave-request-operations.ts
@apps/web/src/lib/invite-token.ts
@apps/web/src/lib/company-owner.ts
@apps/web/src/lib/auth.ts
</context>

<interfaces>
getServerSession from next-auth/next with authOptions from @/lib/auth
Middleware matcher excludes api — session MUST be checked in the route.

inviteEmailSchema: z.object({ email: emailSchema }) from validation.ts
generateInviteToken / hashInviteToken from invite-token.ts
isCompanyOwner from company-owner.ts

acceptUrl: `${NEXTAUTH_URL or origin}/auth/accept-invite?token=${rawToken}`
expires_at: now + 7 days
status pending
token_hash unique index lookup by hash hex

Unauthorized copy (401 and 403 JSON error): Only the company owner can invite teammates.

GET response: { invites: [{ email, createdAt, expiresAt }], teammateCount, companyName }
teammateCount = users with this company_id excluding the owner (for E7 dashboard empty copy)
</interfaces>

## Artifacts this phase produces

- `inviteEmailSchema`, `InviteEmailInput`
- `GET`/`POST` `apps/web/src/app/api/auth/invites/route.ts`
- `InviteTeammatesDialog`
- Navigation item Invite teammates when session.user.isOwner
- Dashboard empty-state copy for owners with teammateCount === 0 (no new table)

<tasks>

<task type="auto" tdd="true">
  <name>Owner invite create and list APIs</name>
  <files>apps/web/src/lib/validation.ts, apps/web/src/app/api/auth/invites/route.ts</files>
  <read_first>apps/web/src/app/api/auth/signup/route.ts, apps/web/src/lib/validation.ts, apps/web/src/lib/invite-token.ts, apps/web/src/lib/company-owner.ts, apps/web/src/lib/auth.ts, apps/web/src/middleware.ts</read_first>
  <behavior>
    - No session → 401 JSON error Only the company owner can invite teammates.
    - Session user is not companies.owner_id → 403 same error string
    - POST { email } as owner → 201 { email, acceptUrl, expiresAt }; company_invites.company_id equals owner company
    - POST email of existing user in that company → 409
    - GET as owner returns only that company's pending rows
  </behavior>
  <action>
    Add inviteEmailSchema using existing emailSchema. Export InviteEmailInput via z.infer.

    Create apps/web/src/app/api/auth/invites/route.ts with GET and POST. getServerSession(authOptions). Load the company where owner_id = session.user.id (or session.user.companyId then verify isCompanyOwner). Never take company_id from the request body.

    POST: validateInput; if email already in users for this company_id or pending invite for this company, 409. generateInviteToken; store hashInviteToken as hex or bytea consistently; expires_at now+7d; status pending. Return acceptUrl with the raw token query param. Never insert via useDatabaseService. Never log the raw token (devLog the invite id only).

    GET: pending invites for this company_id only, plus teammateCount and companyName for the dialog description They'll join {company name} only. They cannot see any other company.

    400 validation, 500 Internal server error + devLog.error. Do not add invite methods to packages/database/src/index.ts IDatabaseService.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - route.ts exports GET and POST
    - file contains getServerSession and isCompanyOwner
    - POST body schema is email only (no company_id field)
    - packages/database/src/index.ts IDatabaseService has no new methods (git diff on that interface is empty)
  </acceptance_criteria>
  <done>Owner can create a hashed invite bound to their company only.</done>
</task>

<task type="auto">
  <name>Invite teammates dialog and nav item</name>
  <files>apps/web/src/components/invite-teammates-dialog.tsx, apps/web/src/components/navigation.tsx</files>
  <read_first>apps/web/src/components/leave-request/enhanced-approve-dialog.tsx, apps/web/src/components/navigation.tsx, apps/web/src/hooks/use-leave-request-operations.ts, .planning/phases/01-company-signup-and-invites/01-UI-SPEC.md</read_first>
  <action>
    Create InviteTeammatesDialog as 'use client'. shadcn Dialog from @/components/ui/dialog. Heading Invite teammates. Description uses companyName from GET. Work email Input h-12 placeholder teammate@company.com. Primary Send invite / Sending invite... with Loader2. sonner toasts (not react-hot-toast): Invite created for {email}. After 201 show helper Share this link if they don’t get email: plus readOnly Input with acceptUrl and outline Copy invite link; on copy, Link copied. Stay open for the next email. Invalid email: Please enter a valid email address. 409: That email is already in this company. Fetch GET on open. Empty list: No teammates yet and Invite people by email so they can join this company. Open your profile menu and choose Invite teammates. List area scrolls on overflow. Import Mail, Loader2 from lucide. 2-space indent.

    navigation.tsx: insert Invite teammates with Mail icon between Settings and the separator before Log out. Render only when session.user.isOwner. onClick opens the dialog (lift dialog state in Navigation). Do not implement Settings. Do not add a header CTA beside LeaveRequestForm.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - InviteTeammatesDialog is exported
    - navigation.tsx contains Invite teammates and isOwner
    - invite dialog imports toast from sonner
    - dashboard-header.tsx has no Invite teammates control
  </acceptance_criteria>
  <done>Owner can copy an invite link from the avatar menu.</done>
</task>

<task type="auto">
  <name>Dashboard empty copy for owners with no teammates</name>
  <files>apps/web/src/components/dashboard/dashboard-view.tsx</files>
  <read_first>apps/web/src/components/dashboard/dashboard-view.tsx, apps/web/src/components/dashboard/dashboard-header.tsx, .planning/phases/01-company-signup-and-invites/01-UI-SPEC.md</read_first>
  <action>
    When session.user.isOwner, GET /api/auth/invites (or reuse a tiny fetch) after session is ready. If teammateCount === 0, render the empty-state heading No teammates yet and body Invite people by email so they can join this company. Open your profile menu and choose Invite teammates. below the existing header, wrapping text, no ellipsis, no new teammate table, no new full-page loader, no visit to (admin)/users.

    Failed fetch: sonner error using existing dashboard toast style; do not add a directory error page. Do not restyle DashboardHeader, tabs, calendar, or LeaveRequestForm. Do not add a header button next to the request form.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-view.tsx contains No teammates yet
    - dashboard-header.tsx has no Invite control
    - apps/web/src/app/(admin)/users/page.tsx has no feature work in this plan's diff
  </acceptance_criteria>
  <done>Owners with only themselves see the UI-SPEC empty copy on the dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/auth/invites | Session cookie required; body email untrusted |
| Invite token in acceptUrl | Secret in URL; hashed at rest |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Elevation of Privilege | POST /api/auth/invites | high | mitigate | getServerSession + isCompanyOwner vs companies.owner_id; no company_id in body |
| T-01-13 | Spoofing | invite tokens | high | mitigate | randomBytes 32; store SHA-256 only |
| T-01-14 | Information Disclosure | GET invites | medium | mitigate | Filter pending rows by owner company_id only; preview of other companies is 01-05 token hash lookup |
| T-01-15 | Tampering | CSRF invite POST | medium | mitigate | Same-origin fetch + NextAuth session cookie SameSite; no extra CSRF library |
| T-01-SC | Tampering | npm installs | high | accept | No new packages |

ASVS L1: owner check is on the API, not only hidden nav. RLS still open (Phase 2).
</threat_model>

<verification>
npm test --workspace=@timeoff/web. Invites route uses getServerSession. Nav item gated on isOwner.
<human-check>
Sign in as owner: Invite teammates between Settings and Log out. Send invite, copy link, dialog stays open. Non-owner: item hidden; POST invites returns 403 copy.
</human-check>
</verification>

<success_criteria>
The company owner can invite by email into only their company and share a copy link.
</success_criteria>

<output>
Create `.planning/phases/01-company-signup-and-invites/01-04-SUMMARY.md` when done
</output>
