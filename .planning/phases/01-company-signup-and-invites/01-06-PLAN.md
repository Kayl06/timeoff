---
phase: 01-company-signup-and-invites
plan: 06
type: execute
wave: 6
depends_on:
  - 01-05
files_modified:
  - apps/web/src/lib/password-client.ts
  - apps/web/src/lib/password-client.test.ts
  - apps/web/src/app/auth/signup/page.tsx
  - apps/web/package.json
autonomous: true
gap_closure: true
gap_ids:
  - G-01-1
requirements:
  - TENANT-01
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On /auth/signup, a password shorter than PASSWORD_REQUIREMENTS.minLength (12) cannot submit Create company; the password field shows Password must be at least 12 characters (G-01-1)."
    - "A 12-character password missing uppercase, lowercase, number, or special character cannot submit; the matching passwordSchema message appears on the password field before the request is sent (G-01-1)."
    - "If POST /api/auth/signup still returns 400, the page copies details.password (and other details keys) onto errors.* so the field message is visible; it does not rely on an unmounted toaster for the rule text (G-01-1)."
    - "Visible toasts on signup (success Company created. Sign in with your new credentials. and non-field failures) use sonner, which SessionProvider already mounts (G-01-1)."
    - "Duplicate email 409 still sets the email field to An account with this email already exists. Sign in, or ask your admin for an invite."
    - "Google OAuth env is not required; do not add GOOGLE_CLIENT_ID checks or skip credentials signup if Google vars are empty."
  artifacts:
    - path: "apps/web/src/lib/password-client.ts"
      provides: "Client checks aligned to PASSWORD_REQUIREMENTS / passwordSchema"
      exports: ["passwordMeetsApiRules", "firstPasswordError", "passwordRequirementItems", "mergeSignupFieldErrors"]
    - path: "apps/web/src/lib/password-client.test.ts"
      provides: "node:test coverage for length, composition, and 400 details merge"
    - path: "apps/web/src/app/auth/signup/page.tsx"
      provides: "Visible password validation on Create your company"
      contains: "passwordMeetsApiRules"
  key_links:
    - from: "apps/web/src/app/auth/signup/page.tsx"
      to: "apps/web/src/lib/password-client.ts"
      via: "validateForm, isFormReady, checklist, 400 details"
      pattern: "passwordMeetsApiRules"
    - from: "apps/web/src/app/auth/signup/page.tsx"
      to: "apps/web/src/app/api/auth/signup/route.ts"
      via: "400 JSON details mapped onto errors.*"
      pattern: "data.details"
    - from: "apps/web/src/app/auth/signup/page.tsx"
      to: "apps/web/src/providers/session-provider.tsx"
      via: "sonner Toaster already mounted; signup imports toast from sonner"
      pattern: "from 'sonner'"
  prohibitions:
    - statement: "Do not rebuild the stub /(admin)/users page."
      status: resolved
      verification: judgment
    - statement: "Do not restyle dashboard, request form, or calendar chrome."
      status: resolved
      verification: judgment
    - statement: "Do not remount a second toaster in session-provider; signup switches to the already-mounted sonner."
      status: resolved
      verification: judgment
    - statement: "Do not weaken API passwordSchema; client must match it, not the reverse."
      status: resolved
      verification: judgment
    - statement: "Do not require Google OAuth env for this plan."
      status: resolved
      verification: judgment
---

## Phase Goal

Close UAT G-01-1: invalid passwords on credentials create-company show a visible error.

<objective>
Align /auth/signup client password rules with PASSWORD_REQUIREMENTS / passwordSchema, map 400 details onto field errors, and use sonner so rejection is visible.

Purpose: UAT test 1 reported no error when the password failed verification. Root cause is client length 8 plus advisory-only complexity, unused 400 details, and toasts aimed at an unmounted library (G-01-1 / WR-02).
Output: password-client helper + tests; signup page wired to those rules, details mapping, and sonner.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-company-signup-and-invites/01-UAT.md
@.planning/debug/signup-password-validation-feedback.md
@.planning/phases/01-company-signup-and-invites/01-02-SUMMARY.md
@apps/web/src/lib/validation.ts
@apps/web/src/app/api/auth/signup/route.ts
@apps/web/src/app/auth/signup/page.tsx
@apps/web/src/app/auth/signin/page.tsx
@apps/web/src/providers/session-provider.tsx
</context>

<interfaces>
PASSWORD_REQUIREMENTS in apps/web/src/lib/validation.ts: minLength 12; requireUppercase, requireLowercase, requireNumbers, requireSpecialChars all true.

passwordSchema messages (copy these strings in firstPasswordError):
- Password must be at least ${PASSWORD_REQUIREMENTS.minLength} characters
- Password must contain at least one uppercase letter
- Password must contain at least one lowercase letter
- Password must contain at least one number
- Password must contain at least one special character

Composition regexes (same as passwordSchema): /[A-Z]/, /[a-z]/, /[0-9]/, /[^A-Za-z0-9]/

POST /api/auth/signup 400 body: { error: 'Validation failed', details: Record<field, message>, message: 'Please check your input and try again' } via formatValidationErrors. details.password holds the real rule text.

SessionProvider mounts Sonner from @/components/ui/sonner only. Sign-in already imports { toast } from 'sonner'.

Duplicate email copy stays DUPLICATE_EMAIL_COPY on the email field for HTTP 409.

Do not change userRegistrationSchema or the signup route 400 shape.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end signup password rejection is visible</name>
  <files>apps/web/src/lib/password-client.ts, apps/web/src/lib/password-client.test.ts, apps/web/src/app/auth/signup/page.tsx, apps/web/package.json</files>
  <read_first>apps/web/src/lib/validation.ts, apps/web/src/app/auth/signup/page.tsx, apps/web/src/app/api/auth/signup/route.ts, apps/web/src/app/auth/signin/page.tsx, apps/web/src/providers/session-provider.tsx, apps/web/package.json</read_first>
  <behavior>
    - passwordMeetsApiRules('short8!!') is false; firstPasswordError is Password must be at least 12 characters
    - passwordMeetsApiRules('abcdefghijkl') is false (no upper/number/special)
    - passwordMeetsApiRules('Abcdefghij1!') is true
    - passwordRequirementItems uses PASSWORD_REQUIREMENTS.minLength in the length label; length met is password.length >= PASSWORD_REQUIREMENTS.minLength
    - mergeSignupFieldErrors({ password: 'Password must be at least 12 characters' }) returns { password: that message }
    - mergeSignupFieldErrors(undefined) returns {}
  </behavior>
  <action>
    Create apps/web/src/lib/password-client.ts importing PASSWORD_REQUIREMENTS from ./validation.ts (use the .ts suffix if Node 26 tests cannot resolve the leaf, matching invite-auth.ts). Export passwordMeetsApiRules(password: string): boolean, firstPasswordError(password: string): string | undefined (empty password: Password is required; else first failing passwordSchema rule in schema order), passwordRequirementItems(password: string): { label: string; met: boolean }[] with five rows matching the existing checklist structure but length gated on PASSWORD_REQUIREMENTS.minLength, and mergeSignupFieldErrors(details: Record&lt;string, string&gt; | undefined): Record&lt;string, string&gt; that copies own enumerable keys from details (empty object when details is missing).

    Tests in password-client.test.ts with node:assert/strict and node:test. Append src/lib/password-client.test.ts to the apps/web package.json test script list (keep the existing files).

    On signup/page.tsx: import the helper; import { toast } from 'sonner' (same as sign-in). Use firstPasswordError in validateForm instead of a length-8-only check. isFormReady requires passwordMeetsApiRules(formData.password). Checklist renders passwordRequirementItems(formData.password). Length contribution to calculatePasswordStrength uses PASSWORD_REQUIREMENTS.minLength.

    In handleTraditionalSignUp, after JSON parse: keep 409 email-field mapping. On !response.ok, call setErrors with mergeSignupFieldErrors(data.details) merged onto previous errors (field messages from details win). toast.error may use data.error only as a fallback when details is missing or empty. Success toast stays Company created. Sign in with your new credentials. then router.push /auth/signin.

    Switch remaining toast calls on this page (Google company-name failures, catch) to the same sonner import. Do not edit session-provider. Do not change the signup route. Do not add Google env checks. Do not restyle the card chrome; only validation gating, checklist labels derived from PASSWORD_REQUIREMENTS, error mapping, and toast import.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - password-client.test.ts is listed in apps/web/package.json test script
    - npm test --workspace=@timeoff/web exits 0
    - signup/page.tsx imports passwordMeetsApiRules and mergeSignupFieldErrors
    - signup/page.tsx imports toast from sonner
    - signup/page.tsx reads data.details on failed POST
    - signup/page.tsx isFormReady uses passwordMeetsApiRules
    - apps/web/src/providers/session-provider.tsx is unchanged (git diff empty on that file)
    - apps/web/src/app/api/auth/signup/route.ts is unchanged
  </acceptance_criteria>
  <done>Invalid passwords on Create your company show a field error (and sonner if needed); 8-character submit is blocked client-side.</done>
</task>

<task type="auto">
  <name>Keep duplicate-email and Google-without-env paths intact</name>
  <files>apps/web/src/app/auth/signup/page.tsx</files>
  <read_first>apps/web/src/app/auth/signup/page.tsx</read_first>
  <action>
    Confirm 409 still sets errors.email to DUPLICATE_EMAIL_COPY and does not treat duplicate as a password failure. Confirm handleGoogleSignUp still only requires a non-empty company name and does not read GOOGLE_CLIENT_ID. Confirm Create company remains disabled until passwordMeetsApiRules is true plus the existing name/email/confirm/terms gates. No dashboard or calendar files.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - signup/page.tsx still contains DUPLICATE_EMAIL_COPY
    - handleGoogleSignUp does not reference GOOGLE_CLIENT_ID
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>G-01-1 fix does not break duplicate-email copy or require Google setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/auth/signup | Untrusted password and profile JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | signup/page.tsx client checks | medium | mitigate | Client gating is UX only; userRegistrationSchema / passwordSchema on the route still 400 invalid passwords |
| T-01-11 | Information Disclosure | 400 details mapped to fields | low | mitigate | Merge only the submitter's own details object onto errors.*; do not invent extra server fields |
| T-01-SC | Tampering | npm installs | high | accept | No new packages |

ASVS L1: API remains source of truth. Client alignment closes the silent-400 UX gap.
</threat_model>

<verification>
npm test --workspace=@timeoff/web exits 0 including password-client.test.ts.
<human-check>
On /auth/signup, type an 8-character password: Create company stays disabled and the length checklist row is unmet. Type 12 letters with no number/special: still disabled with a password field or checklist miss. A valid 12+ composition password enables the CTA. A forced 400 still shows details on the field, not a silent submit.
</human-check>
</verification>

<success_criteria>
G-01-1: password verification failures are visible on credentials create-company. Sonner carries success/error toasts. Google env not required.
</success_criteria>

<output>
Create `.planning/phases/01-company-signup-and-invites/01-06-SUMMARY.md` when done
</output>
