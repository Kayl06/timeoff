---
phase: 02-tenant-isolation-and-server-authz
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/web/src/app/api/leave-requests/[id]/route.ts
  - apps/web/src/app/api/leave-requests/bulk/route.ts
  - apps/web/src/hooks/use-leave-request-operations.ts
  - apps/web/src/lib/validation.ts
autonomous: true
requirements:
  - AUTHZ-01
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PATCH /api/leave-requests/[id] without a session returns 401 (AUTHZ-01)."
    - "approve, reject, cancel, and delete on a signed-in session call IDatabaseService with approver_id from session.user.id, not from JSON (AUTHZ-01)."
    - "POST /api/leave-requests/bulk without a session returns 401; bulk approve/reject uses session.user.id (AUTHZ-01)."
    - "use-leave-request-operations mutationFns fetch those BFF routes with credentials include and keep existing sonner plus query invalidation (AUTHZ-01)."
    - "Flagged assumption TENANT-04 concurrency: two overlapping PATCHes each mint their own JWT; RLS in 02-06 still evaluates per request."
    - "Backstop AUTHZ-01: client cannot set approver_id; bindLeaveApprover runs on the server."
    - "Backstop AUTHZ-02: tenant client still uses the minted company_id claim (policies land in 02-06)."
    - "Flagged assumption AUTHZ-03 concurrency: anon grants unchanged until 02-06; hooks must not fall back to the browser anon client."
  artifacts:
    - path: "apps/web/src/app/api/leave-requests/[id]/route.ts"
      provides: "PATCH approve/reject/cancel/delete"
      exports: ["PATCH"]
    - path: "apps/web/src/app/api/leave-requests/bulk/route.ts"
      provides: "POST bulk approve/reject"
      exports: ["POST"]
    - path: "apps/web/src/lib/validation.ts"
      provides: "leaveRequestPatchBodySchema, leaveRequestBulkBodySchema"
      exports: ["leaveRequestPatchBodySchema", "leaveRequestBulkBodySchema"]
  key_links:
    - from: "apps/web/src/hooks/use-leave-request-operations.ts"
      to: "apps/web/src/app/api/leave-requests/[id]/route.ts"
      via: "fetch PATCH credentials include"
      pattern: "/api/leave-requests/"
    - from: "apps/web/src/hooks/use-leave-request-operations.ts"
      to: "apps/web/src/app/api/leave-requests/bulk/route.ts"
      via: "fetch POST /api/leave-requests/bulk"
      pattern: "/api/leave-requests/bulk"
  prohibitions:
    - statement: "Do not add owner to the users.role CHECK; ownership stays companies.owner_id."
    - statement: "Do not finish the stub /(admin)/users page."
    - statement: "Do not add new screens or restyle approve dialogs or tables."
    - statement: "Do not install jose 6.x, jsonwebtoken, Vitest, Jest, Playwright, or @supabase/ssr."
    - statement: "Do not use service_role for tenant leave queries."
    - statement: "Do not put the minted PostgREST JWT in a cookie or a client module."
    - statement: "Do not add company_id columns to leave_requests this phase."
    - statement: "Do not re-open tenant tables with unrestricted RLS predicates."
    - statement: "Do not implement BAL/LEAVE/NOTIF side-effect correctness; keep existing LeaveRequestService best-effort audit/notify."
---

## Phase Goal

**As a** signed-in employee of Company A, **I want to** request and update leave using my signed-in session, **so that** Company B cannot read or change our people, requests, balances, calendars, notifications, or audit, and someone holding only the public API key cannot operate without a tenant session.

<objective>
Expand the tracer to session-gated approve, reject, cancel, delete, and bulk status updates, and swap the operations hook transport.

Purpose: AUTHZ-01 for remaining leave mutations on existing UI.
Output: PATCH /api/leave-requests/[id], POST /api/leave-requests/bulk, hook fetch.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tenant-isolation-and-server-authz/02-RESEARCH.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md
@apps/web/src/app/api/leave-requests/route.ts
@apps/web/src/app/api/auth/invites/route.ts
@apps/web/src/hooks/use-leave-request-operations.ts
@apps/web/src/lib/bind-leave-actor.ts
@apps/web/src/lib/require-tenant-session.ts
@apps/web/src/lib/validation.ts
@packages/database/src/index.ts
</context>

<interfaces>
From apps/web/src/app/api/leave-requests/route.ts (02-02):
getServerSession(authOptions); tenantSessionRejectStatus; createTenantDatabaseService; 401/400/500 JSON shape.

From Next.js 14 App Router (apps/web next ^14.2.18):
PATCH(request, { params }: { params: { id: string } }) — params is sync, not Promise.

From packages/database/src/index.ts:
approveLeaveRequest(id, approverId, comments?)
rejectLeaveRequest(id, approverId, reason)
deleteLeaveRequest(id)
cancelLeaveRequest(id)
bulkUpdateLeaveRequests(ids, updates: Partial<LeaveRequest>)

From apps/web/src/hooks/use-leave-request-operations.ts:
mutationFns today call databaseService.*; keep onSuccess invalidation keys and toast.error/success from sonner; keep rowSelection state.
approve today passes userId from the hook props — server must ignore that and use session.
</interfaces>

## Artifacts this phase produces

- `PATCH` on `apps/web/src/app/api/leave-requests/[id]/route.ts`
- `POST` on `apps/web/src/app/api/leave-requests/bulk/route.ts`
- `leaveRequestPatchBodySchema`, `leaveRequestBulkBodySchema`

<tasks>

<task type="auto">
  <name>Session-gated PATCH leave request and bulk POST</name>
  <files>apps/web/src/app/api/leave-requests/[id]/route.ts, apps/web/src/app/api/leave-requests/bulk/route.ts, apps/web/src/lib/validation.ts</files>
  <read_first>apps/web/src/app/api/leave-requests/route.ts, apps/web/src/lib/validation.ts, apps/web/src/lib/bind-leave-actor.ts, packages/database/src/index.ts</read_first>
  <action>
    Add leaveRequestPatchBodySchema: action enum approve, reject, cancel, delete; optional comments and reason strings. Add leaveRequestBulkBodySchema: ids as uuid string array min 1; action enum approve, reject. No user_id, approver_id, or company_id fields.

    Create [id]/route.ts PATCH: same session gate as collection route; read params.id (Next 14 sync). Switch on action: approveLeaveRequest / rejectLeaveRequest / cancelLeaveRequest / deleteLeaveRequest. For approve and reject, bindLeaveApprover so approver_id is session.user.id. Reject requires reason. Unknown action 400. 500 on throw.

    Create bulk/route.ts POST: same gate. Build the updates object from action only: status approved or rejected; set approved_at or rejected_at to the current time on the server. Call bindLeaveApprover(updates, session.user.id) so approver_id is session.user.id, then bulkUpdateLeaveRequests(ids, boundUpdates). Ignore any client approver_id or user_id on the JSON body (schema already omits those fields). Same 401/400/500 patterns. Do not add Server Actions.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - [id]/route.ts exports PATCH and calls getServerSession and bindLeaveApprover for approve/reject
    - bulk/route.ts exports POST, calls getServerSession, and calls bindLeaveApprover before bulkUpdateLeaveRequests
    - leaveRequestPatchBodySchema and leaveRequestBulkBodySchema are exported
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Leave write APIs besides create are session-gated.</done>
</task>

<task type="auto">
  <name>Swap leave operations hook to BFF fetch</name>
  <files>apps/web/src/hooks/use-leave-request-operations.ts</files>
  <read_first>apps/web/src/hooks/use-leave-request-operations.ts, apps/web/src/hooks/use-dashboard-data.ts</read_first>
  <action>
    Keep use client, queryClient invalidation keys, sonner toasts, and rowSelection. Replace every databaseService mutationFn with fetch to /api/leave-requests/${id} PATCH or /api/leave-requests/bulk POST, credentials include, JSON body with action (and comments/reason/ids as needed). Do not send approverId or userId in JSON. Check res.ok then json. Remove useDatabaseService if unused. Do not restyle dialogs.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - use-leave-request-operations.ts contains /api/leave-requests and credentials
    - use-leave-request-operations.ts does not call databaseService.approveLeaveRequest or databaseService.bulkUpdateLeaveRequests
    - useDatabaseService is unused or removed from this file
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Approve/reject/cancel/delete/bulk on existing UI go through the BFF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PATCH/POST leave BFF | Untrusted action, ids, comments; actor is the session |
| Bulk ids array | Client-chosen UUIDs; RLS (02-06) must still deny other companies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | PATCH /api/leave-requests/[id] | high | mitigate | bindLeaveApprover from session; Zod action enum |
| T-02-09 | Tampering | POST /api/leave-requests/bulk | high | mitigate | Session gate; bindLeaveApprover so approver_id is session.user.id; ids are UUID strings; RLS in 02-06 denies cross-tenant ids |
| T-02-10 | Spoofing | use-leave-request-operations.ts | medium | mitigate | credentials include; no minted JWT in the browser |
| T-01-06 | Information Disclosure | open RLS | high | mitigate | Closed in 02-06; this plan does not add unrestricted policies |
| T-02-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npm test --workspace=@timeoff/web green. Operations hook has no remaining databaseService leave mutations.
<human-check>
As a manager on the same company, approve, reject, cancel, and bulk-select from the existing dialogs. Network tab shows /api/leave-requests/* not PostgREST leave_requests.
</human-check>
</verification>

<success_criteria>
Signed-in approve/reject/cancel/delete/bulk still work on shipped UI and run on the server with the session actor.
</success_criteria>

<output>
Create `.planning/phases/02-tenant-isolation-and-server-authz/02-03-SUMMARY.md` when done
</output>
