---
phase: 02-tenant-isolation-and-server-authz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/require-tenant-session.ts
  - apps/web/src/lib/require-tenant-session.test.ts
  - apps/web/src/lib/bind-leave-actor.ts
  - apps/web/src/lib/bind-leave-actor.test.ts
  - apps/web/src/lib/supabase-jwt.ts
  - apps/web/src/lib/supabase-jwt.test.ts
autonomous: true
requirements:
  - AUTHZ-01
  - AUTHZ-02
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tenantSessionRejectStatus returns 401 when userId or companyId is missing or empty; otherwise null (AUTHZ-01)."
    - "bindLeaveCreateActor always sets user_id from the session id argument; a body user_id is ignored (AUTHZ-01)."
    - "bindLeaveApprover always sets approver_id from the session id argument; a body approver_id is ignored (AUTHZ-01)."
    - "mintTenantAccessToken payload has role authenticated, sub equal to userId, and company_id equal to companyId (AUTHZ-02)."
    - "apps/web package.json test script still runs all Phase 1 node:test files plus the three new Wave 0 files; no Vitest (AUTHZ-01)."
    - "Flagged assumption TENANT-04 concurrency: isolation is per-request JWT plus request-scoped factory in later plans, not a process-wide tenant id."
    - "Backstop AUTHZ-01: missing session fields fail closed as 401 before any database call in later BFF routes."
    - "Backstop AUTHZ-02: company_id is a top-level JWT claim minted server-side, not user_metadata."
    - "Flagged assumption AUTHZ-03 concurrency: REVOKE lands in one later migration applied by db push; this plan does not change grants."
  artifacts:
    - path: "apps/web/src/lib/require-tenant-session.ts"
      provides: "tenantSessionRejectStatus"
      exports: ["tenantSessionRejectStatus"]
    - path: "apps/web/src/lib/bind-leave-actor.ts"
      provides: "bindLeaveCreateActor, bindLeaveApprover"
      exports: ["bindLeaveCreateActor", "bindLeaveApprover"]
    - path: "apps/web/src/lib/supabase-jwt.ts"
      provides: "mintTenantAccessToken"
      exports: ["mintTenantAccessToken"]
    - path: "apps/web/package.json"
      provides: "jose 4.15.9 direct dependency and extended test script"
      contains: "jose"
  key_links:
    - from: "apps/web/src/lib/require-tenant-session.ts"
      to: "apps/web/src/app/api/leave-requests/route.ts"
      via: "02-02 route will map getServerSession then tenantSessionRejectStatus"
      pattern: "tenantSessionRejectStatus"
    - from: "apps/web/src/lib/supabase-jwt.ts"
      to: "apps/web/src/lib/tenant-supabase.ts"
      via: "02-02 will mint HS256 for PostgREST accessToken"
      pattern: "mintTenantAccessToken"
  prohibitions:
    - statement: "Do not add owner to the users.role CHECK; ownership stays companies.owner_id."
    - statement: "Do not finish the stub /(admin)/users page."
    - statement: "Do not add new screens or restyle dashboard, request form, approve dialogs, or calendar chrome."
    - statement: "Do not install jose 6.x, jsonwebtoken, Vitest, Jest, Playwright, or @supabase/ssr."
    - statement: "Do not use service_role for tenant leave/user/notification queries."
    - statement: "Do not put the minted PostgREST JWT in a cookie or a client module."
    - statement: "Do not add company_id columns to leave_requests, leave_balances, notifications, audit_logs, or calendar_events this phase."
    - statement: "Do not re-open tenant tables with unrestricted RLS predicates."
---

## Phase Goal

**As a** signed-in employee of Company A, **I want to** request and update leave using my signed-in session, **so that** Company B cannot read or change our people, requests, balances, calendars, notifications, or audit, and someone holding only the public API key cannot operate without a tenant session.

<objective>
Wave 0 Nyquist helpers for AUTHZ-01 session fail-closed and actor bind, and AUTHZ-02 JWT claim shape, plus pin jose@4.15.9 so later tracer and RLS plans have green unit tests.

Purpose: 02-VALIDATION.md Wave 0 unit files must exist before BFF and RLS work. Pure functions plus JWT mint; no new routes or tables.
Output: tenantSessionRejectStatus, bindLeaveCreateActor, bindLeaveApprover, mintTenantAccessToken, jose pin, extended web test script.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-RESEARCH.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-VALIDATION.md
@apps/web/src/lib/invite-auth.ts
@apps/web/src/lib/invite-auth.test.ts
@apps/web/src/lib/invite-token.ts
@apps/web/src/lib/invite-token.test.ts
@apps/web/src/lib/create-company-rpc.ts
@apps/web/src/lib/create-company-rpc.test.ts
@apps/web/package.json
</context>

<interfaces>
From apps/web/src/lib/invite-auth.ts:
inviteOwnerRejectStatus(sessionUserId, ownerId): 401 | 403 | null
File-level JSDoc; named export; import leaf modules with explicit .ts for node:test.

From 02-RESEARCH.md Pattern 1 / 02-PATTERNS.md:
tenantSessionRejectStatus(userId: string | undefined | null, companyId: string | undefined | null): 401 | null
401 when userId or companyId is missing or empty. Do not copy owner-only 403.

From 02-RESEARCH.md bind snippet / 02-PATTERNS.md bind-leave-actor:
bindLeaveCreateActor(body: { user_id?: string } & Record<string, unknown>, sessionUserId: string): omit body user_id, set user_id to sessionUserId
bindLeaveApprover(body: { approver_id?: string } & Record<string, unknown>, sessionUserId: string): set approver_id to sessionUserId

From 02-RESEARCH.md mintTenantAccessToken:
mintTenantAccessToken({ userId, companyId, jwtSecret }): Promise<string>
SignJWT claims role authenticated and company_id; setSubject userId; alg HS256; exp 5m.
Use jose@4.15.9 only. Secret is SUPABASE_JWT_SECRET in later plans, not NEXTAUTH_SECRET.
</interfaces>

## Artifacts this phase produces

- `tenantSessionRejectStatus` in `apps/web/src/lib/require-tenant-session.ts`
- `bindLeaveCreateActor`, `bindLeaveApprover` in `apps/web/src/lib/bind-leave-actor.ts`
- `mintTenantAccessToken` in `apps/web/src/lib/supabase-jwt.ts`
- direct dependency `jose` exact `4.15.9` on `@timeoff/web`
- test files `require-tenant-session.test.ts`, `bind-leave-actor.test.ts`, `supabase-jwt.test.ts` appended to the web `test` script

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0: tenant session reject status</name>
  <files>apps/web/src/lib/require-tenant-session.ts, apps/web/src/lib/require-tenant-session.test.ts, apps/web/package.json</files>
  <read_first>apps/web/src/lib/invite-auth.ts, apps/web/src/lib/invite-auth.test.ts, apps/web/package.json, .planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md, .planning/phases/02-tenant-isolation-and-server-authz/02-VALIDATION.md</read_first>
  <behavior>
    - tenantSessionRejectStatus returns 401 when userId is undefined
    - tenantSessionRejectStatus returns 401 when userId is empty string
    - tenantSessionRejectStatus returns 401 when companyId is undefined or empty
    - tenantSessionRejectStatus returns null when both userId and companyId are non-empty
    - npm test --workspace=@timeoff/web includes require-tenant-session.test.ts
  </behavior>
  <action>
    Create require-tenant-session.ts as a pure mapper like invite-auth.ts (file-level JSDoc). Export tenantSessionRejectStatus(userId, companyId) returning 401 when either id is missing or empty, otherwise null. Do not take owner_id and do not return 403. Do not call getServerSession in this file this plan (02-02 route wrapper will). Import leaf modules with .ts if this file later imports siblings so Node 26 strip-types resolves them.

    Create require-tenant-session.test.ts with node:test and node:assert/strict, import ./require-tenant-session.ts. Append src/lib/require-tenant-session.test.ts to apps/web scripts.test. Keep every existing Phase 1 test filename. Do not add Vitest, tsx, or Jest.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - tenantSessionRejectStatus is exported from require-tenant-session.ts
    - apps/web package.json scripts.test includes require-tenant-session.test.ts and invite-token.test.ts
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Session fail-closed helper is unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: bind leave actor from session</name>
  <files>apps/web/src/lib/bind-leave-actor.ts, apps/web/src/lib/bind-leave-actor.test.ts, apps/web/package.json</files>
  <read_first>apps/web/src/lib/create-company-rpc.ts, apps/web/src/lib/create-company-rpc.test.ts, apps/web/src/lib/validation.ts, .planning/phases/02-tenant-isolation-and-server-authz/02-RESEARCH.md</read_first>
  <behavior>
    - bindLeaveCreateActor with body user_id set to another uuid returns user_id equal to sessionUserId
    - bindLeaveCreateActor copies non-actor fields from the body unchanged
    - bindLeaveApprover with body approver_id set to another uuid returns approver_id equal to sessionUserId
  </behavior>
  <action>
    Create bind-leave-actor.ts with named exports bindLeaveCreateActor and bindLeaveApprover (pure mappers like buildCreateCompanyWithOwnerArgs). Overwrite user_id / approver_id from the sessionUserId argument. Ignore body copies of those fields (do not 400 in this helper; routes may 400 later if they want). Do not read company_id from the body.

    Tests in bind-leave-actor.test.ts: object in / object out. Append the test file to the web test script. Per AUTHZ-01: later POST/PATCH must call these before IDatabaseService writes.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - bindLeaveCreateActor and bindLeaveApprover are exported
    - a fixture body user_id different from sessionUserId does not appear as the returned user_id
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Actor bind helpers ignore client-supplied user_id and approver_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: mint tenant access token with jose 4.15.9</name>
  <files>apps/web/src/lib/supabase-jwt.ts, apps/web/src/lib/supabase-jwt.test.ts, apps/web/package.json</files>
  <read_first>apps/web/src/lib/invite-token.ts, apps/web/src/lib/invite-token.test.ts, .planning/phases/02-tenant-isolation-and-server-authz/02-RESEARCH.md, .planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md</read_first>
  <behavior>
    - mintTenantAccessToken returns a JWT string with three segments
    - decodeJwt of that token has role authenticated, sub equal to input userId, company_id equal to input companyId
    - apps/web dependencies.jose is the exact string 4.15.9
  </behavior>
  <action>
    From repo root run npm install jose@4.15.9 --workspace=@timeoff/web so @timeoff/web depends on exact 4.15.9 (Package Legitimacy Audit: OK at 4.15.9; do not install unpinned jose). Do not add jsonwebtoken.

    Create supabase-jwt.ts with file-level JSDoc (never log the secret or the token). Export mintTenantAccessToken using SignJWT from jose: protected header alg HS256 typ JWT; claims role authenticated and company_id; setSubject userId; setIssuedAt; setExpirationTime 5m; sign with TextEncoder of jwtSecret. Do not use node:crypto HMAC.

    Create supabase-jwt.test.ts that mints with a test secret and decodeJwt from jose; assert payload keys. Do not call PostgREST. Append the test file to the web test script.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json dependencies.jose equals 4.15.9
    - mintTenantAccessToken is exported
    - grep of apps/web/package.json for vitest is 0
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>HS256 mint is unit-tested; jose is pinned to 4.15.9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit helpers (no network) | Session ids and JWT secret are test fixtures; no untrusted HTTP yet |
| npm install jose | Supply-chain install into @timeoff/web |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | bind-leave-actor.ts | high | mitigate | Overwrite user_id and approver_id from session argument; never trust body copies |
| T-02-02 | Information Disclosure | supabase-jwt.ts | high | mitigate | HS256 via jose SignJWT; never log jwtSecret or minted token; no NEXT_PUBLIC_ prefix |
| T-02-03 | Spoofing | require-tenant-session.ts | high | mitigate | Missing userId or companyId maps to 401 (fail closed) |
| T-02-SC | Tampering | npm install jose | high | mitigate | Pin jose@4.15.9 (audit OK); do not resolve jose 6.x |
</threat_model>

<verification>
npm test --workspace=@timeoff/web exits 0 including the three new Wave 0 files and all Phase 1 tests.
</verification>

<success_criteria>
Session reject, actor bind, and JWT claim shape are locked by node:test before any Route Handler or RLS migration.
</success_criteria>

<output>
Create `.planning/phases/02-tenant-isolation-and-server-authz/02-01-SUMMARY.md` when done
</output>
