---
phase: 02-tenant-isolation-and-server-authz
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/web/src/app/api/leave-requests/route.ts
  - apps/web/src/app/api/leave-balances/route.ts
  - apps/web/src/app/api/notifications/route.ts
  - apps/web/src/app/api/leave-policies/route.ts
  - apps/web/src/app/api/calendar/leave-requests/route.ts
  - apps/web/src/app/api/manager-team-stats/route.ts
autonomous: true
requirements:
  - AUTHZ-01
  - TENANT-04
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /api/leave-balances, /api/notifications, /api/leave-policies, /api/calendar/leave-requests, and /api/manager-team-stats call getServerSession and createTenantDatabaseService; list user_id comes from the session (AUTHZ-01, TENANT-04 read path)."
    - "GET /api/leave-requests branches own vs team vs all from session.user.role; an employee cannot escalate to all via query or body (AUTHZ-01)."
    - "GET /api/manager-team-stats returns 403 when session.user.role is employee (AUTHZ-01)."
    - "Flagged assumption TENANT-04 concurrency: each GET mints its own JWT; overlapping handler calls do not share a server client."
    - "Backstop AUTHZ-01: unread mark optional PATCH on notifications uses session, not a body user id."
    - "Backstop AUTHZ-02: isolation of these reads is enforced by RLS in 02-06; this plan adds the tenant JWT BFF endpoints 02-07 will fetch."
    - "Flagged assumption AUTHZ-03 concurrency: route handlers use createTenantDatabaseService, not the browser anon client."
  artifacts:
    - path: "apps/web/src/app/api/leave-balances/route.ts"
      provides: "GET balances for session user"
      exports: ["GET"]
    - path: "apps/web/src/app/api/notifications/route.ts"
      provides: "GET notifications"
      exports: ["GET"]
    - path: "apps/web/src/app/api/leave-policies/route.ts"
      provides: "GET catalog"
      exports: ["GET"]
    - path: "apps/web/src/app/api/calendar/leave-requests/route.ts"
      provides: "GET calendar leave lists"
      exports: ["GET"]
    - path: "apps/web/src/app/api/manager-team-stats/route.ts"
      provides: "GET getManagerTeamStats for session user"
      exports: ["GET"]
  key_links:
    - from: "apps/web/src/app/api/leave-balances/route.ts"
      to: "apps/web/src/lib/tenant-supabase.ts"
      via: "createTenantDatabaseService after getServerSession"
      pattern: "createTenantDatabaseService"
    - from: "apps/web/src/app/api/manager-team-stats/route.ts"
      to: "apps/web/src/app/api/auth/invites/route.ts"
      via: "same getServerSession(authOptions) gate as invites GET"
      pattern: "getServerSession"
  prohibitions:
    - statement: "Do not add owner to the users.role CHECK; ownership stays companies.owner_id."
    - statement: "Do not finish the stub /(admin)/users page."
    - statement: "Do not add new screens or restyle dashboard, request form, or calendar chrome."
    - statement: "Do not install jose 6.x, jsonwebtoken, Vitest, Jest, Playwright, or @supabase/ssr."
    - statement: "Do not use service_role for tenant leave/user/notification queries."
    - statement: "Do not put the minted PostgREST JWT in a cookie or a client module."
    - statement: "Do not add company_id columns to child tables this phase."
    - statement: "Do not re-open tenant tables with unrestricted RLS predicates."
    - statement: "Do not import service-role-supabase from a use client module."
---

## Phase Goal

**As a** signed-in employee of Company A, **I want to** request and update leave using my signed-in session, **so that** Company B cannot read or change our people, requests, balances, calendars, notifications, or audit, and someone holding only the public API key cannot operate without a tenant session.

<objective>
Add session-gated GET BFF routes for remaining dashboard, calendar, policy, and manager-stats reads. Client fetch swap and provider/repository changes are 02-07 (same wave, no shared files).

Purpose: AUTHZ-01/TENANT-04 read APIs exist before 02-06 REVOKE. 02-07 wires shipped UI to these paths.
Output: GET balances/notifications/policies/calendar/manager-stats; extended GET /api/leave-requests scope from session role.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tenant-isolation-and-server-authz/02-RESEARCH.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md
@apps/web/src/app/api/leave-requests/route.ts
@apps/web/src/app/api/auth/invites/route.ts
@apps/web/src/hooks/use-dashboard-data.ts
</context>

<interfaces>
From apps/web/src/app/api/leave-requests/route.ts (02-02):
copy session gate + createTenantDatabaseService.

From apps/web/src/hooks/use-dashboard-data.ts:
query keys leaveBalance, recentRequests, teamLeaveRequests, teamStats, allLeaveRequests, notifications — keep these.
Role branch: isAdminOrHR getAllLeaveRequests; isManager getTeamLeaveRequests; else own. GET /api/leave-requests in 02-02 is own-only — extend that GET this plan with a query scope=own|team|all chosen from session.user.role the same way the hook does today. Do not trust a client-supplied scope that exceeds the session role (ignore body/query if it asks for all when role is employee).

From packages/database/src/index.ts:
getLeaveBalance(userId, year)
getNotificationsByUser(userId, limit)
getLeavePolicies()
getManagerTeamStats(managerId)
getLeaveRequestsByUser / getTeamLeaveRequests / getAllLeaveRequests

From 02-PATTERNS.md Authentication + invites GET:
manager-team-stats uses the same getServerSession(authOptions) gate as apps/web/src/app/api/auth/invites/route.ts GET (session cookie, 401 JSON Unauthorized, try/catch 500). 403 only when role is employee on team stats.
</interfaces>

## Artifacts this phase produces

- GET `/api/leave-balances`, `/api/notifications`, `/api/leave-policies`, `/api/calendar/leave-requests`, `/api/manager-team-stats`
- extended GET `/api/leave-requests` scope own|team|all from session role

<tasks>

<task type="auto">
  <name>GET BFF for balances, notifications, policies, and leave list scopes</name>
  <files>apps/web/src/app/api/leave-balances/route.ts, apps/web/src/app/api/notifications/route.ts, apps/web/src/app/api/leave-policies/route.ts, apps/web/src/app/api/leave-requests/route.ts</files>
  <read_first>apps/web/src/app/api/leave-requests/route.ts, apps/web/src/hooks/use-dashboard-data.ts, packages/database/src/index.ts</read_first>
  <action>
    Add GET handlers for leave-balances (getLeaveBalance(session.user.id, current year)), notifications (getNotificationsByUser(session.user.id, 5); optional PATCH markNotificationAsRead(id) with id from query/body but only if the row is loaded through the tenant client), leave-policies (getLeavePolicies). Same session gate and createTenantDatabaseService as 02-02.

    Extend GET /api/leave-requests: if session.user.role is admin or hr, getAllLeaveRequests; else if supervisor/admin/hr manager path, getTeamLeaveRequests(session.user.id, session.user.department); else getLeaveRequestsByUser(session.user.id). Do not accept a client scope that upgrades an employee to all. Keep POST from 02-02 unchanged.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - leave-balances, notifications, and leave-policies route.ts files export GET and call getServerSession
    - leave-requests GET still calls getServerSession and branches on session.user.role
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Dashboard read APIs exist on the BFF.</done>
</task>

<task type="auto">
  <name>Calendar and manager-stats GET BFF routes</name>
  <files>apps/web/src/app/api/calendar/leave-requests/route.ts, apps/web/src/app/api/manager-team-stats/route.ts</files>
  <read_first>apps/web/src/app/api/leave-requests/route.ts, apps/web/src/app/api/auth/invites/route.ts, .planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md</read_first>
  <action>
    GET /api/calendar/leave-requests: same session gate as 02-02 leave-requests; same role branching as dashboard leave lists (own vs team vs all) using session.user.role and session.user.id, not a client user id.

    GET /api/manager-team-stats: copy the invites GET Authentication pattern (getServerSession(authOptions), 401 Unauthorized JSON, try/catch 500). Then createTenantDatabaseService; getManagerTeamStats(session.user.id). Return 403 if session.user.role is employee. Do not accept a client manager id.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - calendar/leave-requests and manager-team-stats export GET and call getServerSession
    - manager-team-stats returns 403 for employee role
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Calendar and manager-stats read APIs exist on the BFF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET BFF | Session cookie; query params must not escalate role |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | GET /api/leave-requests scope | high | mitigate | Scope from session.user.role only; employee cannot request all |
| T-02-22 | Elevation of Privilege | GET /api/manager-team-stats | high | mitigate | Session gate plus 403 when role is employee; managerId from session.user.id |
| T-01-06 | Information Disclosure | open RLS | high | mitigate | Closed in 02-06; this plan does not add unrestricted policies |
| T-02-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npm test --workspace=@timeoff/web green. GET route files call getServerSession and createTenantDatabaseService.
<human-check>
Unauthenticated GET /api/leave-balances and /api/manager-team-stats return 401. Signed-in manager GET /api/manager-team-stats is 200. Client fetch swap is 02-07.
</human-check>
</verification>

<success_criteria>
Session-gated GET BFF routes exist for balances, notifications, policies, calendar leave lists, manager stats, and scoped leave lists.
</success_criteria>

<output>
Create `.planning/phases/02-tenant-isolation-and-server-authz/02-04-SUMMARY.md` when done
</output>
