---
phase: 02-tenant-isolation-and-server-authz
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/web/src/lib/service-role-supabase.ts
  - apps/web/src/lib/auth.ts
  - apps/web/src/app/api/auth/signup/route.ts
  - apps/web/src/app/api/auth/invites/route.ts
  - apps/web/src/app/api/auth/invites/accept/route.ts
  - apps/web/src/app/api/auth/invites/preview/route.ts
  - apps/web/src/app/api/test-connection/route.ts
autonomous: true
requirements:
  - AUTHZ-03
  - AUTHZ-01
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "auth.ts authorize, session callback, signup, and invite hash lookup use the server service-role client with persistSession false (AUTHZ-03 pre-session identity)."
    - "service-role-supabase.ts is never imported from a use client module (AUTHZ-03)."
    - "GET /api/test-connection does not return users rows; it may return env SET/NOT SET only, or 401 without a session (AUTHZ-03)."
    - "Identity still calls create_company_with_owner and accept_invite_with_employee; no third data client besides identity server client and tenant IDatabaseService (AUTHZ-01)."
    - "Flagged assumption TENANT-04 concurrency: service_role bypasses RLS; only identity routes may use it, never leave/notification BFF."
    - "Backstop AUTHZ-01: invite GET/POST still use getServerSession for owner gates; only the Postgres client swaps."
    - "Backstop AUTHZ-02: tenant BFF stays on minted authenticated JWT from 02-02."
    - "Flagged assumption AUTHZ-03 concurrency: this swap must land before 02-06 REVOKE so authorize SELECT on users does not fail mid-push."
  artifacts:
    - path: "apps/web/src/lib/service-role-supabase.ts"
      provides: "identitySupabase server client"
      exports: ["identitySupabase"]
    - path: "apps/web/src/app/api/test-connection/route.ts"
      provides: "GET without user row payload"
      exports: ["GET"]
  key_links:
    - from: "apps/web/src/lib/auth.ts"
      to: "apps/web/src/lib/service-role-supabase.ts"
      via: "identity client for authorize and session user re-read"
      pattern: "identitySupabase"
    - from: "apps/web/src/app/api/auth/signup/route.ts"
      to: "apps/web/src/lib/service-role-supabase.ts"
      via: "rpc create_company_with_owner"
      pattern: "identitySupabase"
  prohibitions:
    - statement: "Do not add owner to the users.role CHECK; ownership stays companies.owner_id."
    - statement: "Do not finish the stub /(admin)/users page."
    - statement: "Do not add new screens."
    - statement: "Do not install jose 6.x, jsonwebtoken, Vitest, Jest, Playwright, or @supabase/ssr."
    - statement: "Do not use service_role for tenant leave/user/notification queries on BFF routes."
    - statement: "Do not put SUPABASE_SERVICE_ROLE_KEY in a client module or NEXT_PUBLIC_ var."
    - statement: "Do not add company_id columns to child tables this phase."
    - statement: "Do not re-open tenant tables with unrestricted RLS predicates."
    - statement: "Do not invent a fourth Supabase client besides browser maps, tenant BFF, and identity."
---

## Phase Goal

**As a** signed-in employee of Company A, **I want to** request and update leave using my signed-in session, **so that** Company B cannot read or change our people, requests, balances, calendars, notifications, or audit, and someone holding only the public API key cannot operate without a tenant session.

<objective>
Switch pre-session identity (credentials authorize, session enrich, signup, invite preview/accept/create) onto a server-only service_role client and close unauthenticated user listing on test-connection, before 02-06 REVOKE from anon.

Purpose: AUTHZ-03 — after grants drop, identity must still SELECT users and run onboarding RPCs; the public test-connection path must not dump people.
Output: identitySupabase; auth/signup/invite imports swapped; test-connection without user rows.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tenant-isolation-and-server-authz/02-RESEARCH.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md
@apps/web/src/lib/supabase.ts
@apps/web/src/lib/env.ts
@apps/web/src/lib/auth.ts
@apps/web/src/app/api/auth/signup/route.ts
@apps/web/src/app/api/auth/invites/route.ts
@apps/web/src/app/api/auth/invites/accept/route.ts
@apps/web/src/app/api/auth/invites/preview/route.ts
@apps/web/src/app/api/test-connection/route.ts
</context>

<interfaces>
From apps/web/src/lib/supabase.ts:
createClient(url, NEXT_PUBLIC_SUPABASE_ANON_KEY, persistSession true) — keep mapUserFromDatabase and any map helpers here. Identity must stop using this client for queries.

From 02-PATTERNS.md service-role-supabase:
createClient(url, env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false, autoRefreshToken: false } })
Export name identitySupabase (named). File must not be imported from use client modules.

From apps/web/src/lib/auth.ts:
import { supabase, mapUserFromDatabase } from './supabase'
authorize and session callback SELECT users — switch supabase to identitySupabase; keep mapUserFromDatabase from supabase.ts (or a maps-only import).

From signup/invites routes:
import { supabase } from '@/lib/supabase' — switch the client import only; keep RPC argument helpers and getServerSession owner gate.
</interfaces>

## Artifacts this phase produces

- `identitySupabase` in `apps/web/src/lib/service-role-supabase.ts`
- identity routes using that client
- `GET /api/test-connection` without a users row payload

<tasks>

<task type="auto">
  <name>Service-role identity client for auth and signup</name>
  <precondition>apps/web/.env.local has SUPABASE_SERVICE_ROLE_KEY (required as of 02-02 env.ts).</precondition>
  <files>apps/web/src/lib/service-role-supabase.ts, apps/web/src/lib/auth.ts, apps/web/src/app/api/auth/signup/route.ts</files>
  <read_first>apps/web/src/lib/supabase.ts, apps/web/src/lib/env.ts, apps/web/src/lib/auth.ts, apps/web/src/app/api/auth/signup/route.ts</read_first>
  <action>
    Create service-role-supabase.ts exporting identitySupabase via createClient(url, env.SUPABASE_SERVICE_ROLE_KEY, persistSession false, autoRefreshToken false). File-level JSDoc: identity only; never import from use client.

    auth.ts: import identitySupabase for all from('users') / rpc calls; keep mapUserFromDatabase from ./supabase. Do not mint tenant JWT in authorize.

    signup/route.ts: use identitySupabase for rpc create_company_with_owner and any user lookup. Keep mapUserFromDatabase from supabase.ts if still needed. Do not add IDatabaseService methods.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - identitySupabase is exported from service-role-supabase.ts
    - auth.ts imports identitySupabase and does not query with the anon supabase client
    - signup/route.ts imports identitySupabase
    - service-role-supabase.ts is not a use client module
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Login and credentials signup use the server identity client.</done>
</task>

<task type="auto">
  <name>Invite routes use identity client</name>
  <files>apps/web/src/app/api/auth/invites/route.ts, apps/web/src/app/api/auth/invites/accept/route.ts, apps/web/src/app/api/auth/invites/preview/route.ts</files>
  <read_first>apps/web/src/app/api/auth/invites/route.ts, apps/web/src/app/api/auth/invites/accept/route.ts, apps/web/src/app/api/auth/invites/preview/route.ts, apps/web/src/lib/service-role-supabase.ts</read_first>
  <action>
    Replace @/lib/supabase client imports with identitySupabase from service-role-supabase. Keep mapUserFromDatabase from supabase.ts on accept if used. Keep getServerSession owner gate, hashInviteTokenHex lookup, and RPC helpers. Do not move invite writes onto IDatabaseService.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - invites/route.ts, accept/route.ts, and preview/route.ts import identitySupabase
    - those files still do not import tenant-supabase
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Invite preview/create/accept survive anon REVOKE in 02-06.</done>
</task>

<task type="auto">
  <name>Close test-connection user listing</name>
  <files>apps/web/src/app/api/test-connection/route.ts</files>
  <read_first>apps/web/src/app/api/test-connection/route.ts, apps/web/src/lib/require-tenant-session.ts</read_first>
  <action>
    Remove the from('users') select of id/email/names. Keep environment url/key SET versus NOT SET probes (do not print values). Do not return a users array or usersCount derived from rows. Optionally require getServerSession and 401 when missing; either way, no row payload. Keep 500 on unexpected throw with error instanceof Error ? message : Unknown error.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - test-connection/route.ts does not call from('users')
    - response JSON does not include a users array
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Unauthenticated diagnostics cannot list people.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server identity client | service_role bypasses RLS — must never ship to the browser |
| GET /api/test-connection | Previously unauthenticated user listing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Elevation of Privilege | service-role-supabase.ts | high | mitigate | Server-only file; persistSession false; no import from use client; not used by leave BFF |
| T-02-15 | Information Disclosure | GET /api/test-connection | high | mitigate | Stop selecting users rows; env SET/NOT SET only |
| T-02-16 | Information Disclosure | auth.ts session callback | medium | mitigate | service_role re-read of the signed-in email only; no tenant JWT mint here |
| T-01-06 | Information Disclosure | open RLS | high | mitigate | Closed in 02-06; this plan does not add unrestricted policies |
| T-02-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npm test --workspace=@timeoff/web green. Identity files import identitySupabase. test-connection has no from('users').
<human-check>
Credentials sign-in, signup create-company, and invite preview/accept still work against local Supabase after this plan (before 02-06 REVOKE). GET /api/test-connection does not include other users' emails.
</human-check>
</verification>

<success_criteria>
Pre-session identity uses service_role on the server only; public test-connection no longer lists users.
</success_criteria>

<output>
Create `.planning/phases/02-tenant-isolation-and-server-authz/02-05-SUMMARY.md` when done
</output>
