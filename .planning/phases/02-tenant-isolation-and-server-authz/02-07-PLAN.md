---
phase: 02-tenant-isolation-and-server-authz
plan: 07
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/web/src/hooks/use-dashboard-data.ts
  - apps/web/src/providers/database-provider.tsx
  - apps/web/src/components/dashboard/unified-calendar-view.tsx
  - apps/web/src/components/dashboard/leave-calendar-view.tsx
  - apps/web/src/components/dashboard/team-calendar-view.tsx
  - apps/web/src/components/leave-request-form.tsx
  - packages/database/src/modules/users/repository.ts
autonomous: true
requirements:
  - AUTHZ-01
  - TENANT-04
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Dashboard leave balance, recent/team/all requests, notifications, and manager team stats load via fetch to session-gated GET BFF routes with credentials include (AUTHZ-01, TENANT-04 read path)."
    - "Personal and team calendar queryFns and leave-policies catalog fetch the same BFF pattern; no new calendar chrome (TENANT-04, CAL deferred except tenant-scope transport)."
    - "UserRepository list/find selects used by the tenant BFF omit the password column (AUTHZ-03 leak close on domain reads)."
    - "DatabaseServiceProvider does not construct createDatabaseService from the browser anon client as the default domain path (AUTHZ-01)."
    - "Flagged assumption TENANT-04 concurrency: React Query may overlap fetches; each GET mints its own JWT on the server from 02-04."
    - "Backstop AUTHZ-01: unread mark optional PATCH on notifications uses session, not a body user id."
    - "Backstop AUTHZ-02: isolation of these reads is enforced by RLS in 02-06; this plan moves transport onto the tenant JWT client."
    - "Flagged assumption AUTHZ-03 concurrency: hooks must not keep a PostgREST fallback; after 02-06 REVOKE the old path would be empty/401."
  artifacts:
    - path: "apps/web/src/hooks/use-dashboard-data.ts"
      provides: "queryFns fetch GET /api/leave-balances, /api/leave-requests, /api/notifications, /api/manager-team-stats"
      contains: "/api/leave-balances"
    - path: "apps/web/src/providers/database-provider.tsx"
      provides: "no default browser createDatabaseService(anon)"
    - path: "packages/database/src/modules/users/repository.ts"
      provides: "findById, findAll, getTeamMembers select without password"
  key_links:
    - from: "apps/web/src/hooks/use-dashboard-data.ts"
      to: "apps/web/src/app/api/leave-balances/route.ts"
      via: "fetch GET credentials include"
      pattern: "/api/leave-balances"
    - from: "apps/web/src/components/leave-request-form.tsx"
      to: "apps/web/src/app/api/leave-policies/route.ts"
      via: "queryKey leave-policies fetch"
      pattern: "/api/leave-policies"
    - from: "packages/database/src/modules/users/repository.ts"
      to: "IDatabaseService getAllUsers / getTeamMembers"
      via: "select without password"
      pattern: "password"
  prohibitions:
    - statement: "Do not add owner to the users.role CHECK; ownership stays companies.owner_id."
    - statement: "Do not finish the stub /(admin)/users page."
    - statement: "Do not add new screens or restyle dashboard, request form, or calendar chrome."
    - statement: "Do not rename the existing leave-policies query key."
    - statement: "Do not install jose 6.x, jsonwebtoken, Vitest, Jest, Playwright, or @supabase/ssr."
    - statement: "Do not use service_role for tenant leave/user/notification queries."
    - statement: "Do not put the minted PostgREST JWT in a cookie or a client module."
    - statement: "Do not add company_id columns to child tables this phase."
    - statement: "Do not re-open tenant tables with unrestricted RLS predicates."
    - statement: "Do not import service-role-supabase from a use client module."
---

## Phase Goal

**As a** signed-in employee of Company A, **I want to** request and update leave using my signed-in session, **so that** Company B cannot read or change our people, requests, balances, calendars, notifications, or audit, and someone holding only the public API key cannot operate without a tenant session.

<objective>
Swap remaining dashboard, calendar, and policy reads onto the 02-04 session-gated GET BFF routes, stop the browser anon IDatabaseService as the domain path, and omit password from UserRepository domain selects.

Purpose: After 02-06 REVOKE, the UI must already fetch through the BFF or dashboards go empty. TENANT-04 read transport; RLS enforcement is 02-06. Split from 02-04 so route files and client files do not share one plan.
Output: hook and calendar fetch swaps; provider default stop; UserRepository selects without password.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tenant-isolation-and-server-authz/02-RESEARCH.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-PATTERNS.md
@apps/web/src/hooks/use-dashboard-data.ts
@apps/web/src/providers/database-provider.tsx
@apps/web/src/components/dashboard/unified-calendar-view.tsx
@apps/web/src/components/dashboard/leave-calendar-view.tsx
@apps/web/src/components/dashboard/team-calendar-view.tsx
@apps/web/src/components/leave-request-form.tsx
@packages/database/src/modules/users/repository.ts
</context>

<interfaces>
From apps/web/src/hooks/use-dashboard-data.ts (02-02 create mutation already fetches POST /api/leave-requests):
query keys leaveBalance, recentRequests, teamLeaveRequests, teamStats, allLeaveRequests, notifications — keep these.
Swap remaining queryFns to GET the 02-04 routes with credentials include. Role branching for which URL/query to hit stays on the server in 02-04 GET /api/leave-requests; the hook should not send a client scope that upgrades an employee.

From 02-04 routes (same wave, no file overlap; fetch by path string):
GET /api/leave-balances, /api/notifications, /api/leave-policies, /api/leave-requests, /api/calendar/leave-requests, /api/manager-team-stats

From packages/database/src/modules/users/repository.ts:
findById, findAll, getTeamMembers use select star today — change to an explicit column list matching User fields (id, email, first_name, last_name, avatar, department, team, role, manager_id, company_id, hire_date, is_active, timestamps). Omit password. Identity authorize in auth.ts keeps its own select including password via the 02-05 service-role client.
</interfaces>

## Artifacts this phase produces

- dashboard and calendar queryFns fetching `/api/*` with credentials
- DatabaseServiceProvider default path no longer constructs a browser domain client
- UserRepository domain selects without password

<tasks>

<task type="auto">
  <name>Dashboard queryFn swap and provider default stop</name>
  <files>apps/web/src/hooks/use-dashboard-data.ts, apps/web/src/providers/database-provider.tsx</files>
  <read_first>apps/web/src/hooks/use-dashboard-data.ts, apps/web/src/providers/database-provider.tsx, apps/web/src/app/api/leave-requests/route.ts</read_first>
  <action>
    Swap remaining use-dashboard-data queryFns (balance, recent, team, teamStats, allRequests, notifications) to fetch GET /api/leave-balances, /api/leave-requests, /api/manager-team-stats, /api/notifications with credentials include. Keep keys, adaptLeave* after json, enabled flags. Keep create mutation from 02-02. Remove useDatabaseService if unused.

    DatabaseServiceProvider: do not call createDatabaseService(supabase) as the default. If service prop is omitted, provide null so useDatabaseService throws. Do not import tenant-supabase or service-role-supabase here (use client).
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - use-dashboard-data.ts queryFns contain /api/ and credentials
    - use-dashboard-data.ts does not call databaseService.getLeaveBalance or databaseService.getAllLeaveRequests
    - database-provider.tsx default path does not call createDatabaseService(supabase)
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Dashboard reads use BFF; browser provider is not the domain client.</done>
</task>

<task type="auto">
  <name>Calendar views and policy form fetch</name>
  <files>apps/web/src/components/dashboard/unified-calendar-view.tsx, apps/web/src/components/dashboard/leave-calendar-view.tsx, apps/web/src/components/dashboard/team-calendar-view.tsx, apps/web/src/components/leave-request-form.tsx</files>
  <read_first>apps/web/src/components/dashboard/unified-calendar-view.tsx, apps/web/src/components/dashboard/leave-calendar-view.tsx, apps/web/src/components/leave-request-form.tsx</read_first>
  <action>
    unified-calendar-view and leave-calendar-view: keep query keys personalLeaveRequests / teamLeaveRequests; swap queryFn to fetch /api/calendar/leave-requests with credentials include. Remove useDatabaseService. team-calendar-view: remove the unused useDatabaseService import only; do not restyle.

    leave-request-form: keep queryKey leave-policies (do not rename); fetch GET /api/leave-policies with credentials include; remove useDatabaseService.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - unified-calendar-view.tsx and leave-calendar-view.tsx contain /api/calendar/leave-requests
    - leave-request-form.tsx contains /api/leave-policies and still uses queryKey leave-policies
    - team-calendar-view.tsx does not import useDatabaseService
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Calendar and policy catalog use BFF fetch.</done>
</task>

<task type="auto">
  <name>UserRepository domain select without password</name>
  <files>packages/database/src/modules/users/repository.ts</files>
  <read_first>packages/database/src/modules/users/repository.ts</read_first>
  <action>
    UserRepository findById, findAll, getTeamMembers: replace select star with an explicit column list of User fields; omit password. Keep try/catch and DatabaseUtils.handleDatabaseError. Do not change auth.ts authorize select this plan (02-05).
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - users/repository.ts findAll select string does not include password
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Domain user reads omit password.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET BFF | Session cookie; query params must not escalate role (server in 02-04) |
| Tenant BFF → users table | select must not return password hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Information Disclosure | UserRepository select | high | mitigate | Explicit columns; omit password on BFF paths |
| T-02-13 | Information Disclosure | database-provider.tsx | high | mitigate | Stop default browser createDatabaseService(anon) so hooks cannot silently reopen PostgREST |
| T-01-06 | Information Disclosure | open RLS | high | mitigate | Closed in 02-06; this plan does not add unrestricted policies |
| T-02-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
npm test --workspace=@timeoff/web green. Dashboard and calendar hooks fetch /api/* with credentials. UserRepository domain selects omit password.
<human-check>
Signed-in employee dashboard loads balances/requests/notifications without Network calls to /rest/v1/leave_requests. Personal and team calendar pages still render using /api/calendar/leave-requests.
</human-check>
</verification>

<success_criteria>
Shipped dashboard and calendar reads go through session-gated BFF; the browser anon client is no longer the domain path.
</success_criteria>

<output>
Create `.planning/phases/02-tenant-isolation-and-server-authz/02-07-SUMMARY.md` when done
</output>
