---
phase: 03-live-remaining-days
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/leave-balance-display.ts
  - apps/web/src/lib/leave-balance-display.test.ts
  - apps/web/package.json
  - packages/database/src/modules/leave-balances/plan-default-inserts.ts
  - packages/database/src/modules/leave-balances/plan-default-inserts.test.ts
  - packages/database/package.json
autonomous: true
requirements:
  - BAL-01
  - BAL-02
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "balancesForLeaveCard returns vacation, then sick, then personal when all three exist, and omits any other leave_type (BAL-01, D-12)."
    - "balancesForLeaveCard returns only the types present (partial list stays ordered) and returns [] for no rows (BAL-01, D-08, D-15)."
    - "planDefaultBalanceInserts copies total_allowance from the matching active policy default_allowance at plan time and sets used_days 0, remaining_days equal to that allowance, carried_over 0 (BAL-02, D-01, D-02, D-14)."
    - "planDefaultBalanceInserts emits only vacation, sick, and personal; skips a type with no active policy and skips a type that already has a row even when used_days is non-zero (BAL-02, D-03, D-07, D-15)."
    - "Every planned insert uses the userId argument (never a second user's id) and the year argument (BAL-02, D-04, D-06)."
    - "Flagged assumption BAL-01 unclassified (no SPEC): remaining-day card binds current-calendar-year rows for the signed-in user only; extra leave types and other users are not this card."
    - "Flagged assumption BAL-02 unclassified (no SPEC): default seed is insert-only for vacation/sick/personal when an active leave_policies row exists; unique (user_id, leave_type, year) conflict is ignored and never becomes an UPDATE of used_days."
    - "After load, zero rows (or fetch error) render Empty state body inside the still-visible Card. Do not hide the card. Do not paint 0-day bars without rows (D-08)."
    - "isLoading true shows the existing three-row pulse skeleton (h-4 + h-2 bg-gray-200, space-y-4). Title stays. No spinner, no mock numbers (D-09)."
    - "Failed GET: sonner Error state toast + same empty Card as empty (D-09). No mock 5/10 fallback. No inline red alert on the card."
    - "Typical volume: three rows (Vacation, Sick Leave, Personal) bound to live used_days, total_allowance, remaining_days. Seeded unused start is 0/{allowance} days and {allowance} days remaining."
    - "One or two of the three types present: render only those rows in vacation then sick then personal order. Missing types get no placeholder bar (D-12, D-15)."
    - "At most three rows this phase (space-y-6). Extra GET types are not rendered (D-12). No inner scroll on the card."
    - "Zero: empty copy. One: single type row. Two or three: stacked rows. Never four+ on this card."
    - "Empty state body wraps centered in py-8. Card width follows the Overview left column (lg:col-span-3). No horizontal clip."
    - "Copy is the fixed sentence in Empty state body. Do not truncate or ellipsis."
    - "Existing flex justify-between on each row. Numbers stay on one line; do not wrap the used/allowance pair onto the type name."
    - "Type names are the three fixed labels. Day counts are integers from the row. Do not ellipsis type names. Do not format with commas this phase."
  artifacts:
    - path: "apps/web/src/lib/leave-balance-display.ts"
      provides: "balancesForLeaveCard"
      exports: ["balancesForLeaveCard"]
    - path: "packages/database/src/modules/leave-balances/plan-default-inserts.ts"
      provides: "planDefaultBalanceInserts"
      exports: ["planDefaultBalanceInserts"]
    - path: "apps/web/package.json"
      provides: "leave-balance-display.test.ts on the web test script"
      contains: "leave-balance-display.test.ts"
    - path: "packages/database/package.json"
      provides: "node:test script for plan-default-inserts.test.ts"
      contains: "plan-default-inserts.test.ts"
  key_links:
    - from: "apps/web/src/lib/leave-balance-display.ts"
      to: "apps/web/src/components/dashboard/leave-balance-card.tsx"
      via: "03-04 will call balancesForLeaveCard before map"
      pattern: "balancesForLeaveCard"
    - from: "packages/database/src/modules/leave-balances/plan-default-inserts.ts"
      to: "packages/database/src/modules/leave-balances/service.ts"
      via: "03-02 ensureDefaultBalances will call planDefaultBalanceInserts"
      pattern: "planDefaultBalanceInserts"
  prohibitions:
    - statement: "Do not restyle the dashboard Leave Balance card (spacing, colors, Progress, dots, or type labels)."
    - statement: "Do not fall back to mock 5/10 remaining-day numbers when GET is empty or fails."
    - statement: "Do not UPDATE existing leave_balances used_days or remaining_days this phase."
    - statement: "Do not seed maternity, paternity, bereavement, or unpaid balance rows this phase."
    - statement: "Do not use service_role for GET /api/leave-balances."
    - statement: "Do not drop Phase 2 GRANT/REVOKE or SECURITY DEFINER SET search_path = public on onboarding RPCs."
    - statement: "Do not add company_id columns to leave_balances or other child tables."
    - statement: "Do not deduct remaining days on approve, reject, or cancel (Phase 4)."
---

## Phase Goal

**As a** signed-in employee, **I want to** see my remaining vacation, sick, and personal days from live leave_balances on the dashboard, **so that** remaining days are real rows after signup or invite instead of empty mock cards.

<objective>
Wave 0 Nyquist helpers for BAL-01 card type order/filter and BAL-02 insert-only default seed planning. D-01: D-02: D-03: D-04: D-05: D-06: D-07: D-08: D-09: D-10: D-11: D-12: D-13: D-14: D-15: so later tracer and RPC plans have green unit tests.

Purpose: No VALIDATION.md this phase; Dimension 8 seed + card binding tests live here before GET self-heal and RPC CREATE OR REPLACE.
Output: balancesForLeaveCard, planDefaultBalanceInserts, node:test files, web and database test scripts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-remaining-days/03-CONTEXT.md
@.planning/phases/03-live-remaining-days/03-PATTERNS.md
@.planning/phases/03-live-remaining-days/03-UI-SPEC.md
@apps/web/src/lib/create-company-rpc.ts
@apps/web/src/lib/create-company-rpc.test.ts
@apps/web/package.json
@packages/database/package.json
@packages/database/src/modules/leave-balances/types.ts
@packages/database/src/modules/leave-policies/types.ts
</context>

<interfaces>
From 03-PATTERNS.md leave-balance-display:
balancesForLeaveCard(rows: LeaveBalance[]): LeaveBalance[]
CARD_TYPES order vacation, sick, personal. map find filter non-null. Compare leave_type string values (LeaveType.VACATION is 'vacation'), not enum keys.

From 03-CONTEXT.md D-01 D-02 D-03 D-04 D-07 D-14 D-15:
planDefaultBalanceInserts(existing, policies, userId, year) returns insert payloads only.
Existing row shape needs leave_type. Policy row shape needs leave_type, default_allowance, is_active, name.
Multiple active policies for one type: sort by name ascending, take the first (matches LeavePolicyRepository.findByLeaveType order name then policies[0]).
Inactive or missing policy: omit that type. Existing (user_id, leave_type, year) equivalent: omit that type even if used_days is not 0.
Do not return update payloads.

From apps/web/src/lib/create-company-rpc.test.ts:
node:assert/strict, node:test describe/it, import leaf with explicit .ts suffix for Node 26 strip-types.
</interfaces>

## Artifacts this phase produces

- `balancesForLeaveCard` in `apps/web/src/lib/leave-balance-display.ts`
- `planDefaultBalanceInserts` in `packages/database/src/modules/leave-balances/plan-default-inserts.ts`
- test files registered on `@timeoff/web` and `@timeoff/database` test scripts

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0: card type order and filter helper</name>
  <files>apps/web/src/lib/leave-balance-display.ts, apps/web/src/lib/leave-balance-display.test.ts, apps/web/package.json</files>
  <read_first>apps/web/src/lib/create-company-rpc.test.ts, apps/web/package.json, apps/web/src/components/dashboard/leave-balance-card.tsx, .planning/phases/03-live-remaining-days/03-PATTERNS.md, .planning/phases/03-live-remaining-days/03-UI-SPEC.md</read_first>
  <behavior>
    - balancesForLeaveCard on [personal, vacation, sick] returns vacation then sick then personal (D-12)
    - balancesForLeaveCard drops a maternity row and any type not vacation, sick, or personal (D-12)
    - balancesForLeaveCard on [sick] returns a one-element sick array (partial, D-15)
    - balancesForLeaveCard on [] returns []
  </behavior>
  <action>
    Create leave-balance-display.ts as a pure mapper (file-level JSDoc). Export balancesForLeaveCard(rows) that returns the vacation, sick, then personal rows that exist in rows, in that order per D-12. Use string leave_type values vacation, sick, personal. Do not invent 0-allowance placeholders for missing types. Do not restyle and do not import React.

    Create leave-balance-display.test.ts with node:test and node:assert/strict, import ./leave-balance-display.ts. Cover order, extra-type drop, partial, and empty. Append src/lib/leave-balance-display.test.ts to apps/web scripts.test. Keep every existing test filename. Do not add Vitest, tsx, or Jest.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - balancesForLeaveCard is exported from leave-balance-display.ts
    - apps/web package.json scripts.test includes leave-balance-display.test.ts and leave-list-scope.test.ts
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Card order/filter helper is unit-tested (BAL-01, D-12).</done>
</task>

<task type="auto" tdd="true">
  <name>Wave 0: insert-only default seed planner</name>
  <reversibility rating="costly">Seed numbers come from leave_policies.default_allowance at insert time (D-01); changing catalog later leaves existing rows unless a backfill is added.</reversibility>
  <files>packages/database/src/modules/leave-balances/plan-default-inserts.ts, packages/database/src/modules/leave-balances/plan-default-inserts.test.ts, packages/database/package.json</files>
  <read_first>packages/database/src/modules/leave-balances/types.ts, packages/database/src/modules/leave-policies/types.ts, packages/database/src/modules/leave-policies/repository.ts, packages/database/package.json, .planning/phases/03-live-remaining-days/03-CONTEXT.md</read_first>
  <behavior>
    - Given no existing rows and active policies vacation 20, sick 10, personal 5, returns three inserts with those total_allowance values, used_days 0, remaining_days equal to allowance, carried_over 0, year and user_id from arguments (D-01, D-02, D-04, D-06, D-14)
    - Given an existing vacation row with used_days 5, returns only sick and personal inserts (D-07)
    - Given no active personal policy, returns only vacation and sick (D-15)
    - Never includes a type other than vacation, sick, or personal (D-03)
    - When two active policies share sick, uses default_allowance from the name-sorted first policy
  </behavior>
  <action>
    Create plan-default-inserts.ts with no database client import (pure function). Export planDefaultBalanceInserts(existing, policies, userId, year). For each of vacation, sick, personal per D-03: if existing already has that leave_type, skip (D-07). If no policy with is_active true (treat missing is_active as inactive unless the fixture sets true) for that type, skip (D-15). Otherwise push one insert: user_id from the userId argument (D-06), leave_type, total_allowance from that policy default_allowance at plan time (D-01, D-14), used_days 0, remaining_days equal to total_allowance, carried_over 0 (D-02), year from the year argument (D-04). When several active policies share a type, sort by name and take index 0. Do not return update fields or call any repository.

    Create plan-default-inserts.test.ts with node:test, import ./plan-default-inserts.ts. Add a test script on packages/database/package.json that runs node --test --experimental-strip-types on that test file only. Do not add Vitest.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/database</automated>
  </verify>
  <acceptance_criteria>
    - planDefaultBalanceInserts is exported
    - packages/database package.json scripts.test includes plan-default-inserts.test.ts
    - a fixture existing vacation row with used_days 5 produces no vacation insert
    - npm test --workspace=@timeoff/database exits 0
  </acceptance_criteria>
  <done>Insert-only seed planner is unit-tested (BAL-02, D-01 through D-07, D-14, D-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit helpers (no network) | userId and policy allowances are test fixtures; no HTTP |
| npm test scripts | No new runtime packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | planDefaultBalanceInserts | high | mitigate | Every insert user_id is the function argument; no company-wide user list (D-06) |
| T-03-02 | Tampering | planDefaultBalanceInserts | high | mitigate | Planner returns inserts only; existing used_days is never in an update payload (D-07) |
| T-03-04 | Information Disclosure | leave-balance-display.ts | medium | mitigate | Helper does not read users or password; it only reorders LeaveBalance rows |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new package-manager installs this plan |

</threat_model>

<verification>
npm test --workspace=@timeoff/web exits 0 including leave-balance-display.test.ts and all prior web tests. npm test --workspace=@timeoff/database exits 0. GET self-heal and RPC SQL land in 03-02 and 03-03.
</verification>

<success_criteria>
Card order/filter and insert-only seed planning are locked by node:test before any Route Handler or CREATE OR REPLACE.
</success_criteria>

<output>
Create `.planning/phases/03-live-remaining-days/03-01-SUMMARY.md` when done
</output>
