---
phase: 03-live-remaining-days
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - apps/web/src/components/dashboard/leave-balance-card.tsx
  - apps/web/src/hooks/use-dashboard-data.ts
autonomous: true
requirements:
  - BAL-01
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "After load, zero rows (or fetch error) render Empty state body inside the still-visible Card. Do not hide the card. Do not paint 0-day bars without rows (D-08)."
    - "isLoading true shows the existing three-row pulse skeleton (h-4 + h-2 bg-gray-200, space-y-4). Title stays. No spinner, no mock numbers (D-09)."
    - "Failed GET: sonner Error state toast + same empty Card as empty (D-09). No mock 5/10 fallback. No inline red alert on the card."
    - "Typical volume: three rows (Vacation, Sick Leave, Personal) bound to live used_days, total_allowance, remaining_days. Seeded unused start is 0/{allowance} days and {allowance} days remaining."
    - "One or two of the three types present: render only those rows in vacation then sick then personal order. Missing types get no placeholder bar (D-12, D-15)."
    - "At most three rows this phase (space-y-6). Extra GET types are not rendered (D-12). No inner scroll on the card."
    - "Zero: empty copy. One: single type row. Two or three: stacked rows. Never four+ on this card."
    - "Empty state body wraps centered in py-8. Card width follows the Overview left column (lg:col-span-3). No horizontal clip."
    - "Copy is the fixed sentence in Empty state body. Do not truncate or ellipsis."
    - "Existing flex justify-between on each row. Numbers stay on one line; do not wrap the used/allowance pair onto the type name."
    - "Type names are the three fixed labels. Day counts are integers from the row. Do not ellipsis type names. Do not format with commas this phase."
    - "balancesForLeaveCard is the only mapper the card uses before rendering type rows (BAL-01, D-12)."
    - "leaveBalance query onError toasts via existing sonner; query key stays ['leaveBalance', user.id] (BAL-01, D-09)."
    - "Flagged assumption BAL-01 unclassified (no SPEC): remaining-day card binds current-calendar-year rows for the signed-in user only; extra leave types and other users are not this card."
    - "Flagged assumption BAL-02 unclassified (no SPEC): default seed is insert-only for vacation/sick/personal when an active leave_policies row exists; unique (user_id, leave_type, year) conflict is ignored and never becomes an UPDATE of used_days."
  artifacts:
    - path: "apps/web/src/components/dashboard/leave-balance-card.tsx"
      provides: "empty branch plus ordered populated rows"
      contains: "balancesForLeaveCard"
    - path: "apps/web/src/hooks/use-dashboard-data.ts"
      provides: "leaveBalance query onError toast"
      contains: "Failed to load leave balances"
  key_links:
    - from: "apps/web/src/hooks/use-dashboard-data.ts"
      to: "apps/web/src/app/api/leave-balances/route.ts"
      via: "fetch GET /api/leave-balances credentials include"
      pattern: "/api/leave-balances"
    - from: "apps/web/src/components/dashboard/leave-balance-card.tsx"
      to: "apps/web/src/lib/leave-balance-display.ts"
      via: "balancesForLeaveCard(leaveBalance) before map"
      pattern: "balancesForLeaveCard"
    - from: "apps/web/src/hooks/use-dashboard-data.ts"
      to: "apps/web/src/providers/session-provider.tsx"
      via: "toast.error uses existing Sonner; no second toaster"
      pattern: "toast.error"
  prohibitions:
    - statement: "Do not restyle the dashboard Leave Balance card (spacing, colors, Progress, dots, or type labels)."
    - statement: "Do not fall back to mock 5/10 remaining-day numbers when GET is empty or fails."
    - statement: "Do not UPDATE existing leave_balances used_days or remaining_days this phase."
    - statement: "Do not seed maternity, paternity, bereavement, or unpaid balance rows this phase."
    - statement: "Do not use service_role for GET /api/leave-balances."
    - statement: "Do not drop Phase 2 GRANT/REVOKE or SECURITY DEFINER SET search_path = public on onboarding RPCs."
    - statement: "Do not add company_id columns to leave_balances or other child tables."
    - statement: "Do not deduct remaining days on approve, reject, or cancel (Phase 4)."
---

## Phase Goal

**As a** signed-in employee, **I want to** see my remaining vacation, sick, and personal days from live leave_balances on the dashboard, **so that** remaining days are real rows after signup or invite instead of empty mock cards.

<objective>
Empty, fetch-error, partial, and type-order states per 03-UI-SPEC. D-01: D-02: D-03: D-04: D-05: D-06: D-07: D-08: D-09: D-10: D-11: D-12: D-13: D-14: D-15: on the existing Leave Balance card after the tracer bound live remaining_days.

Purpose: Happy-path bind is 03-02; this slice makes zero rows, GET failure, extra types, and one-or-two-type results truthful without restyling.
Output: uncommented empty branch, sonner onError on the leaveBalance query, balancesForLeaveCard in the card map.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live-remaining-days/03-UI-SPEC.md
@.planning/phases/03-live-remaining-days/03-PATTERNS.md
@.planning/phases/03-live-remaining-days/03-CONTEXT.md
@apps/web/src/components/dashboard/leave-balance-card.tsx
@apps/web/src/components/dashboard/recent-requests-card.tsx
@apps/web/src/hooks/use-dashboard-data.ts
@apps/web/src/hooks/use-leave-request-operations.ts
@apps/web/src/lib/leave-balance-display.ts
@apps/web/src/providers/session-provider.tsx
</context>

<interfaces>
From 03-UI-SPEC.md Binding and interaction contract plus Copywriting Contract:
Empty when !leaveBalance || leaveBalance.length === 0 after loading (and on fetch error). Title stays Leave Balance. Empty state body is the existing commented sentence (do not rewrite). Card stays visible. Zero remaining_days is populated, not empty.
Loading: keep existing three-row animate-pulse h-4 + h-2 bg-gray-200 space-y-4. No Skeleton primitive, no spinner.
Error: toast Failed to load leave balances via sonner (already the fetchSessionJson fallbackError). No inline destructive on the card. No retry button. Reuse session-provider Sonner.
Order: balancesForLeaveCard then map. Extra types not rendered. Partial: only present types, vacation then sick then personal. Progress still used_days/total_allowance. Remaining still remaining_days (D-11, already bound in 03-02).
Query key ['leaveBalance', user.id]. GET /api/leave-balances credentials include. Do not useDatabaseService for remaining days.

From use-leave-request-operations.ts:
onError: (error) => { toast.error(...) }
toast already imported from sonner in use-dashboard-data.ts.

From recent-requests-card.tsx empty branch: Card + CardHeader title + CardContent p.text-muted-foreground.text-center.py-8.
</interfaces>

## Artifacts this phase produces

- Leave Balance card empty branch (D-08)
- leaveBalance query onError toast (D-09)
- balancesForLeaveCard wired in the populated map (D-12)

<tasks>

<task type="auto" tdd="true">
  <name>Empty card and fetch-error toast</name>
  <files>apps/web/src/components/dashboard/leave-balance-card.tsx, apps/web/src/hooks/use-dashboard-data.ts</files>
  <read_first>apps/web/src/components/dashboard/leave-balance-card.tsx, apps/web/src/components/dashboard/recent-requests-card.tsx, apps/web/src/hooks/use-dashboard-data.ts, apps/web/src/hooks/use-leave-request-operations.ts, apps/web/src/providers/session-provider.tsx, .planning/phases/03-live-remaining-days/03-UI-SPEC.md</read_first>
  <behavior>
    - When isLoading is false and leaveBalance is missing or length 0, the card still renders title Leave Balance and Empty state body (D-08)
    - When the leaveBalance query fails, onError calls toast.error with the existing fallbackError string (D-09)
    - After error, adaptLeaveBalances of undefined/[] still yields [] so the empty branch runs; isLoading is false
  </behavior>
  <action>
    Uncomment the empty branch already written in leave-balance-card.tsx (D-08). Keep Card chrome visible. Do not hide the card. Do not paint 0-day bars when there are no rows. Do not restore the in-card hardcoded fixture array. Keep the pulse skeleton for isLoading (D-09). A row with remaining_days 0 stays in the populated branch.

    On the leaveBalance useQuery in use-dashboard-data.ts add onError that toast.error uses the same string already passed as fallbackError to fetchSessionJson (D-09). Keep queryKey ['leaveBalance', user.id] and credentials include. Do not mount a second toaster. Do not add approve-query invalidation (Phase 4). Do not read remaining days through useDatabaseService.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - leave-balance-card.tsx empty branch is live (not commented) and includes Empty state body
    - use-dashboard-data.ts leaveBalance useQuery has onError and toast.error
    - use-dashboard-data.ts still fetches /api/leave-balances with credentials include
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Empty and fetch-error states match UI-SPEC without mock numbers (BAL-01, D-08, D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Display order, partial rows, extra types hidden</name>
  <files>apps/web/src/components/dashboard/leave-balance-card.tsx</files>
  <read_first>apps/web/src/lib/leave-balance-display.ts, apps/web/src/lib/leave-balance-display.test.ts, apps/web/src/components/dashboard/leave-balance-card.tsx, .planning/phases/03-live-remaining-days/03-UI-SPEC.md</read_first>
  <behavior>
    - Populated map iterates balancesForLeaveCard(leaveBalance), not the raw prop order (D-12)
    - Extra types in GET data are not rendered
    - One or two of vacation/sick/personal render only those rows in that order with no placeholder bars (D-15)
  </behavior>
  <action>
    Import balancesForLeaveCard from @/lib/leave-balance-display. After the empty check, map balancesForLeaveCard(leaveBalance) for populated rows (D-12). Keep display-name and color maps (including unused extra keys) but do not output those extra types. Keep remaining_days on the remaining label (D-11). Keep Progress as used_days/total_allowance. Keep space-y-6 / space-y-3 / flex justify-between / 12px dots. Do not restyle. Do not add the card to Requests or Calendar tabs. Do not edit dashboard-overview.tsx.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web</automated>
  </verify>
  <acceptance_criteria>
    - leave-balance-card.tsx imports and calls balancesForLeaveCard
    - populated map does not iterate a maternity-capable extra type list
    - leave-balance-display.test.ts still passes
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Card shows at most vacation, sick, personal in that order; partial lists have no placeholder bars (BAL-01, D-12, D-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/leave-balances | Session cookie; error payload is untrusted JSON (already gated by fetchSessionJson) |
| Card render | leaveBalance prop is data from BFF; extra types must not appear as a second product surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Information Disclosure | use-dashboard-data.ts | high | mitigate | Keep fetch credentials include to GET /api/leave-balances; do not construct a browser domain client for remaining days |
| T-03-01 | Elevation of Privilege | LeaveBalanceCard | medium | accept | Card is read-only; it cannot choose another user_id. Residual: displaying extra types is a product leak of catalog, mitigated by balancesForLeaveCard |
| T-03-04 | Information Disclosure | leaveBalance JSON | high | mitigate | Do not add a users embed or password to the dashboard leaveBalance mapping |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new package-manager installs this plan; do not npx shadcn add |

</threat_model>

<verification>
npm test --workspace=@timeoff/web exits 0 including leave-balance-display.test.ts. Card empty branch is uncommented. leaveBalance query toasts onError. Populated rows go through balancesForLeaveCard.
<human-check>
Overview Leave Balance: loading pulse, then live remaining_days. Empty copy when GET returns []. Failed GET: sonner toast plus empty copy, no 5/10 numbers. Extra types from GET do not appear.
</human-check>
</verification>

<success_criteria>
Empty, error, partial, and overflow type states match 03-UI-SPEC; dashboard chrome is unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-live-remaining-days/03-04-SUMMARY.md` when done
</output>
