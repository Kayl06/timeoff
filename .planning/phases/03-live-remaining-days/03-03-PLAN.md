---
phase: 03-live-remaining-days
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/database/migrations/20250830120000_seed_default_leave_balances.sql
  - supabase/migrations/20250830120000_seed_default_leave_balances.sql
  - supabase/tests/seed_default_leave_balances.test.sql
autonomous: true
requirements:
  - BAL-02

user_setup:
  - service: supabase
    why: Apply onboarding RPC CREATE OR REPLACE locally; hosted project may lag (same as Phase 2 — hosted password is not a skip)
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "apps/web/.env.local for local API; supabase/config.toml project_id timeoff"
    dashboard_config:
      - task: "Local stack running (npm run supabase:start). Hosted db push is a separate human step if .env.local points at hosted."
        location: "repo root supabase/config.toml project_id timeoff"

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "create_company_with_owner inserts vacation/sick/personal leave_balances for the new owner in the same transaction, reading default_allowance from active leave_policies (BAL-02, D-01, D-13, D-14)."
    - "accept_invite_with_employee inserts the same three types for the new employee in the same transaction (BAL-02, D-13)."
    - "RPC inserts set used_days 0, remaining_days equal to default_allowance, carried_over 0, year EXTRACT(YEAR FROM CURRENT_DATE) (BAL-02, D-02, D-04)."
    - "ON CONFLICT (user_id, leave_type, year) DO NOTHING so existing used_days is never overwritten (BAL-02, D-07)."
    - "A type with no active policy row is skipped (BAL-02, D-15)."
    - "Both functions remain SECURITY DEFINER SET search_path = public and GRANT EXECUTE to anon, authenticated, service_role (D-13)."
    - "Flagged assumption BAL-01 unclassified (no SPEC): remaining-day card binds current-calendar-year rows for the signed-in user only; extra leave types and other users are not this card."
    - "Flagged assumption BAL-02 unclassified (no SPEC): default seed is insert-only for vacation/sick/personal when an active leave_policies row exists; unique (user_id, leave_type, year) conflict is ignored and never becomes an UPDATE of used_days."
    - "After load, zero rows (or fetch error) render Empty state body inside the still-visible Card. Do not hide the card. Do not paint 0-day bars without rows (D-08)."
    - "isLoading true shows the existing three-row pulse skeleton (h-4 + h-2 bg-gray-200, space-y-4). Title stays. No spinner, no mock numbers (D-09)."
    - "Failed GET: sonner Error state toast + same empty Card as empty (D-09). No mock 5/10 fallback. No inline red alert on the card."
    - "Typical volume: three rows (Vacation, Sick Leave, Personal) bound to live used_days, total_allowance, remaining_days. Seeded unused start is 0/{allowance} days and {allowance} days remaining."
    - "One or two of the three types present: render only those rows in vacation then sick then personal order. Missing types get no placeholder bar (D-12, D-15)."
    - "At most three rows this phase (space-y-6). Extra GET types are not rendered (D-12). No inner scroll on the card."
    - "Zero: empty copy. One: single type row. Two or three: stacked rows. Never four+ on this card."
    - "Empty state body wraps centered in py-8. Card width follows the Overview left column (lg:col-span-3). No horizontal clip."
    - "Copy is the fixed sentence in Empty state body. Do not truncate or ellipsis."
    - "Existing flex justify-between on each row. Numbers stay on one line; do not wrap the used/allowance pair onto the type name."
    - "Type names are the three fixed labels. Day counts are integers from the row. Do not ellipsis type names. Do not format with commas this phase."
  artifacts:
    - path: "packages/database/migrations/20250830120000_seed_default_leave_balances.sql"
      provides: "CREATE OR REPLACE create_company_with_owner and accept_invite_with_employee with policy-driven leave_balances INSERT"
      contains: "leave_policies"
    - path: "supabase/tests/seed_default_leave_balances.test.sql"
      provides: "pgTAP owner and invitee default balance rows plus conflict does not overwrite used_days"
      contains: "create_company_with_owner"
  key_links:
    - from: "packages/database/migrations/20250830120000_seed_default_leave_balances.sql"
      to: "packages/database/migrations/20250818193733_add_companies_and_invites.sql"
      via: "CREATE OR REPLACE copies owner insert then adds leave_balances INSERT before RETURN"
      pattern: "create_company_with_owner"
    - from: "packages/database/migrations/20250830120000_seed_default_leave_balances.sql"
      to: "packages/database/migrations/20250818193735_tenant_rls_and_anon_revoke.sql"
      via: "SECURITY DEFINER SET search_path = public plus GRANT EXECUTE re-applied"
      pattern: "search_path"
    - from: "supabase/tests/seed_default_leave_balances.test.sql"
      to: "packages/database/migrations/20250830120000_seed_default_leave_balances.sql"
      via: "supabase test db after db push --local"
      pattern: "leave_balances"
  prohibitions:
    - statement: "Do not restyle the dashboard Leave Balance card (spacing, colors, Progress, dots, or type labels)."
    - statement: "Do not fall back to mock 5/10 remaining-day numbers when GET is empty or fails."
    - statement: "Do not UPDATE existing leave_balances used_days or remaining_days this phase."
    - statement: "Do not seed maternity, paternity, bereavement, or unpaid balance rows this phase."
    - statement: "Do not use service_role for GET /api/leave-balances."
    - statement: "Do not drop Phase 2 GRANT/REVOKE or SECURITY DEFINER SET search_path = public on onboarding RPCs."
    - statement: "Do not add company_id columns to leave_balances or other child tables."
    - statement: "Do not deduct remaining days on approve, reject, or cancel (Phase 4)."
---

## Phase Goal

**As a** signed-in employee, **I want to** see my remaining vacation, sick, and personal days from live leave_balances on the dashboard, **so that** remaining days are real rows after signup or invite instead of empty mock cards.

<objective>
Onboarding RPCs seed default leave_balances in the same transaction as the new user. D-01: D-02: D-03: D-04: D-05: D-06: D-07: D-08: D-09: D-10: D-11: D-12: D-13: D-14: D-15: then [BLOCKING] local schema push so first-user signup and invite accept are not empty (BAL-02).

Purpose: GET self-heal covers existing Phase 1 users; new companies and invitees need rows at create time without a second round-trip.
Output: timestamped CREATE OR REPLACE migration, pgTAP seed file, local db push.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live-remaining-days/03-CONTEXT.md
@.planning/phases/03-live-remaining-days/03-PATTERNS.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-SECURITY.md
@packages/database/migrations/20250818193733_add_companies_and_invites.sql
@packages/database/migrations/20250818193734_accept_invite_with_employee.sql
@packages/database/migrations/20250818193735_tenant_rls_and_anon_revoke.sql
@packages/database/migrations/001_initial_schema.sql
@supabase/tests/tenant_rls.test.sql
@supabase/seed.sql
@package.json
</context>

<interfaces>
supabase/migrations is a symlink to packages/database/migrations. Write the SQL once at packages/database/migrations/20250830120000_seed_default_leave_balances.sql. Do not run scripts/create-migration.js (it would increment 20250818193735 numerically). Do not edit 001_initial_schema.sql, 20250818193733, 20250818193734, or 20250818193735 in place. Do not rewrite supabase/seed.sql demo used_days.

From 20250818193733 create_company_with_owner(TEXT, TEXT, TEXT, TEXT, TEXT) RETURNS users: copy the entire body (nullable p_password, insert company, insert admin user, set owner_id). Add leave_balances INSERT after owner_id update, before RETURN v_user.

From 20250818193734 accept_invite_with_employee(uuid, TEXT, TEXT, TEXT, TEXT, uuid) RETURNS users: copy the entire body (nullable p_password, not STRICT, RAISE invite_not_pending). Add the same leave_balances INSERT after the invite accepted update succeeds, still before RETURN v_user, still in the same transaction.

INSERT shape (D-01, D-02, D-03, D-04, D-07, D-14, D-15):
INSERT INTO public.leave_balances (user_id, leave_type, total_allowance, used_days, remaining_days, carried_over, year)
SELECT v_user.id, lp.leave_type, lp.default_allowance, 0, lp.default_allowance, 0, EXTRACT(YEAR FROM CURRENT_DATE)::integer
FROM public.leave_policies lp
WHERE lp.leave_type IN ('vacation', 'sick', 'personal') AND lp.is_active = true
If multiple active policies share a type, DISTINCT ON (leave_type) ORDER BY leave_type, name.
ON CONFLICT (user_id, leave_type, year) DO NOTHING.

From 20250818193735 lines 252-265: after both CREATE OR REPLACE, ALTER FUNCTION ... SECURITY DEFINER SET search_path = public and GRANT EXECUTE TO anon, authenticated, service_role for both signatures. Prefer SECURITY DEFINER SET search_path = public on the CREATE OR REPLACE FUNCTION line as well.

package.json: npm run supabase:db:push. Orchestrator requires local: npx supabase db push --local --yes. Do not use hosted supabase db push from this plan.
</interfaces>

## Artifacts this phase produces

- `packages/database/migrations/20250830120000_seed_default_leave_balances.sql`
- `supabase/tests/seed_default_leave_balances.test.sql`
- Local schema push of that migration

<tasks>

<task type="auto">
  <name>CREATE OR REPLACE onboarding RPCs with policy-driven balance seed</name>
  <reversibility rating="costly">SECURITY DEFINER RPC bodies are shared by signup and invite; changing seed later needs another CREATE OR REPLACE plus a data backfill for users already inserted (D-13).</reversibility>
  <files>packages/database/migrations/20250830120000_seed_default_leave_balances.sql</files>
  <read_first>packages/database/migrations/20250818193733_add_companies_and_invites.sql, packages/database/migrations/20250818193734_accept_invite_with_employee.sql, packages/database/migrations/20250818193735_tenant_rls_and_anon_revoke.sql, packages/database/migrations/001_initial_schema.sql, .planning/phases/03-live-remaining-days/03-PATTERNS.md</read_first>
  <action>
    Create packages/database/migrations/20250830120000_seed_default_leave_balances.sql. CREATE OR REPLACE both onboarding functions by copying their current bodies, then insert leave_balances from leave_policies as specified in interfaces (D-01, D-02, D-03, D-04, D-07, D-13, D-14, D-15). Do not hardcode 20, 10, or 5 as literals in the INSERT. Do not add STRICT. Keep p_password nullable. Do not add company_id on leave_balances.

    Put SECURITY DEFINER SET search_path = public on each CREATE OR REPLACE FUNCTION. Repeat ALTER FUNCTION ... SECURITY DEFINER SET search_path = public and GRANT EXECUTE ON FUNCTION ... TO anon, authenticated, service_role for both signatures (D-13). Do not REVOKE those grants. Do not change create-company-rpc.ts or accept-invite-rpc.ts signatures.
  </action>
  <verify>
    <automated>test -f packages/database/migrations/20250830120000_seed_default_leave_balances.sql && grep -c 'default_allowance' packages/database/migrations/20250830120000_seed_default_leave_balances.sql</automated>
  </verify>
  <acceptance_criteria>
    - file exists under packages/database/migrations/ with prefix 20250830120000
    - both function names are CREATE OR REPLACE in that file
    - INSERT selects default_allowance from leave_policies
    - file contains ON CONFLICT (user_id, leave_type, year) DO NOTHING
    - file contains SET search_path = public
    - grep of that file for DO UPDATE is 0
  </acceptance_criteria>
  <done>Both onboarding RPCs seed insert-only policy-driven balances in-transaction (BAL-02, D-13, D-14).</done>
</task>

<task type="auto">
  <name>pgTAP for owner/invitee seed and conflict skip</name>
  <files>supabase/tests/seed_default_leave_balances.test.sql</files>
  <read_first>supabase/tests/tenant_rls.test.sql, packages/database/migrations/001_initial_schema.sql</read_first>
  <action>
    Create supabase/tests/seed_default_leave_balances.test.sql for supabase test db (pgTAP), analog tenant_rls.test.sql (BEGIN, plan(N), unique emails).

    Assert create_company_with_owner for a unique email returns a users row and that user has three leave_balances rows for vacation, sick, personal for EXTRACT(YEAR FROM CURRENT_DATE), used_days 0, remaining_days equal to that type's active leave_policies.default_allowance.

    Assert accept_invite_with_employee after inserting a pending company_invites row similarly seeds three types for the new employee (D-13).

    Assert: set used_days on the owner's vacation row to a non-zero value, run the same INSERT...ON CONFLICT DO NOTHING pattern (or a second seed call that would conflict), and used_days is unchanged (D-07).

    Do not reset supabase/seed.sql demo rows. Do not add Vitest.
  </action>
  <verify>
    <automated>test -f supabase/tests/seed_default_leave_balances.test.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/tests/seed_default_leave_balances.test.sql exists
    - file calls create_company_with_owner and accept_invite_with_employee
    - file asserts used_days stays non-zero after a conflicting insert
  </acceptance_criteria>
  <done>pgTAP file is ready to run after push (BAL-02, D-07, D-13).</done>
</task>

<task type="auto">
  <name>[BLOCKING] Push seed RPC migration locally and run pgTAP</name>
  <precondition>Local Supabase is running (npm run supabase:start) and apps/web/.env.local has NEXT_PUBLIC_SUPABASE_URL plus the anon key; do not claim hosted is updated.</precondition>
  <files>packages/database/migrations/20250830120000_seed_default_leave_balances.sql, supabase/tests/seed_default_leave_balances.test.sql</files>
  <read_first>package.json, packages/database/migrations/20250830120000_seed_default_leave_balances.sql</read_first>
  <action>
    After the migration file from the first task exists, apply it before treating onboarding seed as done.

    From repo root run npx supabase db push --local --yes. If the CLI rejects --local, use npm run supabase:db:push -- --yes against the local stack from supabase/config.toml. If the CLI still prompts, stop — this plan is autonomous only if the CLI accepts --yes; do not invent a different runner. Do not skip because hosted lags. Do not run hosted supabase db push.

    Then npx supabase test db and confirm seed_default_leave_balances.test.sql and tenant_rls.test.sql still pass (GRANT/REVOKE and search_path from Phase 2 must still hold). Then npm test --workspace=@timeoff/web and npm test --workspace=@timeoff/database.
  </action>
  <verify>
    <automated>npx supabase db push --local --yes && npx supabase test db && npm test --workspace=@timeoff/web && npm test --workspace=@timeoff/database</automated>
  </verify>
  <acceptance_criteria>
    - npx supabase db push --local --yes (or the documented fallback) applied 20250830120000_seed_default_leave_balances.sql
    - supabase test db exits 0 including seed_default_leave_balances.test.sql and tenant_rls.test.sql
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Local Postgres seeds default balances on signup and invite accept without overwriting existing used_days (BAL-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SECURITY DEFINER RPCs | Run as owner; must insert only v_user.id created in the same transaction |
| PostgREST as authenticated | Tenant JWT; RLS still filters leave_balances by company |
| Identity routes | signup/accept already call these RPCs via identitySupabase; SQL INSERT needs no new TS args |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | create_company_with_owner / accept_invite_with_employee | high | mitigate | INSERT user_id is v_user.id from the just-inserted row; not a parameter choosing another user |
| T-03-02 | Tampering | leave_balances INSERT in RPCs | high | mitigate | ON CONFLICT DO NOTHING; no UPDATE of used_days (D-07) |
| T-03-05 | Elevation of Privilege | onboarding RPCs | high | mitigate | SECURITY DEFINER SET search_path = public; GRANT EXECUTE unchanged from Phase 2 (D-13) |
| T-03-03 | Information Disclosure | GET remaining-days | high | mitigate | This plan does not change GET /api/leave-balances to identitySupabase |
| T-03-04 | Information Disclosure | RPC RETURNS users | medium | mitigate | Keep existing RETURNS users; do not expand to select password into JSON from this SQL (callers already omit password in BFF) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new package-manager installs this plan |

</threat_model>

<verification>
Local db push applied. supabase test db green including seed_default_leave_balances.test.sql and existing tenant_rls.test.sql. npm test --workspace=@timeoff/web green. Migration INSERT reads leave_policies.default_allowance and uses ON CONFLICT DO NOTHING.
</verification>

<success_criteria>
First user of a new company and invited users have default leave_balances so remaining days are not empty; existing used_days rows stay as-is.
</success_criteria>

<output>
Create `.planning/phases/03-live-remaining-days/03-03-SUMMARY.md` when done
</output>
