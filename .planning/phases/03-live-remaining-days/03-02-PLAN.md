---
phase: 03-live-remaining-days
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/database/src/modules/leave-balances/repository.ts
  - packages/database/src/modules/leave-balances/service.ts
  - packages/database/src/index.ts
  - apps/web/src/app/api/leave-balances/route.ts
  - apps/web/src/components/dashboard/leave-balance-card.tsx
autonomous: true
requirements:
  - BAL-01
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /api/leave-balances with a valid session inserts missing vacation/sick/personal rows for session.user.id and the current calendar year only, then returns that user's rows (BAL-01, D-04, D-05, D-06)."
    - "Self-heal inserts use leave_policies.default_allowance via getLeavePolicies plus planDefaultBalanceInserts; used_days starts at 0 and remaining_days equals total_allowance (BAL-01, D-01, D-02, D-14)."
    - "A row that already exists for (user_id, leave_type, year) is not updated; duplicate insert is treated as success and GET re-selects (BAL-01, D-07)."
    - "Leave Balance card populated rows bind used_days, total_allowance, and remaining_days from the leaveBalance prop; remaining label uses remaining_days (BAL-01, D-10, D-11)."
    - "GET /api/leave-balances uses createTenantDatabaseService (tenant JWT + RLS), not the identity client (BAL-01)."
    - "Flagged assumption BAL-01 unclassified (no SPEC): remaining-day card binds current-calendar-year rows for the signed-in user only; extra leave types and other users are not this card."
    - "Flagged assumption BAL-02 unclassified (no SPEC): default seed is insert-only for vacation/sick/personal when an active leave_policies row exists; unique (user_id, leave_type, year) conflict is ignored and never becomes an UPDATE of used_days."
    - "After load, zero rows (or fetch error) render Empty state body inside the still-visible Card. Do not hide the card. Do not paint 0-day bars without rows (D-08)."
    - "isLoading true shows the existing three-row pulse skeleton (h-4 + h-2 bg-gray-200, space-y-4). Title stays. No spinner, no mock numbers (D-09)."
    - "Failed GET: sonner Error state toast + same empty Card as empty (D-09). No mock 5/10 fallback. No inline red alert on the card."
    - "Typical volume: three rows (Vacation, Sick Leave, Personal) bound to live used_days, total_allowance, remaining_days. Seeded unused start is 0/{allowance} days and {allowance} days remaining."
    - "One or two of the three types present: render only those rows in vacation then sick then personal order. Missing types get no placeholder bar (D-12, D-15)."
    - "At most three rows this phase (space-y-6). Extra GET types are not rendered (D-12). No inner scroll on the card."
    - "Zero: empty copy. One: single type row. Two or three: stacked rows. Never four+ on this card."
    - "Empty state body wraps centered in py-8. Card width follows the Overview left column (lg:col-span-3). No horizontal clip."
    - "Copy is the fixed sentence in Empty state body. Do not truncate or ellipsis."
    - "Existing flex justify-between on each row. Numbers stay on one line; do not wrap the used/allowance pair onto the type name."
    - "Type names are the three fixed labels. Day counts are integers from the row. Do not ellipsis type names. Do not format with commas this phase."
  artifacts:
    - path: "packages/database/src/modules/leave-balances/repository.ts"
      provides: "insert-only create on leave_balances"
      exports: ["LeaveBalanceRepository"]
    - path: "packages/database/src/modules/leave-balances/service.ts"
      provides: "ensureDefaultBalances"
      contains: "ensureDefaultBalances"
    - path: "packages/database/src/index.ts"
      provides: "ensureDefaultLeaveBalances on IDatabaseService"
      contains: "ensureDefaultLeaveBalances"
    - path: "apps/web/src/app/api/leave-balances/route.ts"
      provides: "GET self-heal then list"
      exports: ["GET"]
    - path: "apps/web/src/components/dashboard/leave-balance-card.tsx"
      provides: "LeaveBalanceCard bound to fetched remaining_days"
      contains: "remaining_days"
  key_links:
    - from: "apps/web/src/app/api/leave-balances/route.ts"
      to: "apps/web/src/lib/tenant-supabase.ts"
      via: "createTenantDatabaseService after requireTenantSession"
      pattern: "createTenantDatabaseService"
    - from: "apps/web/src/app/api/leave-balances/route.ts"
      to: "packages/database/src/index.ts"
      via: "ensureDefaultLeaveBalances(session.user.id, year, policies) then getLeaveBalance"
      pattern: "ensureDefaultLeaveBalances"
    - from: "apps/web/src/components/dashboard/leave-balance-card.tsx"
      to: "apps/web/src/hooks/use-dashboard-data.ts"
      via: "leaveBalance prop already passed from dashboard-overview"
      pattern: "leaveBalance"
    - from: "packages/database/src/modules/leave-balances/service.ts"
      to: "packages/database/src/modules/leave-balances/plan-default-inserts.ts"
      via: "planDefaultBalanceInserts then repository.create"
      pattern: "planDefaultBalanceInserts"
  prohibitions:
    - statement: "Do not restyle the dashboard Leave Balance card (spacing, colors, Progress, dots, or type labels)."
    - statement: "Do not fall back to mock 5/10 remaining-day numbers when GET is empty or fails."
    - statement: "Do not UPDATE existing leave_balances used_days or remaining_days this phase."
    - statement: "Do not seed maternity, paternity, bereavement, or unpaid balance rows this phase."
    - statement: "Do not use service_role for GET /api/leave-balances."
    - statement: "Do not drop Phase 2 GRANT/REVOKE or SECURITY DEFINER SET search_path = public on onboarding RPCs."
    - statement: "Do not add company_id columns to leave_balances or other child tables."
    - statement: "Do not deduct remaining days on approve, reject, or cancel (Phase 4)."
---

## Phase Goal

**As a** signed-in employee, **I want to** see my remaining vacation, sick, and personal days from live leave_balances on the dashboard, **so that** remaining days are real rows after signup or invite instead of empty mock cards.

<objective>
End-to-end tracer: GET /api/leave-balances self-heals insert-only default rows for the signed-in user. D-01: D-02: D-03: D-04: D-05: D-06: D-07: D-08: D-09: D-10: D-11: D-12: D-13: D-14: D-15: then the Leave Balance card binds live remaining_days so a Phase 1 user with no current-year rows sees catalog remaining days after dashboard load (BAL-01).

Purpose: Prove BFF + tenant JWT + insert-only seed + card binding before RPC onboarding seed and empty/error polish.
Output: LeaveBalanceRepository.create, ensureDefaultBalances, IDatabaseService.ensureDefaultLeaveBalances, GET self-heal, card bound to fetched rows.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-remaining-days/03-CONTEXT.md
@.planning/phases/03-live-remaining-days/03-PATTERNS.md
@.planning/phases/02-tenant-isolation-and-server-authz/02-SECURITY.md
@apps/web/src/app/api/leave-balances/route.ts
@apps/web/src/app/api/leave-policies/route.ts
@apps/web/src/app/api/notifications/route.ts
@apps/web/src/lib/tenant-supabase.ts
@packages/database/src/modules/leave-balances/repository.ts
@packages/database/src/modules/leave-balances/service.ts
@packages/database/src/modules/leave-balances/plan-default-inserts.ts
@packages/database/src/modules/leave-policies/repository.ts
@packages/database/src/index.ts
@apps/web/src/components/dashboard/leave-balance-card.tsx
</context>

<interfaces>
From apps/web/src/app/api/leave-balances/route.ts:
Keep requireTenantSession, UNAUTHORIZED 401, createTenantDatabaseService({ userId, companyId }), year = new Date().getFullYear(), getLeaveBalance, catch 500 Internal server error.
Self-heal after tenant client, before the final getLeaveBalance return: getLeavePolicies() then ensureDefaultLeaveBalances(session.user.id, year, policies) then getLeaveBalance again. Insert user_id must be session.user.id (D-06).

From packages/database/src/index.ts:
getLeaveBalance(userId, year), getLeavePolicies(), updateLeaveBalance (existing; delegates to createLeaveBalance upsert — do not call it for self-heal).
Add ensureDefaultLeaveBalances(userId, year, policies) returning Promise of void next to getLeaveBalance on IDatabaseService and DatabaseService.

From packages/database/src/modules/leave-policies/repository.ts:
findByLeaveType filters is_active true and order name. getLeavePolicies facade is getActivePolicies.

From packages/database/src/modules/leave-requests/repository.ts create:
insert sanitizedData, select, single, handleDatabaseError. Copy that shape for leave_balances. Required fields user_id, leave_type, total_allowance, year.

From packages/database/src/modules/shared/utils.ts:
Postgres 23505 maps to ServiceError.code DUPLICATE_ENTRY. Self-heal catch that code and continue, then re-select.

From 03-01:
planDefaultBalanceInserts(existing, policies, userId, year)
</interfaces>

## Artifacts this phase produces

- `LeaveBalanceRepository.create` (insert, not upsert)
- `LeaveBalanceService.ensureDefaultBalances`
- `IDatabaseService.ensureDefaultLeaveBalances`
- GET `/api/leave-balances` self-heal for `session.user.id`
- Leave Balance card populated from `leaveBalance` prop using `remaining_days`

<tasks>

<task type="tracer">
  <name>End-to-end live remaining days — GET self-heal then card bind</name>
  <precondition>03-01 exported planDefaultBalanceInserts and balancesForLeaveCard; Phase 2 createTenantDatabaseService and GET leave-balances session gate exist.</precondition>
  <reversibility rating="costly">Self-heal inserts durable leave_balances rows for existing Phase 1 users (D-05); undoing means deleting those rows or a reverse migration, and Phase 4 will deduct from them.</reversibility>
  <files>packages/database/src/modules/leave-balances/repository.ts, packages/database/src/modules/leave-balances/service.ts, packages/database/src/index.ts, apps/web/src/app/api/leave-balances/route.ts, apps/web/src/components/dashboard/leave-balance-card.tsx</files>
  <read_first>packages/database/src/modules/leave-policies/repository.ts, packages/database/src/modules/leave-requests/repository.ts, packages/database/src/modules/leave-balances/plan-default-inserts.ts, apps/web/src/app/api/leave-balances/route.ts, apps/web/src/lib/tenant-supabase.ts, apps/web/src/components/dashboard/leave-balance-card.tsx, .planning/phases/02-tenant-isolation-and-server-authz/02-SECURITY.md</read_first>
  <action>
    Add LeaveBalanceRepository.create that inserts into leave_balances (same validateRequiredFields as the existing upsert method: user_id, leave_type, total_allowance, year), sanitizeData, insert, select, single. Do not copy this file's upsert into create. Keep the existing upsert method for createLeaveBalance callers.

    Add LeaveBalanceService.ensureDefaultBalances(userId, year, policies): findByUserId, planDefaultBalanceInserts per D-01 D-02 D-03 D-07 D-14 D-15, then create each planned row. Catch ServiceError code DUPLICATE_ENTRY and continue (concurrent GET vs RPC). Do not call repository.update or the upsert method from ensureDefaultBalances. Leave updateBalanceAfterApproval unchanged (Phase 4).

    Add ensureDefaultLeaveBalances on IDatabaseService and DatabaseService delegating to the leave-balance service. Do not route self-heal through updateLeaveBalance.

    In GET /api/leave-balances keep requireTenantSession and createTenantDatabaseService. After the tenant client: year = new Date().getFullYear() (D-04), policies = getLeavePolicies(), ensureDefaultLeaveBalances(session.user.id, year, policies) (D-05, D-06), then getLeaveBalance(session.user.id, year) and return JSON. Do not import identitySupabase. Do not add a users embed or password field to the JSON.

    On LeaveBalanceCard: delete the in-card hardcoded LeaveBalance array (D-10). Map the leaveBalance prop for the populated branch. Render remaining label from remaining_days (D-11). Keep Card/Progress/dot/Calendar chrome, pulse skeleton, getLeaveTypeDisplayName, getLeaveTypeColor, Progress as used_days/total_allowance, carried_over branch. Do not uncomment empty copy this plan (03-04). Do not restyle. Do not edit dashboard-overview.tsx.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web && npm test --workspace=@timeoff/database</automated>
  </verify>
  <acceptance_criteria>
    - LeaveBalanceRepository.create uses insert
    - IDatabaseService lists ensureDefaultLeaveBalances
    - GET handler calls ensureDefaultLeaveBalances with session.user.id and createTenantDatabaseService
    - leave-balance-card.tsx contains remaining_days and does not contain the identifier mockLeaveBalance
    - npm test --workspace=@timeoff/web exits 0
    - npm test --workspace=@timeoff/database exits 0
  </acceptance_criteria>
  <done>A signed-in user with no current-year rows gets insert-only defaults on GET and the card shows remaining_days from fetched rows (BAL-01, D-05, D-06, D-07, D-10, D-11).</done>
</task>

<task type="auto">
  <name>Self-heal duplicate race stays insert-only</name>
  <files>packages/database/src/modules/leave-balances/service.ts, apps/web/src/app/api/leave-balances/route.ts</files>
  <read_first>packages/database/src/modules/shared/utils.ts, packages/database/src/modules/leave-balances/service.ts, apps/web/src/app/api/leave-balances/route.ts</read_first>
  <action>
    Confirm ensureDefaultBalances never calls updateBalanceAfterApproval, repository.update, or createLeaveBalance. Duplicate unique (user_id, leave_type, year) must continue then the route re-fetches getLeaveBalance. Confirm GET passes only session.user.id as the userId (no company member loop). Skip types with no active policy because planDefaultBalanceInserts already does (D-15). Keep the 500 catch and 401 gate unchanged.
  </action>
  <verify>
    <automated>npm test --workspace=@timeoff/web && npm test --workspace=@timeoff/database</automated>
  </verify>
  <acceptance_criteria>
    - ensureDefaultBalances catch path treats DUPLICATE_ENTRY as continue
    - leave-balances/route.ts grep -c identitySupabase is 0
    - leave-balances/route.ts grep -c updateLeaveBalance is 0
    - npm test --workspace=@timeoff/web exits 0
  </acceptance_criteria>
  <done>Concurrent GET vs RPC cannot overwrite used_days; GET stays on the tenant client (D-07, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/leave-balances | Session cookie; no body. Self-heal must not accept a target user id from the client |
| BFF → PostgREST | Minted authenticated JWT; RLS leave_balances_tenant_all USING/WITH CHECK same company |
| leave_policies catalog | Authenticated SELECT USING (true); default_allowance is not a tenant secret but must not be hardcoded in SQL this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | GET /api/leave-balances | high | mitigate | Insert user_id is session.user.id only; no company-wide backfill on dashboard load (D-06) |
| T-03-02 | Tampering | ensureDefaultBalances | high | mitigate | repository.create insert only; DUPLICATE_ENTRY continue; never upsert or update existing used_days (D-07) |
| T-03-03 | Information Disclosure | GET /api/leave-balances | high | mitigate | createTenantDatabaseService; do not import identitySupabase |
| T-03-04 | Information Disclosure | GET JSON | high | mitigate | Return leave_balances rows only; no users embed and no password field |
| T-03-05 | Elevation of Privilege | identity RPCs | high | mitigate | This plan does not call create_company_with_owner or accept_invite_with_employee as a tenant data path |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No new package-manager installs this plan |

</threat_model>

<verification>
npm test --workspace=@timeoff/web and npm test --workspace=@timeoff/database exit 0. GET route uses createTenantDatabaseService and ensureDefaultLeaveBalances(session.user.id). Card populated branch reads remaining_days from props. Empty copy, fetch toast, and type-order filter land in 03-04. RPC onboarding seed lands in 03-03.
</verification>

<success_criteria>
Dashboard remaining days for the signed-in user come from live leave_balances after GET self-heal; existing used_days rows are not overwritten.
</success_criteria>

<output>
Create `.planning/phases/03-live-remaining-days/03-02-SUMMARY.md` when done
</output>
